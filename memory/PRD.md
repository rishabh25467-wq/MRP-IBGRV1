# PRD - SAP BOM Viewer / Materials Hub

## Original problem statement
Extend the existing SAP BOM viewer application: Production Plan page (OMS Open-PO Demand feed), 2-Tier MRP, Microsoft Entra ID (Azure AD) SSO, Supplier Portal for external vendors, PO Creation automation, Inbound STO Receipt page (groups multiple SAP delivery lines), native SAP Business ByDesign Custom Business Object (ABSL) replacing Playwright for multi-line STO Outbound Deliveries. Most recent asks (Sep 2026): Supplier Portal entity restriction + GRN Site/Warehouse auto-derivation + JDE theme visual pass, and a Production Confirmation bug (Net Weight wrongly mandatory for hardware-kit BOMs).

## Architecture
- `/app/backend/`: FastAPI + MongoDB. Key files: `server.py` (routes, incl. `_pick_rm_item`/`_compute_scrap_calc` scrap-weight logic), `supplier_shipment_service.py` (vendor shipment lifecycle), `sap_po_client.py` (PO cache/buyer entity), `sap_wip_clearing_client.py` (site->company RI/RT mapping), `sap_valuation_client.py` (pricing - has an open bug, see backlog).
- `/app/frontend/src/`: React. `GrnApprovalPage.jsx` (internal GRN approval), `pages/supplier-portal/*` (vendor-facing: Dashboard, Shipments, Login, Signup, Pending).
- Theme: JDE Enterprise ERP (`/app/design_guidelines.json`) - Chivo headings, IBM Plex Sans body, JetBrains Mono data, #004B87 primary, #0E7C86 header teal, #EAECF0 table headers, grid-bordered zebra tables. `tailwind.config.js` defines `fontFamily.heading/sans/data`.

## FEATURE: Open Purchase Orders page - search by PO number + real SAP status (Sep 14, 2026 session, same-day follow-up)
- **Search by PO number**: new sibling search box next to the existing Supplier search (`/purchase-orders/lookup-by-number` endpoint, `get_cached_po_by_number` in supplier_shipment_service.py) - looks up any PO's cached open lines directly, across any vendor, no need to pick a vendor first. Screenshot-verified live (PO 29482, 5 lines).
- **Real PO status badge**: user's ask "need to see status of PO for now" - `sap_po_client.py`'s `fetch_recent_window` now also captures `PurchaseOrderLifeCycleStatusCode` per PO and maps it to friendly text via the full SAP ByDesign code list (1=In Preparation...10=Finished), stored as `lifecycle_status_code`/`lifecycle_status_text` on every cached row (flows through automatically via the existing `{**it}` cache-write spread, no schema migration needed). Frontend badge on the Open Purchase Orders page now shows the real value (e.g. "Follow-Up Document Created") instead of a generic "Open in SAP" placeholder. Screenshot-verified live after a manual cache refresh.
- **Answered (no build yet, pending user's choice)**: Edit PO and Cancel PO are NOT supported anywhere in the app - would need a new SAP write action each (`ManagePurchaseOrderIn`, actionCode "02" for update; a real cancel action) plus scope decisions on which fields are editable. User has not yet confirmed whether to build these.

## INVESTIGATION + FIX: Supplier Dashboard PO validity check + Rejected POs now also excluded (Sep 14, 2026 session, same-day follow-up)
- **User's ask**: re-verify that everything currently visible on the Supplier Dashboard (real screenshot, vendor RAD-P2-S, POs 29395/29429/29430/29431/29439/29456/29482/29510) is genuinely valid per SAP, not cancelled/in-preparation.
- **Live SAP investigation (read-only)**: queried all 8 PO numbers directly against `SAP_SOAP_PO_ENDPOINT` - every one has `ApprovalStatusCode="2"` (Released) and `PurchaseOrderLifeCycleStatusCode` either `"6"` (Sent) or `"9"` (Follow-Up Document Created, per SAP ByDesign's own code list: 1=In Preparation, 4=Rejected, 8=Canceled, 10=Finished - everything else is a normal active stage). **Conclusion: all 8 are genuinely valid, open POs** - correctly NOT excluded by the existing filter. PO 29482 showing 5 rows is legitimate too (5 real delivery line items, same product, different due dates/remaining qty - same pattern as the already-documented PO 29455 case).
- **Follow-up fix (user's explicit "yes")**: `PurchaseOrderLifeCycleStatusCode="4"` (Rejected) is now also excluded in `fetch_recent_window` (`sap_po_client.py`, new `REJECTED_LIFECYCLE_STATUS_CODE`), alongside the existing Cancelled(8)/In-Preparation(1) exclusions - proactive, no live Rejected PO existed in the current window to remediate.

## FIX + FEATURE + INVESTIGATION (Sep 14, 2026 session)
- **Issue 1, CORRECTED (P0)**: the first attempt (checking the list row's own text for "Finished", then a follow-up that tried clicking a notification ID link to "navigate into" a detail screen) was proven wrong by the user's own live screenshot of the real failure - PO 29482, notification `TEST_0506-WFRRSL-29482`. **Real root cause**: "Delivery Status" is never one of the Inbound Delivery Notifications list's own columns (only ID/Notification Status/Release Status/Planned Delivery/Sender/Created By/Delivery Type show there) - it only ever renders in the "Details: Delivery Notification ..." panel SAP's own master-detail layout shows BELOW the list the INSTANT a row is selected (already done by `_click_po_row`, before this check even runs) - no link, no navigation, no extra click needed at all; the first fix's ID-link click was a no-op because no such link exists in the row. **Fix**: new `_is_notification_finished_in_details_panel()` (sap_playwright_supplier_pgr_service.py) reads the WHOLE page's text (not just the row's), scans line-by-line for a line containing "Delivery Status", and checks that same line (or the next, for a stacked label/value layout) for "Finished" - deliberately not a blind "finish" anywhere on the page, since a status filter dropdown's own hidden option list can genuinely contain that word regardless of the real document status. Unit-tested (9/9 pass, `tests/test_finished_notification_detection_iter166.py`), including a direct repro of the user's exact PO 29482 screenshot text. **Still NOT live-verified against real SAP** - needs a real GRN retry against notification `TEST_0506-WFRRSL-29482` (or any other already-Finished PO) to confirm end to end.
- **Feature**: Confirmed GRN detail dialog's item table (`GrnApprovalPage.jsx`) was missing a per-row "PO Number" column (had one only in the Pending Shipments panel) - added (`grn-confirmed-detail-po-number-*`). Screenshot-verified live.
- **Investigated PO 29480 "still shows cancelled" report**: confirmed directly in this preview's DB that all 5 of PO 29480's `supplier_portal_po_cache` rows are already `expired: True` (the Sep 13 fix's exclusion + remediation is active and working here), and the background PO-cache refresh loop's watermark is fresh (updated minutes ago). This preview environment is correctly NOT showing PO 29480 anymore. If the user is still seeing it live, that's the already-flagged production/preview deployment sync gap (production needs a redeploy via "Save to GitHub" to pick up the Sep 13 fix) - not a new code bug.
- **Concurrency question answered (no code change needed)**: `playwright_concurrency.py` already caps Playwright/SAP-UI automation at 3 concurrent sessions (dedicated SAP login pool: itadmin/STOREBOT1/STOREBOT2) - a 4th+ concurrent GRN/receipt job queues automatically (never races/collides on a shared SAP login) via `GET /api/system/job-concurrency`, surfaced live in the frontend's `ConcurrencyBadge.jsx`. This already covers "many users at once" for the Playwright-driven flows; non-Playwright API endpoints have no artificial concurrency cap beyond `sap_rate_limiter.py`'s SOAP/OData semaphore.

## VERIFIED: GRN Retry always refreshes to the latest attempt's diagnostics, no stale data (Sep 13, 2026 session, same-day follow-up)

## VERIFIED: GRN Retry always refreshes to the latest attempt's diagnostics, no stale data (Sep 13, 2026 session, same-day follow-up)
- User asked for 100% verification that Retry shows the LATEST result+diagnostics (not the previous failed attempt's stale data) - this was the subject of a real Sep 12 2026 fix, needed certainty it still holds.
- testing_agent verified BOTH Retry entry points end-to-end with mocked retry-job network responses (no real SAP/Playwright call): top-level pending-lookup `retryGoodsReceipt` and the Confirmed-detail dialog's `retryConfirmedGoodsReceipt`. Both correctly poll the real job to completion, replace shipment/dialog state with the fresh result, and auto-open Diagnostics on any non-posted outcome. A full page-body text scan after each retry confirmed the OLD diagnostics text was completely gone - state is replaced, not merged/appended. See `/app/test_reports/iteration_165.json`.
- **Minor UX inconsistency found (not fixed, flagged for product decision)**: once `sap_sync_status` is `"failed"` (after `MAX_GR_RETRIES_BEFORE_FAILED`), the top-level lookup detail panel hides "Retry" behind "Reset Retry" (forces SAP Admin intervention first) - but the Confirmed-detail dialog still shows a plain "Retry" button in that same state. Two UIs currently disagree on whether a fully-failed shipment should offer Retry directly. Not changed without user confirmation on intended behavior.
- **Fixed the UX inconsistency** (Sep 13 2026, same-day follow-up, user's ask "align it and choose best for users interest"): chose the SAFER of the two behaviors as the standard everywhere - the Confirmed-detail dialog now also hides plain "Retry" behind "Reset Retry" (new `resetConfirmedRetry`, `grn-confirmed-detail-reset-retry-button`) once `sap_sync_status === "failed"`, matching the top-level panel. Rationale: once staff hit `MAX_GR_RETRIES_BEFORE_FAILED`, the same unresolved SAP-side error will just keep reproducing - forcing a SAP Admin sign-off first (via Reset Retry) before allowing more retries protects against wasted attempts/stuck SAP drafts, in both places consistently now. Verified live via screenshot on the TSTRTY fixture.

## FEATURE: Supplier Invoice Number input capped at 14 characters (Sep 13, 2026 session, same-day follow-up)
- GRN Approval page's "Supplier Invoice Number" field (GrnApprovalPage.jsx, `grn-supplier-doc-num-input`) now hard-caps at 14 characters (`maxLength={14}` + `.slice(0, 14)` on change) - any characters still allowed (not digits-only, per user's explicit confirmation), just length-limited.
- Verified live via screenshot: typing 18 characters into the field results in exactly 14 characters retained.

## FEATURE: SAP Delivery Notification ID now includes the Supplier Invoice No for traceability (Sep 13, 2026 session, same-day follow-up)
- **User's ask** ("Can we go for a number like inv number / doc code"): the SAP Notification ID (e.g. `9TJP96-29510`) didn't reflect the supplier's own typed Invoice No (`GRN_TEST_10`) at all, purely by design from the Sep 12 collision fix - user wanted the invoice number visible again without reopening that bug.
- **Fix**: new `_build_notification_id(supplier_doc_num, doc_code, po_number)` helper (sap_playwright_supplier_pgr_service.py) now builds `{sanitized_invoice_no}-{doc_code}-{po_number}` - the `{doc_code}-{po_number}` tail is ALWAYS kept intact as the uniqueness guarantee (same as the Sep 12 fix), invoice number is just a prepended, sanitized (SAP ID fields don't reliably take spaces/slashes), length-capped (~35 chars, SAP's real BusinessTransactionDocumentID limit) prefix. Falls back to the old `{doc_code}-{po_number}`-only ID when the invoice number is blank.
- Verified via isolated unit tests: normal invoice number, blank/whitespace-only (fallback), special characters (sanitized), and an oversized invoice number (truncated, suffix never lost) - all stayed under 35 chars and always ended with the guaranteed-unique suffix. Only affects NEW GRN runs going forward - existing already-created SAP notifications (e.g. `9TJP96-29510`) are untouched.

## BUG FIX (CONFIRMED root cause + live SAP verification): Unreleased ("In Preparation") POs were visible to suppliers before buyer approval (Sep 13, 2026 session, same-day follow-up)
- **User's question** ("Why is it visible to supplier in their vendor control?") after seeing "PO not yet Released ('In Preparation')" listed as a real failure cause - led to checking whether `fetch_recent_window` filters on release status at all. It didn't.
- **Root cause, confirmed live**: queried a real 500-PO window and grouped by `(LifeCycle, DeliveryProcessing, ApprovalStatusCode)` - found `ApprovalStatusCode = "1"` (not yet approved/released by the buyer) paired ONLY with genuine draft POs (LifeCycle=1, Delivery=1), 42 real POs in this state across dozens of vendors. Every other PO (open/in-process/finished/cancelled) had `ApprovalStatusCode = "2"` (Released). This field was never checked, so a PO still in draft was fully visible and shippable-against on the Supplier Dashboard the moment it existed in SAP - before the buyer had actually released it.
- **Fix**: added `NOT_YET_RELEASED_APPROVAL_STATUS_CODE = "1"` and excluded it in `fetch_recent_window` alongside the Finished/Cancelled checks.
- **Immediate remediation**: expired all 88 already-cached `supplier_portal_po_cache` line items for those 42 draft POs right away. Verified live: vendor V2107's previously-visible draft POs (29273, 29278, 29262, 29115, 29121, 29507, etc.) no longer appear via `get_cached_pos_with_remaining`.

## BUG FIX (CONFIRMED root cause + live SAP verification): Cancelled PO still showed as "pending" on Supplier Dashboard (Sep 13, 2026 session, same-day follow-up)
- **User report**: PO 29480 is "Canceled" in SAP (header + all 4 item lines) but still showed as an open/pending PO on vendor RAD-P2-S's Supplier Dashboard, confusing the supplier.
- **Root cause, confirmed live**: `sap_po_client.fetch_recent_window` only ever excluded `DeliveryProcessingStatusCode == "3"` (Finished). Queried this tenant's real response for PO 29480: `DeliveryProcessingStatusCode` was `4` (not `3`) and `PurchaseOrderLifeCycleStatusCode` was `8` - a genuinely different, never-checked field. Cross-checked a 500-ID window of real POs: the (LifeCycle=8, Delivery=4) pair was unique to exactly 3 POs (29480, 29275, 29179) - every other combination was a real open/in-process/finished PO.
- **Fix**: added `CANCELLED_LIFECYCLE_STATUS_CODE = "8"` and excluded it in `fetch_recent_window` alongside the existing Finished check, so a cancelled PO is dropped from the vendor's cache on the very next background refresh going forward.
- **Immediate remediation**: directly marked all 8 already-cached `supplier_portal_po_cache` rows for the 3 currently-cancelled POs (29480, 29275, 29179) as `expired: True` in Mongo so they disappear right away instead of waiting for the cache's normal missing/stale-cycle heuristic (~30 min).
- Verified live: `get_cached_pos_with_remaining(db, "RAD-P2-S")` no longer returns PO 29480.

## FIX: Inbound Delivery Notifications screen's SAP default view changed to "All Delivery Notifications by Selection" (Sep 13, 2026 session, same-day follow-up)
- SAP Admin changed this screen's own personalization so it now defaults to "All Delivery Notifications by Selection" directly (was "Advised Delivery Notifications"). `_switch_to_all_deliveries_view` (sap_playwright_supplier_pgr_service.py) now checks first if that view is already showing and returns success immediately with zero clicks/retries - falls back to the old click-based switch only if it isn't (safety net for a different SAP login or if personalization reverts). Avoids the previous ~16s wasted retry loop + confusing "Could not switch view" event on every PO now that the old default no longer exists. Verified via isolated unit test (both the already-correct short-circuit and the fallback path).

## FEATURE + BUG FIX: GRN line-level skipping (only drop the bad line, not the whole PO) + upfront Supplier Portal product_id guard (Sep 13, 2026 session)
- **User's explicit ask**: "If 1 PO or 1 line is skipped, other POs should get written with GRN completed." Previously, `_post_one_po` (sap_playwright_supplier_pgr_service.py) aborted the ENTIRE PO the moment ANY one line was missing a cached `product_id`, even when the rest of that PO's lines were perfectly fine.
- **Fix**: `missing_products` lines are now filtered OUT of `item_qtys`/`item_products` before the SOAP `maintain_bundle` create and before `_fill_line_actual_quantities` - only the bad line(s) are dropped, the rest of the PO still posts (`status: "posted"`) and carries a new `skipped_items` list (`item_number` + `reason`). The whole PO only returns `status: "skipped"` when EVERY line on it is missing `product_id` (unchanged legacy behavior for that edge case).
- **Downstream fix**: `_post_goods_movement_for_items`/new helper `_skipped_line_items_from_gr_results` (supplier_shipment_service.py) now skip the Goods Movement/stock-move call for exactly those dropped `(po_number, item_number)` pairs (since no GR was ever posted for them in SAP) instead of blindly moving every shipment line.
- **Root-cause fix (user's follow-up question, "why did this delivery come in when PO is not valid")**: `_resolve_items` (shared by `create_shipment`/`update_shipment_items`) never checked the cached `product_id` before letting a supplier add a PO line to a shipment - now raises `ShipmentValidationError` (HTTP 400) upfront if it's missing, pointing to the SAP Admin/product-master-link gap, so bad lines can no longer even reach GRN.
- Frontend: GrnApprovalPage.jsx's "View Diagnostics" button now also shows for an overall-`"posted"` PO that has `skipped_items`, opening a modal with an amber "Dropped item(s) - not received in SAP" box (`data-testid="grn-diagnostics-skipped-items"`).
- Tested: 9/9 backend tests passed (unit-level `_post_one_po` filtering with mocked SOAP/Playwright calls, `_post_goods_movement_for_items` skip logic, and a real live API test of the new `_resolve_items` guard returning 400). See `/app/test_reports/iteration_164.json`. Frontend Diagnostics change was code-reviewed only (not live-triggered, since a real GRN run is an irreversible SAP write).


## BUG FIX (CONFIRMED root cause via user's crash screenshot): PO 29455 GRN - duplicate Product ID collapsed all lines onto one row (Sep 12, 2026 session)
- **Correction of the earlier same-day hypothesis**: the previous "stale table row" theory (see the fix above it in this changelog) was a reasonable guess from the FIRST screenshot but not the real cause. The user then shared the actual SAP "Create Inbound Delivery and Goods Receipt" screen at the moment of the crash: PO 29455 has 5 line items (Delivery Item ID 10/20/30/40/50), ALL for the exact same Product ID (WAS8PZ) - only item 10's Actual Quantity was filled (with a value that looked like it had been overwritten multiple times), items 20/30/40/50 were empty with SAP's own validation banner "Actual quantity for Delivery Item ID 20 missing" (+5 more identical messages, "6 Messages" total).
- **Real root cause**: `_fill_line_actual_quantities()` matched each PO line to a grid row by Product ID text alone. When a PO has multiple lines for the IDENTICAL product (common for split delivery schedules, exactly PO 29455's case), Product ID is not a unique key - every item_number resolved to the SAME first row, silently overwriting it each time, leaving the other rows blank. SAP's save-time validation then failed, which cascaded into the generic "unexpected_crash" seen originally.
- **Fix**: SAP's own "Reference Details Description" column echoes back this app's own `"{po_number}-{item_number}"` reference per line (confirmed live in the screenshot: "29455-1".."29455-5") - a genuinely unique per-row key. Matching now tries this reference key FIRST, falls back to Product ID only if not found, and tracks already-matched rows (`used_rows`) so two different item_numbers can never claim the same row even on the fallback path.
- Verified the matching algorithm in isolation (5 identical-Product-ID lines, same shape as PO 29455) - all 5 now resolve to 5 distinct rows instead of collapsing onto row 0. Full live-SAP verification not possible from this dev environment - user should retry PO 29455's GRT next and confirm all 5 lines fill correctly.

## BUG FIX (CONFIRMED root cause via real SAP error text): Goods Movement rejected when target warehouse = the assumed GR receiving area (Sep 12, 2026 session)
- **User's report**: after a SUCCESSFUL GRN (shipment LFG29A/PO 29482, Inbound Delivery 53018 posted), the app then showed "Warehouse movement pending (target P1/P1-RM)" with no visible reason. Pulling the shipment's own data via the API surfaced the real SAP error: "SAP rejected the movement: Source and target logistics area are same".
- **Root cause**: `_post_goods_movement_for_items()` always assumes the Goods Receipt lands in `{site}-RM` and then tries to move it to the shipment's chosen target warehouse. `P1-RM` is ALSO this app's own default RM warehouse for site P1 (see the earlier "site-specific default warehouses" feature) - so for any shipment using that default, source and target are the literal same string, and SAP correctly rejects the movement as a no-op. This affects any shipment whose target warehouse happens to equal `{site}-RM`, not just this one PO.
- **Fix**: when `source_area == warehouse_id`, the function now skips the SAP call entirely and returns `ok: True, skipped: True` - there's genuinely nothing to move, the stock is already exactly where it needs to be.
- **Bonus fix, same investigation**: the "Warehouse movement pending" badge previously never showed WHY - the real `sap_movement_result.per_item[].error` text was already being captured but only ever surfaced in a toast at the moment Retry was clicked, invisible on a plain page load. Added a persistent error-detail block under the badge (`grn-movement-error-detail`) showing each failed item's real SAP error.
- Verified: direct unit test with a `goods_movement` client that raises if ever called - confirms the fix short-circuits before any SAP write is attempted, returning a clean success. Frontend error-detail block verified via API-level data structure (UI click-through blocked by an unrelated Playwright/environment flakiness on this specific form, not re-attempted since the underlying JSX pattern was already proven working in this exact session for the Diagnostics modal).

## FEATURE: "which SAP user was used" now answerable from the app itself, both GRN flows (Sep 12, 2026 session)
- **User's ask (asked twice - Supplier GRN via screenshot, then again for GRN 53015)**: which SAP UI login did the Playwright automation actually use for a given receipt, so SAP-side permissions/logs can be checked.
- **Fix**: both `sap_playwright_supplier_pgr_service.post_goods_receipt_via_ui` and `sap_playwright_pgr_service.post_goods_receipts_via_ui` now return which pooled SAP UI account (`itadmin`/`STOREBOT1`/`STOREBOT2` - see `playwright_concurrency.py`) actually ran that specific attempt. Threaded through `finalize_goods_receipt`/`finalize_receipt` into `sap_gr_result.sap_username` (Supplier shipments) and `receipt_sap_username` (STO inbound receipts) - persisted per attempt, so this is now always answerable from the shipment/STO's own data going forward, no log-digging needed. Also prepended as the first entry in the per-PO Diagnostics events list ("Logged in to SAP as STOREBOT1").
- **Frontend**: new "SAP User Used" row in the Confirmed-tab detail dialog.
- Verified end-to-end via screenshot with a seeded PO 53015 example matching the user's own question - both the detail row and the Diagnostics modal's first event show "STOREBOT1" correctly.

## BUG FIX: GRN Confirmed-tab Retry didn't poll for completion - modal/list stayed stale, looked like "retry did nothing" (Sep 12, 2026 session)
- **User's report**: "retry does not work as nothing in this modal still shows diagnostics button or details" + "retry history shows nowhere, it still shows original date and time so no idea if retry happened."
- **Root cause**: `retryConfirmedGoodsReceipt()` (GrnApprovalPage.jsx, the "Retry" button inside the Confirmed-tab detail dialog) fired the retry job and just waited a flat 3 seconds before reloading the closed LIST only - a real SAP GRN attempt takes 1-2+ minutes, so that reload almost always ran while the job was still mid-flight, AND it never touched the currently OPEN `confirmedDetail` dialog at all - so the modal kept showing the pre-retry `sap_gr_result`/screenshot/events forever (looking exactly like "retry did nothing"), and there was never any indication a retry had even started.
- **Fix**: now reuses the same `pollGrnJob()` polling loop the main Approve/Retry flow already relied on (`GET /admin/grn/receipt-status/{job_id}` every 2.5s, up to 6 minutes) - once the job actually finishes, it pushes the real result straight into the open dialog (`setConfirmedDetail(result)`, only if it's still the same shipment) and refreshes the list. Added a "Retrying - checking SAP now..." label in place of the Retry button while polling, so there's now always a visible signal a retry is genuinely in progress.
- Verified end-to-end via screenshot: clicking Retry immediately shows "Retrying - checking SAP now..." (previously the button just silently sat there with no feedback for the next several minutes).

## FEATURE: GRN "Diagnostics" modal - failed step, SAP error, and the full Playwright event trail (Sep 12, 2026 session)
- **User's explicit ask**: "add a diagnostics click to open modal on this screen that shows which screen we failed on, what is SAP error and a flow of events that happened in playwright successfully before it hit a failure."
- **Backend**: `_post_one_po()` (sap_playwright_supplier_pgr_service.py) now builds a plain-English `events` list live as it progresses (e.g. "Found PO 29455 in the list" -> "Clicked 'Post Goods Receipt'" -> "Entered Actual Quantity for item(s): 1" -> "Removed any zero-quantity line(s)..." -> "Clicked 'Save and Close'"), mutated IN PLACE so it's preserved even if the function crashes unhandled (the outer catch-all in `post_goods_receipt_via_ui` passes the same list into `_capture_failure` for the `unexpected_crash` case too). `_capture_failure()` now includes `events` in every failure result; the success path also stamps a final "SAP confirmed..." event for consistency. This flows straight through to `sap_gr_result.per_po[].events` on the shipment doc with zero new API endpoints (same object already returned everywhere).
- **Frontend** (`GrnApprovalPage.jsx`): new "View Diagnostics" link next to the existing "View exact SAP screen..." link, in both the just-approved panel and the Confirmed-tab detail dialog - opens a new modal showing Failed Step, the SAP Error text, and the numbered Events list.
- Verified end-to-end with a seeded shipment matching the user's own PO 29455/VSH4BA example (screenshot confirmed: modal shows "Failed at step: saving", the SAP error, and all 9 milestone events leading up to it).

## BUG FIX (CONFIRMED root cause + corrected fix via user's clarification): PO 29455 GRN - shipment covered only 1 of 5 open PO lines, extra lines needed removing not filling (Sep 12, 2026 session)
- **Correction of the earlier same-day hypothesis**: the previous "stale table row" theory was a reasonable guess from the first screenshot but not the real cause. A second screenshot then showed the actual SAP draft: PO 29455 has 5 open lines (Delivery Item 10/20/30/40/50, all Product WAS8PZ), only item 10 had an Actual Quantity, items 20/30/40/50 were blank with SAP's own "Actual quantity for Delivery Item ID 20 missing" validation (6 messages total). My first fix (matching rows by a unique per-line reference key instead of Product ID alone) was a real, worthwhile hardening for genuine multi-line-same-product shipments, but the user then clarified the REAL business fact: **this delivery only ever covered item 10** - the other 4 lines aren't a matching problem, they're lines that were never part of this shipment at all and should be REMOVED from the draft, not filled.
- **Real root cause**: "Post Goods Receipt" on a PO opens SAP's Inbound Delivery draft pre-populated with ALL of that PO's still-open lines, regardless of how many the current shipment actually covers. The automation only ever filled quantities for what `item_qtys` says this shipment covers (correctly, just item 10) - it never removed the other 4 SAP-proposed lines, so they sat at zero and SAP's save-time validation rejected the whole draft.
- **Fix**: after filling quantities for the lines that ARE part of this shipment, the automation now clicks SAP's own "Remove Zero Quantity Items" toolbar button (visible in the draft's toolbar) before Save and Close - this strips every line nobody actually delivered, leaving only the real lines to post. Best-effort (never hard-fails if the button is disabled/not found - e.g. when there's nothing to remove).
- Verified the underlying unique-reference row-matching algorithm in isolation (still valuable for real multi-line-same-product shipments). The "Remove Zero Quantity Items" step itself could not be exercised against live SAP from this dev environment - user should retry PO 29455's GRN and confirm only item 10 posts, with lines 20-50 cleanly removed rather than blocking the save.

## BUG FIX (unverified against live SAP): GRN Playwright crash likely acted on the wrong PO's row (Sep 12, 2026 session)
- **User's report**: PO 29455's GRN failed with "unexpected_crash" - "Could not reach SAP's receipt screen - please retry". Retrying did not help. The crash screenshot the user shared showed the SAP Purchase Orders LIST screen with a completely DIFFERENT, unrelated PO (2133) still highlighted/selected - not PO 29455 at all.
- **Hypothesis (based on the screenshot evidence, NOT confirmed live)**: `_search_po_exact()` used a fixed 4000ms wait after clicking "Go" before reading the row count, and `_post_one_po()` then blindly clicked `rows[0]` with no check it actually corresponds to the searched PO. If this tenant's UI takes longer than 4s to re-render the filtered table, the automation could read/click a STALE row left over from a previous PO's search - explaining both the unrelated-PO screenshot and why every Retry reproduced the identical crash (a deterministic timing/logic issue, not a transient SAP blip).
- **Fix**: `_search_po_exact` now waits for the table's own "Loading..." indicator to clear (`_wait_for_table_load`, the same pattern already used elsewhere in this file) instead of a fixed wait. `_post_one_po` now verifies `rows[0]`'s text actually contains the searched `po_number` before clicking it; on a mismatch, it re-searches once and checks again, and if it STILL doesn't match, fails with a precise, diagnosable reason ("SAP's Purchase Orders list did not refresh to show PO X after searching...") instead of crashing generically deep inside the wrong PO's receipt screen.
- **NOT live-verified** - this Playwright flow drives the user's real SAP tenant and can't be exercised from this dev environment; the fix is a code-review-level hardening consistent with several previous "confirmed live" fixes in this exact file (see its own Sep 2 2026 comment history). User should test on the next real GRN attempt for PO 29455 (after an Admin clicks "Reset Retry" on the Supplier Shipment's GRN screen first, since it likely already exhausted its 3-attempt budget and flipped to "SAP Sync Failed" - `supplier_shipment_service.MAX_GR_RETRIES_BEFORE_FAILED = 3`).

## BUG FIX: retrying after a timeout created a duplicate Store Request + duplicate SAP Proposal (Sep 12, 2026 session, urgent)
- **User's report**: "if user face timeout error while creating proposal then retry the proposal system create store request duplicate" - retrying "Create Production Order" for the same material/site/qty after a pipeline failure created a SECOND SAP Proposal + SECOND Store Request even while the first was still open, confusing the Store team with duplicate asks.
- **Fix**: new `store_approval_service.find_open_request(db, site_id, material_id, quantity, requester)` - checks for any currently-OPEN (pending/issuing/partial_pending_planner) request with an exact site+material+quantity+requester match, no time cutoff needed (an unresolved identical request is always still relevant). `_run_create_and_release_job` (server.py) calls this right after computing `short_rm`, BEFORE `_create_proposal_for_payload` runs - on a hit, the job fails immediately with `result.reason="duplicate_store_request"`, `production_proposal_id=None`, and a clear error pointing to the existing request ID. **No second SAP Proposal is ever written** on the duplicate path (not just no second store request - the fix blocks the wasted SAP write too). Dedup is scoped per-requester (a different person asking for the same material/qty is never blocked). Frontend Active Orders row shows a distinct message for this reason pointing to the Store Approval screen instead of the generic "nothing created" copy.
- Tested via `testing_agent` (iteration_163): 12/12 pass (unit tests on `find_open_request` incl. resolved/cancelled no-longer-blocking, integration tests on the full job pipeline confirming `_create_proposal_for_payload` is never called on the duplicate path, different-requester not blocked, no-shortage regression path unaffected). Live UI verification hit a transient Cloudflare 502 during the test window (confirmed not a real regression via a fresh screenshot afterward - page loads fine).

## CRITICAL FIX: by-product/scrap posted multiple times to SAP while main output never posted (Sep 12, 2026 session, urgent)
- **User's report + real incident**: Lot 72722 - by-product (scrap) inventory was posted to SAP 4 TIMES (inflating real scrap stock 4x) while the main production output was never posted at all.
- **Root cause, confirmed with user**: `_confirm_production_inner` posts the by-product's `MaterialOutput` line BEFORE the main `ReportingPoint` call (deliberate ordering from an earlier fix, so the by-product survives even if the main call's own `finish_task()` later locks the lot). SAP's `confirm_material_output`/`create_material_output` are **not idempotent** - each call ADDS the confirmed quantity to SAP's own running total, never overwrites it. If the main confirmation then fails for its own unrelated reason and the user retries the exact same confirmation, the old code blindly re-ran the by-product SAP call every retry - 4 retries = 4x real scrap inventory, while the main output stayed unposted the whole time.
- **Fix (prevention only, per user's explicit choice - the already-corrupted SAP data for the real Lot 72722 is being corrected by the user/SAP admin directly, out of scope here)**: new `production_confirmation_service.find_successful_byproduct_confirmation(db, production_lot_id, reporting_point_id, byproduct_material_output_uuid=None, new_byproduct_product_id=None)` - looks at the MOST RECENT history record for that exact lot+reporting point; if it shows `success=False` (main failed) but `byproduct_confirmation.success=True` for the SAME by-product target, returns that result to be REUSED instead of calling SAP again. `_confirm_production_inner` calls this FIRST and short-circuits (real `if/elif/elif`) the by-product SAP call entirely when it matches. `log_confirmation()` now also persists `new_byproduct_product_id` so the "create a brand-new by-product line" path can be identity-matched on retry too, not just the "existing line" path.
- **Bonus fix, same incident**: the "WIP: Error" filter (added earlier same day) only ever looked at the WIP Clearing Run's own status, which never starts until the main confirmation succeeds - so a lot stuck exactly like 72722 (main confirm keeps failing) was invisible under "WIP: Error" despite being clearly broken. Broadened (`ProductionConfirmationPage.js` visibleRows) so "WIP: Error" also matches any row whose latest confirmation history has `success===false`, regardless of whether `wip_clearing` ever ran. "WIP: Posted" intentionally NOT broadened - still strictly means a真 successful WIP Clearing Run.
- Tested via `testing_agent` (iteration_162): 9/9 new backend unit tests (every branch: matching-target reuse, mismatched UUID/RP no-reuse, resolved-after-retry no-longer-reuses, new-byproduct-product_id path, only-latest-record-considered), code review of the real `_confirm_production_inner` call order, and UI+code verification of the broadened WIP filter (this dev tenant has 0 live open SAP lots, so a true end-to-end live-SAP retry couldn't be reproduced - flagged explicitly, unit-level + code-review verification used instead).

## BUG FIX: Store Issue silently clamped an over-issue down to the required qty (Sep 12, 2026 session, urgent)
- **User's report + real incident**: request P1-000007/SI-4426SF - production demanded 9.3 KG of CR-SCRAP, but store staff deliberately issued 10 KG (their own discretion/rounding), yet the app recorded/printed only 9.3 KG as Issued Qty.
- **Root cause**: `store_approval_service.start_issue()` (the live issue-flow entry point) computed `cumulative = min(c["required_qty"], prev_cumulative + issued_this_round)` - hard-capping the STORED/displayed issued qty at required_qty no matter what the store actually typed. The frontend (`StoreApprovalPage.js`) was already correct - it only validates the typed qty against real usable RM warehouse stock (`usableRmQty`), never against required_qty, by explicit design ("a store person should never be able to type in more than what's actually free to issue... regardless of how much production still needs"). The SAP Goods Movement itself was NOT affected (it uses the unclamped `issued_this_round`) - only the recorded/shown `issued_qty` (and therefore the shortfall calc, print slip, and Store Return "available to return" qty) was wrong.
- **Fix**: removed the `min(required_qty, ...)` cap - `cumulative = prev_cumulative + issued_this_round` (shortfall still floors at 0, so an over-issue simply shows 0 shortfall, never negative).
- Verified via a direct unit test against `start_issue()`: single-round over-issue (9.3 required, 10 issued) now correctly records `issued_qty: 10`, `shortfall: 0`; a 2-round partial+balance reopen (5 required, 3 then 3) correctly accumulates to `issued_qty: 6` with `shortfall: 0` too - no regression to the existing partial-issue/reopen flow.

## Production Confirmation page: date range, Excel export, WIP/By-product filters (Sep 12, 2026 session, same-day follow-up)
- **User's ask, clarified via ask_human**: on the main "Production Confirmation" tab's Open Lots table (the one with Status filter + Refresh button) - (1) a "between" date range filter is required, (2) an Excel export button matching exactly what's visible in the table, (3) two more filters: WIP Clearing (Posted/Error) and By-product (Posted/Error), where "Posted"=success, "Error"=failure.
- **All 3 done purely client-side, zero backend changes** - the date filter uses each row's existing "Last Confirmation" data (`lastConfirmationByLot`, already fetched via `POST /production-confirmation/history/latest-batch`) rather than any new SAP/Mongo query; WIP/By-product filters read the same `wip_clearing`/`byproduct_confirmation` objects already used to render the "Last Confirmation" column's badges. `exportToExcel()` (reuses the `xlsx` lib, same pattern as `InventoryClosingReportPage.js`) writes exactly `visibleRows` (post-filter) with a 17-column header including the new WIP/By-product/Last Confirmed At columns.
- New UI: second filter row on the Confirm tab (Last Confirmed date-from/to, WIP dropdown, By-product dropdown, Clear Filters, Export Excel pinned right).
- Tested via `testing_agent` (iteration_161): 100% of testable paths - all new controls render/interact correctly, Export Excel downloads a real .xlsx with no JS errors (filtered + unfiltered), Clear Filters visibility toggles correctly, zero regressions to the rest of the tab or the Create Production Order tab. Posted/Error data-driven filtering was verified via code review only (dev env had 0 open SAP lots at test time) - re-verify with live data if doubt ever arises.

## Production Confirmation page: filter header + KPI redesign (Sep 12, 2026 session)
- **User's ask, clarified via ask_human**: (1) "Global Access Filter" actually meant a filter header (date range/result/model ID/product) on the "Create Production Order" tab's Proposal/Release history table, NOT a site-access restriction. (2) Date range filter = "created on", using our own Mongo history (fast). (3) New KPI cards on the Confirm tab, replacing the old 4 (Reporting Points Shown/Distinct Lots/Confirmed Today/Scrap Trend): "Today Confirmed Output Qty", "Today Released Output Qty", "Today Output Open Qty", "Today Scrap Posted Qty" - user explicitly said to start tracking `released_at` from now on (no retroactive data). (4) Output Product filter = simple client-side search box on the Confirm tab's Open Lots table.
- **Backend**: `production_confirmation_service.get_today_output_stats()` - sums `confirmed_quantity`/`confirmed_scrap` from `production_confirmation_history` for today (IST) + sums `quantity` from `production_order_creation_history` where `released_at` is today, returning the list of those `production_order_id`s. `get_proposal_and_release_history()` now takes optional `start_utc`/`end_utc` (drops the 200-row cap when a date filter is active). `log_order_release()`'s plain-insert fallback branch (order released outside any known proposal/job) now also sets `released`/`released_at`. New `GET /production-confirmation/today-stats` (server.py) does ONE live SAP `find_open_lots(["2","3","4"])` call, matches rows back to today's released order IDs, sums each matched lot's terminal (last routing step) Reporting Point's `open_quantity` for "Today Output Open Qty" - documented as a best-effort approximation. `GET /production-confirmation/proposal-history` now accepts `start_date`/`end_date` (YYYY-MM-DD, 400 on bad format). Old `/confirmed-today`/`/scrap-trend` endpoints left in place (unused by frontend now, harmless).
- **Frontend** (`ProductionConfirmationPage.js`): `CreateOrderTab`'s history table has a new filter bar (date-from/to inputs trigger a real refetch; Result dropdown + Model ID + Product text inputs filter client-side; "Clear Filters" button appears only when a filter is active). Confirm tab: 4 new `StatCard`s wired to `/today-stats`; Status filter dropdown gained a "Finished" option (value `"4"`, zero backend change needed - the endpoint already parses arbitrary comma-separated status codes); new "Filter by Output Product..." input filters `visibleRows` client-side. Removed the now-dead `ScrapTrendCard` component.
- Tested via `testing_agent` (iteration_160): backend 10/10 (both super_admin and a site-bound "user" role), frontend 95% - 1 MEDIUM bug (wrong empty-state copy when a server-side date filter returns 0 rows) + 1 minor backend issue (bad date format returned 500 instead of 400) - both found and fixed same session, spot-verified via screenshot after the fix (empty-state now correctly shows "No rows match the current filters.", bad date now returns 400 with a clear message).
- Known approximation, not a bug: "Today Released/Open Output Qty" will show 0 until a real order is released through this app on/after Sep 12 2026 (no retroactive `released_at` data exists for older releases, per the user's own explicit choice).

## Manual "Refresh from SAP" for POs Created Directly in SAP (Sep 10, 2026 session, same-day follow-up)
- **User's ask**: created PO 29430 for RAD-P2-S directly in SAP itself (not via this app's PO Creation flow, which the earlier fix already covers) - still didn't appear on Open Purchase Orders right away.
- **Root cause**: the earlier "seed at creation" fix only covers POs created via `/purchase-orders/create` - a PO entered directly in SAP has no way to reach this app before the next 10-min background poll. This is architecturally correct (no live per-request full sync - too slow/unstable per the loop's own docstring), just needed a way for staff to force it on demand.
- **Fix**: `POST /admin/purchase-orders/refresh-cache` runs the exact same `fetch_recent_window` + `refresh_all_vendor_caches` SAP sync as a background job (real SAP call, ~50-100s), polled via `GET /admin/purchase-orders/refresh-cache/poll/{job_id}`. New "Refresh from SAP" button added to `OpenPurchaseOrdersPage.jsx` header - re-searches the currently selected supplier once done.
- **Bug caught during self-testing**: both new routes were initially defined AFTER `app.include_router(api_router)`, so FastAPI never registered them (404) - moved before that line. Reinforces: any NEW `@api_router` route added near the end of server.py must go before line ~7556's `include_router` call.
- Live-verified end-to-end: ran the real refresh (81 vendors / 599 line items, ~50s), confirmed PO 29429 AND 29430 (the user's real just-created-in-SAP PO) both now appear correctly in `PO_CACHE_COLLECTION` and render on the Open Purchase Orders page with correct item/qty/Printed PO # data. Screenshot-confirmed button renders correctly.

## PO Creation Visibility Latency Fix (Sep 10, 2026 session, same-day follow-up)
- **User's ask**: created PO 29429 for supplier RAD-P2-S via the app - took ~10 minutes to appear on the Supplier Dashboard, then a shipment against it took additional time to reflect "in transit" on the Open PO table.
- **Root cause**: `PO_CACHE_COLLECTION` (backs Supplier Dashboard + internal Open Purchase Orders page) only updates via the shared background SAP poll (`refresh_all_vendor_caches`, every 10 min) - deliberately NOT done live per-page-load (too slow/unstable, per that loop's own docstring). A PO created via THIS app's own `/purchase-orders/create` flow had no way to appear before the next poll cycle. The "in transit" qty itself is always computed live from the shipments collection (no separate cache) - so once the PO/item exists in cache at all, everything downstream is already instant; the ONE real fix needed was seeding the PO cache immediately at creation time.
- **Fix**: new `supplier_shipment_service.seed_po_cache_items()` - upserts just the newly-created PO's own items into the cache right after a successful create (never touches/expires the vendor's other cached rows, unlike the full `refresh_po_cache`). Item numbers assumed "1","2","3"... in submission order (confirmed via a real prior creation's history record) - self-heals within 30 min via the real background loop even if ever wrong. Wired into `/purchase-orders/create` right after the existing Printed PO # one-off fetch.
- Live-verified: seeded a test PO/item directly, confirmed it appears instantly via `get_cached_pos_with_remaining` (same function both Supplier Dashboard and Open PO page call) with correct remaining/in-transit qty math; screenshot-verified the real Open Purchase Orders page for RAD-P2-S still renders normally (real POs 29053/29060/29062/29179/29269, Printed PO # column intact) - no regression.

## PNC9DW manual-receipt lookup investigation (Sep 10, 2026 session, same-day follow-up)
- **User's ask**: "PNC9DW... failed to inbound in SAP then user manually received it. Do you able to fetch it?" - wanted the SAP Inbound Delivery ID for a manually-posted GR against a shipment our automation couldn't post.
- **What was found (live, read-only SAP investigation, no writes)**: no OData/SOAP path exposes a supplier PO's linked Inbound Delivery ID (checked 4 separate services/queries, all dead ends - see "GRN Status Truthfulness" section above). Built a NEW read-only Playwright script (not yet added to the codebase - purely a one-off diagnostic run from `/tmp`, not persisted) that navigates "Inbound Logistics > Inbound Deliveries" (a work center view never used by this app before) and searches by PO number - this DOES show Delivery ID + Delivery Notification ID + Fulfilled Quantity per PO.
- **Key finding**: PO 29073 (PNC9DW's PO, per user) has exactly ONE Inbound Delivery document in SAP - **Delivery 52723** (posted 09.09.2026), whose Fulfilled Quantities (950.74kg + 619.91kg) exactly match the PO's ENTIRE delivered total. This is the SAME delivery already known in this app for a DIFFERENT shipment code. **PNC9DW itself has no separate delivery document in SAP at all** - it does not appear to have actually been posted/received despite the user's initial claim.
- **Status: PAUSED, awaiting user confirmation** - asked user to double check in the app whether they mixed up shipment codes or whether PNC9DW is genuinely still un-posted; user deferred ("let me double check... get back to you"). **Next agent: do not assume PNC9DW is resolved - wait for the user's follow-up before taking any action on it.**
- No code changes were made from this specific investigation (purely read-only SAP navigation for diagnosis). The diagnostic script lived in `/tmp` (non-persistent) - if this "search Inbound Deliveries by PO" capability proves generally useful, it should be properly added to `sap_playwright_supplier_pgr_service.py` as a real function, not re-created ad-hoc each time.

## GRN Status Truthfulness + Sync Failed state (Sep 10, 2026 session, continued - user report of 3 stuck production GRNs)
- **User's ask**: 4 real production shipments went through GRN approval, only 1 posted to SAP successfully; the other 3 stayed stuck, Retry did nothing, AND critically: the app's top badge said "Received" (green) even though SAP never actually confirmed the Goods Receipt - user explicitly said "Do not show status received until the SAP inbound number is not received."
- **Root cause investigated**: `status` (internal, becomes "approved" the instant staff physically match goods+invoice - by design, independent of SAP) was being directly mapped to a green "Received" badge, conflating "staff reconciled it" with "SAP confirmed it". Separately, `sap_sync_status` used to only ever be "pending" (never "failed") no matter how many times Retry reproduced the exact same SAP-side error - matches "Issue 1: Backend Job for PGR Silently Fails" from the original handoff (previously deferred at user's request, now actively causing harm so fixed).
- **Fix 1 (status truthfulness)**: `GrnApprovalPage.jsx`'s top badge (`resolveStatusBadge`) now shows "Pending SAP Sync" (orange) or "SAP Sync Failed" (red) instead of "Received" (green) whenever `status === "approved"` but `sap_sync_status !== "posted"`. Only truly SAP-posted shipments show "Received" now.
- **Fix 2 (stop the endless ambiguous retry loop)**: `supplier_shipment_service.py` - `prepare_retry_goods_receipt` now increments a `sap_gr_retry_count` on every Retry click; `finalize_goods_receipt` flips `sap_sync_status` to `"failed"` (distinct, red "Sync Failed" badge everywhere: top badge, single-lookup panel, Confirmed GRNs list column, Confirmed GRN detail popup) once `sap_gr_retry_count >= 3` and the GR still hasn't posted - instead of staying "pending"/"In Process" forever. This doesn't fix the underlying SAP validation error itself (see below) but stops the system from implying "still actively trying" when it's actually stuck on the same recurring issue.
- **Investigated but NOT resolved - the actual SAP-side blocker**: the specific error text ("Actual quantity for Delivery Item ID 40") comes from SAP's own live Inbound Delivery Notification screen validation during our Playwright automation's Save step - confirmed via code review this step never touches warehouse/bin/zone fields at all, so the user's P3-zone-vs-plain-RM hypothesis does NOT apply to this error (that's step 2/Goods Movement, which is correctly SKIPPED here purely because step 1 hasn't succeeded - not a separate bug). Tried to get the full/untruncated SAP message via a live OData read (found `InboundDeliveryItemBusinessTransactionDocumentReferencePurchaseOrderCollection` which theoretically links a PO to its Inbound Delivery) but it returned empty for real supplier PO 29118 - the `inboundstockemergent` custom OData service appears scoped to STO-only deliveries (per its own module docstring), no read path exists for supplier-GRN deliveries. **This specific production issue (3 stuck shipments, PO 29118/29073) still needs either the SAP admin's help to see the full validation message inside SAP's own UI, or a live Playwright debug-screenshot capture from production** (not reproducible from this session - the shipment data only exists in production's database, confirmed with user their screenshots are from `mrp.radishtechnologies.com`, not this preview).
- Live-verified via a synthetic test shipment doc (inserted+removed) in this preview: "SAP Sync Failed" (red) now renders correctly on all 4 surfaces (top badge, single-lookup error panel, Confirmed GRNs list, Confirmed GRN detail popup). Backend regression-checked via curl (200 OK, valid sap_sync_status values).
- **IMPORTANT ENVIRONMENT NOTE (repeated)**: none of this session's work (Printed PO # everywhere + this GRN status fix) is live on production yet - needs Deploy.

## CORRECTION + real bug found on 6700-302359 @ P9 (Sep 11, 2026 session, same-day follow-up)
- **User's report**: testing 6700-302359 at P9 gave "Valuation data missing for material X business residence P9" - a NEW, different error than "Account det. group is missing".
- **Root cause, live-confirmed**: the "MAJOR FIX" above only works when the site ALREADY has SOME Valuation presence (even "In Preparation") - 6800-004473's sites happened to already have stub rows from some earlier point. 6700-302359 @ P9 had ZERO prior Valuation presence at all (confirmed: no `<Valuation>` block for P9 in the read query, unlike P1/P2/P7 which all had one). For a genuinely fresh site, BOTH SOAP paths refuse to bootstrap a level from absolute zero - tried `ManageMaterialIn`'s plain create AND `ManageMaterialValuationDataIn`'s own `actionCode="01"` create, both fail identically with "Valuation data missing"/"Valuation level does not exist". This matches SAP's own documentation: the very FIRST valuation level for a site must be created ONCE, manually, via SAP UI (Inventory Valuation work center -> Material valuation tab -> Maintain Product Specification Valuation) - only AFTER that exists can either API adjust it further.
- **Fix applied**: `activate_site()` now detects this specific case and returns a clear, actionable message telling the user exactly which one-time SAP UI action their admin needs to take, instead of just the raw ambiguous SAP error text.
- **Correction to earlier claim**: Valuation activation via this app is NOT "fully solved" for every material/site - it auto-fixes existing-but-incomplete Valuation records (the common case for materials that went through Planning/Logistics activation before), but a site with literally zero prior Valuation history for a material still needs one manual SAP UI bootstrap step, per site, done once by an SAP admin.

## MAJOR FIX: Valuation activation FULLY SOLVED via ManageMaterialValuationDataIn (Sep 11, 2026 session, same-day follow-up)
- **User's ask**: pointed at SAP's `ManageMaterialValuationDataIn` service, then gave exact business rules: Account Determination Group by category (RM=3010, SFG=3020, FG=3030, Scrap=Z001), Perpetual Cost Method always "2" (Moving Average), Valid From = today-40 days, Cost = 0 at activation.
- **This was previously believed to be a hard SAP Business Configuration blocker (see earlier same-day entries above) - turned out to be WRONG.** Deep live-testing (multiple real SOAP calls against material 6800-004473, sites P2/P4/P6 - all previously stuck "In Preparation") proved the Account Determination Group CAN be set via API after all, via a 2-step process:
  1. `ManageMaterialValuationDataIn` (`MaterialValuationData actionCode="06"`, new client `sap_material_valuation_data_client.py`) sets `AccountDeterminationSpecification` + `InventoryValuationSpecification` (Moving Average) + an opening `ValuationPrice` (0) on the EXISTING "In Preparation" record. Live-derived, undocumented-by-us-until-now schema quirks: for "Business Residence" level type (the only type this app uses), `ProductValuationLevelID` must be OMITTED entirely (only `PermanentEstablishmentID` + `ProductValuationLevelTypeCode=1`) - sending it caused "Valuation level X does not exist". `ValuationPrice`'s `StartDate` MUST be the 1st of a calendar month ("must be valid from first day of period") or the WHOLE bundle silently fails atomically (including the other 2 fields).
  2. Retry the existing `ManageMaterialIn` Valuation `actionCode="02"` (update) - this now succeeds and flips `LifeCycleStatusCode` 1 -> 2 for real.
- `sap_material_create_client.activate_site()` now chains this automatically: if the plain Valuation attempt fails with "Account det. group is missing" AND category+set-of-books info is available, it runs the new 2-step process and retries - fully transparent to callers (`server.py`'s `post_activate_material_site` now also resolves `product_category_id` via `sap_material_client.resolve_material_info()` and `set_of_books_id` via the existing `company_and_set_of_books_for_site()`).
- **Live-verified end-to-end for real** (not simulated): 6800-004473 now genuinely Active (status "2") at P1/P2/P3/P4/P6/P9 - only P7 intentionally left for the user's own click. Confirmed via direct before/after SOAP queries AND through the actual `/admin/material-sites/activate` app endpoint (not just raw scripts).
- New `SAP_SOAP_MATERIAL_VALUATION_MANAGE_ENDPOINT` env var. `ACCOUNT_DETERMINATION_GROUP_BY_CATEGORY` mapping currently covers "RM"/"SFG"/"FG"/"SCRAP" category codes (FG/SCRAP keys are best-guess pending live confirmation of their exact `ProductCategoryID` string - RM and SFG are live-confirmed); an unmapped category surfaces a clear "ask your SAP admin" message rather than guessing wrong.

## BUG FIX: "already active" was wrong for 6800-004473 - 2 real bugs found+fixed (Sep 11, 2026 session, same-day follow-up)
- **User's report**: "i test one item 6800-004473 its showing in preparation in the all functional area but while check in emergent test its showing already active it create confusion." Reproduced live against real SAP data before touching any code.
- **Bug 1 (display)**: `sap_material_client.resolve_material_info()`'s new `active_sites` list (added earlier this session for the Activate Material Site page) counted ANY `<SupplyPlanning>` node as "active", regardless of its own `LifeCycleStatusCode`. Live-confirmed: SAP had P2/P4/P6/P7 present but at status "1" (In Preparation, matching the user's screenshot) while only P1/P3/P9 were truly "2" (Active) - our list wrongly included P2/P4/P6/P7 as "already active". Fixed: only counts a site when `LifeCycleStatusCode == "2"`.
- **Bug 2 (deeper, in the actual activation write path - `sap_material_create_client.activate_site()`)**: when a site's Planning/Logistics/AvailabilityConfirmation nodes already exist but are stuck at status "1", SAP rejects a plain `actionCode="01"` (create) with "already exists" - the OLD code treated ANY "already exists" as an unconditional success ("the site IS active"), which was FALSE: the site never actually flipped to "2". This is used by BOTH the new standalone Activate page AND the older auto-triggered Stock Transfer activation flow, so this bug could silently under-activate a site anywhere in the app. Fixed: on "already exists", retries the exact same fields with `actionCode="02"` (update) - live-verified this genuinely flips `LifeCycleStatusCode` 1 -> 2 in real SAP (confirmed via direct before/after SOAP queries, not just trusting the response).
- **Live-fixed the real material while investigating** (genuine side benefit, not just a test): 6800-004473 is now truly Active (status "2") at P1/P2/P3/P4/P9 (P2 and P4 were the ones stuck at "In Preparation" that this fix corrected for real) - P6/P7 intentionally left untouched (user's own future call via the Activate page). Valuation at P4 still fails with "Account det. group is missing for material 6800-004473 company RT bus res P4" - same known SAP Business Configuration gate as before, unrelated to these 2 bugs, surfaced correctly (not hidden).
- Verified via direct live SOAP before/after queries against real production SAP data (stronger than a mocked/simulated test) + a fresh curl against the fixed `/admin/material-sites/lookup/` endpoint confirming the corrected active_sites list.

## NEW FEATURE: standalone "Activate Material Site" page under Master Data (Sep 11, 2026 session, same-day follow-up)
- **User's ask**: "I want to just active material in another branch... find material then select site from the dropdown then... active tab... with confirmation message. item active with all the tab including valuation: ex: PALL-373518." Confirmed scope: new page under Master Data (not a tab on Create Material); site dropdown shows ALL sites (not filtered to missing-only), idempotent/safe if an already-active site is picked.
- **Reused proven code, no new SAP-write risk**: `sap_material_create_client.activate_site()` (Aug/Sep 2026, already live-proven for Planning/Logistics/Availability Confirmation) is unchanged - just exposed via a new standalone page instead of only the auto-triggered notification flow. Widened `POST /admin/material-sites/activate`'s permission check + added new `GET /admin/material-sites/lookup/{material_id}` (extends `sap_material_client.resolve_material_info()` to also return `product_category_id` + `active_sites`, read-only, zero risk).
- **Valuation investigated deeply, confirmed still gated by SAP Business Configuration**: researched SAP's own docs for `ManageMaterialValuationDataIn`/`AccountDeterminationSpecification` - confirmed a real chicken-and-egg SAP platform constraint: the underlying "valuation level" record must already exist before you can set its Account Determination Group via API, and creating that very level (via `ManageMaterialIn`'s `<Valuation actionCode="01">`, already attempted) requires the Account Determination Group at creation time - a hard, config-side gate, not a code/payload gap on our end (matches the Sep 5 2026 finding). Live-checked PALL-373518 (category RM): Valuation is already active at P1/P2/P4/P6/P7 (set up some other way, likely manually in SAP UI) - did NOT live-test-write a NEW site for real (would be an irreversible production SAP change) since user didn't explicitly authorize a live test; the new page attempts it for real (best-effort) and surfaces SAP's exact rejection reason per-tab, never silently hiding a Valuation failure behind a green Planning/Logistics success.
- New permission `admin_activate_material_site` (Master Data group, mirrors `admin_create_material`'s exact wiring across `auth_service.py`/`AuthContext.jsx`/`NavTabs.jsx`/`AccessManagementPage.jsx`/`App.js`). New page `ActivateMaterialSitePage.js` mirrors `CreateMaterialPage.js`'s passcode-gate pattern exactly (passcode "admin").
- Verified via `testing_agent` (iteration_159): backend 7/8 pytest (the 1 "failure" was a transient real SAP write-lock held by the tenant's own OData user at that moment, correctly surfaced end-to-end, not a bug), frontend 100% (lookup, site dropdown with "(already active)" suffixes, negative case, access-control 403s + nav/route hiding for an unpermissioned user). Real "Activate" click deliberately NOT exercised against a genuinely new site during testing (irreversible SAP write) - first real use should be the user's own click.

## GRN Approval: default Warehouse changed from QC to RM per site (Sep 11, 2026 session, same-day follow-up)
- **User's ask**: "set default warehouse RM for the all site ex: P1-RM only P3 set P3-Z1-01-A while receiving the GRN by the supplier Unique code" - overrides the earlier documented "Warehouse should always be QC default" rule (mrp vendor side changes.docx, Sep 2026).
- **Fix**: `GrnApprovalPage.jsx`'s warehouse-loading effect now pre-selects the site's `-RM` suffixed warehouse_id (e.g. `P1-RM`, `P4-RM`, `P8-RM`...) instead of `-QC`. Live-checked real warehouse master data across all 8 known sites (P1/P2/P2W/P3/P4/P7/P8/P9) - every site except P3 has a literal `{SITE}-RM` id. P3 has no `-RM` id at all, only a zone-coded one (`P3-Z1-01-A`, named "P3-RM-Zone-1-01-A") - falls back to a name-regex match (`/(^|[-\s])RM([-\s]|$)/i`) so the rule generalizes without a hardcoded P3 special-case in code, and correctly resolves to `P3-Z1-01-A` for that site.
- Still fully editable per-shipment (dropdown, not locked) - only changes the pre-selected default.
- Verified: live-checked the actual per-site warehouse lists via backend (`stock_transfer_service.list_known_warehouses_for_site`) and confirmed the new JS predicate resolves correctly for every site (P1->P1-RM, P3->P3-Z1-01-A, P8->P8-RM, etc.) via direct logic simulation against that real data. NOTE: could not get a visual screenshot confirmation this session (screenshot tool had a cookie/session flakiness issue unrelated to this code change - confirmed via curl that a fresh valid session against the real `/admin/grn/warehouses/{site}` endpoint returns the exact data the new frontend logic was validated against). Recommend a quick visual spot-check next session if any doubt remains.

## NEW FEATURE: "Fetch from SAP" manual Inbound Delivery # recovery (Sep 11, 2026 session, same-day follow-up)
- **User's ask**: some live GRNs actually posted/were received in SAP (confirmed by user directly in SAP's own "Inbound Delivery Detailed Details" analytics report, e.g. PO 29368 -> real Inbound Delivery 52897) but the app's Playwright automation never captured the resulting Inbound Delivery ID - stayed blank forever with no recovery path. Explicit ask: a "Fetch from SAP" button ONLY on the Confirmed GRNs list (not Pending Shipments) that live-matches SAP and fills the real number in, tagged distinctly from an automated success, and flips SAP Status to Posted once confirmed.
- **Investigation** (the exact field the user's given report URL exposes as `CCONF_INB_DEL_ID` is a DEAD field in this tenant - confirmed blank across 300+ real live rows regardless of PO/site/date): the real Inbound Delivery ID lives under a differently-named field, `CDELIVERY_UUID` (despite the misleading name, holds the plain numeric ID) - and ONLY visible when querying with `SAP_ODATA_BUSINESS_USER` credentials, not the technical `SAP_ODATA_USERNAME` (same class of Key-User-field authorization gap as the earlier "Printed PO #" investigation). Matches on PO number (`CREF_MST_ID`) + the exact supplier bill number on file (`CREF_ID`, SAP's own Inbound Delivery Notification reference), cross-checked against the shipment's own item codes (`CPRODUCT_UUID`).
- **New**: `sap_inbound_delivery_report_client.py` (new OData client, same `ana_businessanalytics_analytics.svc` service, ReportID `RPZ9645E136D1191FF78E36A7`), `supplier_shipment_service.fetch_inbound_delivery_ids_from_sap()` (per-PO match, never raises on a single PO's no-match/ambiguous case) + `manually_confirm_inbound_delivery()` (writes `inbound_delivery_id`/`manually_confirmed`/`manually_confirmed_by` onto the matched PO's `per_po` entry, flips overall `sap_sync_status` to `posted` only once every PO on the shipment has posted). New `POST /admin/grn/{doc_code}/fetch-inbound-delivery` (job-poll pattern, mirrors the "Refresh from SAP" PO-cache button - a live SAP query here takes 30-100s+) + `GET .../poll/{job_id}`.
- **Frontend**: `GrnApprovalPage.jsx`'s Confirmed GRNs table - "Fetch from SAP" button inline in the blank SAP Inbound Delivery # cell (stops row-click propagation), polls to completion, shows a "Manually confirmed from SAP" badge once filled (same badge mirrored read-only in the row's detail dialog).
- Live-verified against REAL SAP data (read-only, no write): PO 29368 + bill "FWIPL/26-27/T555" correctly resolves to Inbound Delivery **52897** exactly matching the user's screenshot. Tested via `testing_agent` (iteration_158): backend 7/7 pytest (positive live-SAP match, negative fake-bill case returns clean failure, auth/site-access guards, no regression to existing list endpoints), frontend 100% (button only on Confirmed tab, loading state, badge + Posted status after fetch, detail dialog mirrors it). One cosmetic bug found+fixed: a stray `\u2014` string escape rendered literally instead of an em-dash (was outside a string literal in JSX).

## GRN Failure-Screenshot feature: non-blocking fix + Confirmed GRNs dialog gap FIXED (Sep 11, 2026 session)
- **User's ask**: continuation of prior session's Object Storage screenshot-on-GRN-failure feature - explicitly asked "make sure u do not slow things down" and the visual modal had never actually been screenshot-verified.
- **Real bug found+fixed**: `sap_playwright_supplier_pgr_service.py`'s `_capture_failure()` called the Object Storage upload (`object_storage_client.upload_failure_screenshot`, a blocking `requests.put` under the hood) directly inside an `async def` function - would stall the asyncio event loop (other concurrent GRN jobs + API requests sharing the same loop) for the duration of every failure upload. Fixed by wrapping in `await asyncio.to_thread(...)`. The screenshot-serving endpoint (`GET /admin/grn/failure-screenshot`) already correctly used `asyncio.to_thread` - only the upload path needed the fix.
- **Real UI gap found by testing_agent (iteration_156) + fixed**: the "View Failure Screenshot" button only existed inside the pending-shipment Lookup card (`shipment` state) - never added to the Confirmed GRNs tab's row-click detail dialog (`confirmedDetail` state), which is the ONLY place a Sync-Failed already-approved shipment is actually browsable from. Added the same button (`grn-confirmed-detail-view-failure-screenshot-button`) inside `grn-confirmed-detail-dialog` in `GrnApprovalPage.jsx`.
- **Hardening fix (from testing_agent's code review)**: `supplier_shipment_service.attach_po_pricing()` used raw dict indexing (`it['item_number']`/`it['po_number']`) - a single shipment item missing either field would 500 the ENTIRE `/admin/grn/shipments` list endpoint for every vendor. Changed to `.get(...)`.
- Verified via `testing_agent` (iteration_156 backend 100%, iteration_157 frontend 100% after the dialog fix) - failure-screenshot button now renders + opens + loads the real image from both the pending-lookup card AND the Confirmed GRNs detail dialog. Synthetic shipment/session test data cleaned up after both rounds.

## What's been implemented (Sep 10, 2026 session, continued - custom SAP "Printed PO #" surfaced app-wide)
- **User's ask**: extract the tenant's custom SAP "Purchase Order Number" Key User field (e.g. `P1PO-00609/26-27`, the number printed on the physical PO document - different from the plain sequential internal PO number like `29347` already shown everywhere) and show it ADDITIONALLY on Supplier Dashboard, GRN Approval ("Vendor Goods Receiving"), and PO Creation/Created POs.
- **Root-cause investigation** (this took most of the session, several dead ends): the field is NOT returned by either of this app's 2 existing PO-read channels - confirmed live: (1) `QueryPurchaseOrderQueryIn`'s `PurchaseOrderSimpleByElementsQuery_sync` (what `sap_po_client.py` already uses) - field absent from a real live response; (2) the custom OData `khpurchaseorder` service (`sap_po_odata_client.py`) - `$metadata` only exposes `PODate_KUT`/`PortalPRNumber_KUT`/`BusinesResidence_SDK`, this newer field isn't there either. User's screenshot of SAP's own Adaptation Mode "Further Usage of Extension Field > Services" tab was the key clue: "Field Available" is checked ONLY for `ManagePurchaseOrderIn`'s "Manage Purchase Order Read" row (message `PurchaseOrderByIDResponse_sync`), unchecked for the Simple Query's inbound/outbound rows. A web search then confirmed the correct request shape (`<PurchaseOrder><ID>{po_number}</ID></PurchaseOrder>` under `PurchaseOrderByIDQuery_sync`, namespace `http://sap.com/xi/SAPGlobal20/Global`, hit on `SAP_SOAP_PO_MANAGE_ENDPOINT`) - several earlier guesses (`<PurchaseOrderID>` at root, `<PurchaseOrderKey>` wrapper, raw UUID) all silently returned an empty `<Log/>` with zero error, which is why this took multiple live round-trips to pin down. Field itself is populated immediately at PO creation (confirmed on 3 different real POs, no workflow delay) and is simply `<n1:PurchaseOrderNumber>` (NOT hash-suffixed) inside the A4CF6 namespace in this response.
- **Backend**: `sap_po_write_client.SAPPurchaseOrderWriteClient.get_purchase_order_number(po_number)` (best-effort, never raises) + new Mongo cache `sap_po_custom_number_cache` (`supplier_shipment_service.py`: `get_sap_po_number`/`store_sap_po_number`/`attach_sap_po_numbers`/`list_po_numbers_missing_custom_number`) + `server.py`'s new `start_sap_po_number_refresh_loop` (5 min, catches up 15 POs/cycle for ones the vendor PO cache already knows about but haven't been resolved yet). `create_purchase_order` does one immediate live fetch+cache right after a real create (so it's instant on the success screen, not waiting on the loop); `/purchase-orders/history` and GRN endpoints (`/admin/grn/shipments`, `/admin/grn/lookup/{doc_code}`) all join from the cache only (never a live call inline, keeps those endpoints fast).
- **Frontend**: `SupplierDashboardPage.jsx` (PO table row + detail modal), `GrnApprovalPage.jsx` (single lookup item table + Confirmed GRN detail popup), `PurchaseOrderPage.jsx` (result dialog), `CreatedPurchaseOrdersPage.jsx` (new column + detail field + CSV export column) - all labeled "Printed PO #" to distinguish from the existing internal "PO Number"/"SAP PO #", all optional-rendered (never shows blank/undefined for an uncached PO).
- Live-verified: PO 29086 -> `P1PO-00609/26-27`, PO 29347 -> `P1PO-00641/26-27` (both pre-warmed manually this session), then `testing_agent` (iteration_153): 100% pass both backend (all 3 endpoints return the new key, null-safe) and frontend (all 4 surfaces render correctly, zero regressions to existing PO Price/Qty/Ship columns). PO Creation's own result dialog was code-reviewed only (not live-clicked - real production SAP write). Synthetic test sessions deleted after.
- **Follow-up 1 same session (user report + screenshot)**: shipment `39LDDQ` (PO 29206) showed NO "Printed PO #" at all on the single-code GRN lookup screen - the 5-min background catch-up loop (15 POs/cycle) simply hadn't reached PO 29206 yet, and the single lookup endpoint was cache-only (by design, to stay fast). Fixed: new `supplier_shipment_service.ensure_sap_po_numbers_live()` - a single GRN lookup only ever touches a handful of distinct POs, so it's safe to do a live SAP fetch there for whichever ones are still missing (attaches immediately + caches for every other screen too), unlike the list endpoints which intentionally stay cache-only. Wired into `/admin/grn/lookup/{doc_code}` only. Live-verified: PO 29206 (previously uncached, deliberately reproduced) now resolves live to `P2PO-01382/26-27` in ~1.5s and shows correctly on the lookup screen.
- **Follow-up 2 same session (user ask + screenshots)**: add "Printed PO #" as its own column (not just inline subtext) on 3 more list views: GRN Approval's "Pending Shipments" tab table, GRN Approval's "Confirmed GRNs" tab table, and the Supplier Portal's own Shipments page (`SupplierShipmentsPage.jsx`, "In-Transit Shipments & Dispatch Tracking"). All 3 are cache-only joins (`attach_sap_po_numbers`, no live SAP call) since these are list views showing many shipments at once - uncached rows just show "—". Live-verified via screenshot: all 3 tables render the new column correctly (e.g. PO 29086 -> "P1PO-00609/26-27", uncached PO 28792 -> "—", no crash/blank).
- **Follow-up 3 same session (user ask + screenshots)**: (a) added "Printed PO #" to the internal "Open Purchase Orders" page too (`OpenPurchaseOrdersPage.jsx`, `/purchase-orders/open` - already reused `get_cached_pos_with_remaining` so only the frontend needed the new line). (b) User reported many blanks on "Created Purchase Orders" even for POs created well after today's fix - investigated live and found SAP itself is inconsistent about when this field becomes readable right after a PO Create (some brand-new POs resolve instantly, e.g. 29336/29216; others created minutes apart with the identical company/site/vendor return empty, e.g. 29205/29204 - not a bug in our parsing, confirmed by testing the identical live call). Fixed the resilience side: `list_po_numbers_missing_custom_number` now also pulls from `purchase_order_creation_history` (last 30 days), not just the vendor PO cache, so the 5-min background loop keeps retrying every PO created via this app's own Create flow until SAP resolves it (or permanently shows "—" if SAP genuinely never sets it for that PO). **IMPORTANT ENVIRONMENT NOTE**: user's screenshot POs (29404, 29389, etc, created by real staff Sandeep Sharma/Sakshi Gupta/etc.) do not exist in this session's dev database at all - confirmed with user this was from their PRODUCTION deployment (`https://mrp.radishtechnologies.com/`), not this preview environment. **None of this session's "Printed PO #" work (Task 1 + all 3 follow-ups) is live for the user's team yet - it only exists in this preview/dev session until they hit Deploy.**

## What's been implemented (Sep 2, 2026 session)
- **Supplier Portal entity restriction**: vendor must pick RI or RT (buyer entity) before seeing/acting on POs; entity switcher pills + forced picker card when 2+ entities exist; cart clears on switch. `SupplierDashboardPage.jsx`.
- **Shipment single-entity enforcement**: `supplier_shipment_service._resolve_items()` raises `ShipmentValidationError` if a shipment (create or edit) would mix RI+RT items; `SupplierShipmentsPage.jsx`'s edit "add item" search only offers same-entity POs.
- **GRN Approval Site/Warehouse derivation**: `GET /admin/grn/lookup/{doc_code}` returns `buyer_code`, `buyer_entity_name`, `allowed_site_ids` (RI: P1/P8, RT: everything else known), and `site_access_blocked` (blocks Site/Warehouse/Approve with a red banner when the user has zero bound sites for that entity; Reject/Discrepancy stay usable). Warehouse auto-preselects a `-QC` suffixed warehouse when available. Backend also validates site_id server-side in `prepare_approval()` (400 if mismatched).
- **JDE theme visual refresh**: `GrnApprovalPage.jsx` + all 5 Supplier Portal pages now match the rest of Materials Hub (teal h-16 headers, #004B87 buttons, grid-bordered zebra tables, Chivo/IBM Plex Sans/JetBrains Mono typography - additive `tailwind.config.js` change, app-wide, no regression).
- Tested via `testing_agent` twice (iteration_140 found 2 bugs, both fixed; iteration_141 regression 100% pass, 3 cosmetic issues found+fixed same session). No real SAP production write was ever triggered during testing.
- **Production Confirmation "Net Weight mandatory" bug fix** (Sep 2 2026): `_pick_rm_item()` used to fall back to "whichever mass-tracked BOM component has the biggest quantity" whenever nothing matched a known raw-stock keyword - for a hardware-bag BOM (Wall Anchor 309,772 qty > Lag Bolt 160,540 qty, both just fasteners, no real sheet/coil/flat/rod/pipe input) this wrongly picked the Wall Anchor as "the raw material being formed" and demanded a Net Weight be configured before confirmation would proceed. Fixed by: (1) adding Rod/Pipe/Angle/Channel/Mesh/Ingot to the recognized raw-stock keyword list (validated against all 1121 real BOMs with mass components - correctly auto-classifies 115 genuine items like "MS Rod"/"MS Pipe"/"Angle 65x65x5mm" with zero false positives), (2) removing the blind "biggest quantity wins" fallback for anything that still doesn't match a keyword - it now only applies if that exact finished item already has a Net Weight configured in `component_master` (real precedent, e.g. ~9 legacy cryptic-coded items like "SUSPENSION SECTION" keep working exactly as before). Verified live against Lot 71353's real SAP MaterialInput data (returns "No raw-material found", no longer blocks) - user confirmed fix is working.
- **Valuation Rate Mismatch on Stock Transfer print - FIXED** (Sep 2 2026): `sap_valuation_client.get_standard_costs()` used to merge a product's valuation levels across ALL sites and pick whichever's price won a StartDate tie-break, regardless of which site was actually shipping. Added `site_id` param (filters by that site's `PermanentEstablishmentUUID`, falling back to all-sites merge only if the site genuinely has no valuation level for that product) + the full 8-site `SITE_TO_PERMANENT_ESTABLISHMENT_UUID` map (P1/P2/P2W/P3/P4/P7/P8/P9 - discovered empirically, no SAP service exposes this mapping directly; method documented in `sap_valuation_client.py`'s comment). Applied to both Stock Transfer print call sites (`_price_hsn_for_note` preview + `create_delivery_challan`'s permanently-stored rate) via `doc["ship_from_site_id"]`. Verified live: Copper Coil CUCOIL-1.86 now correctly returns P1=₹1456.55 vs P2=₹960.00 instead of one ambiguous merged value. User explicitly scoped this fix to Stock Transfer print only - **the same ambiguity likely still exists** in BOM Explorer / Inventory valuation / Purchasing Plan price displays (`inventory_service.py`, `purchasing_plan.py`, `server.py` lines ~814/846), which still call `get_standard_costs()` without a `site_id` - not yet fixed, user said "just Stock Transfer for now".

- **GRN Site field STILL not auto-locking - REAL FIX (Sep 2, 2026, this session)**: root cause was that `allowed_site_ids_for_buyer_code` only ever narrowed the Site dropdown to a buyer ENTITY's full site list (RI = P1 **and** P8), never the exact single site - so for any RI/RT entity that owns 2+ sites, the frontend's `eligible.length===1` auto-lock could never fire, leaving "Select site" empty exactly like the previous "fix" claimed to prevent. Found the real field: SAP's own `PurchaseOrderItem/ShipToLocation/LocationID` gives the EXACT site a PO was placed against (confirmed live via raw SOAP dump). Fix: `sap_po_client.py` now captures `ship_to_site_id` per cached PO line; new `supplier_shipment_service.derive_ship_to_site_id()` re-reads it live from `supplier_portal_po_cache` at GRN-lookup time (not stored on the shipment - so even shipments created BEFORE this fix resolve correctly once the PO cache itself refreshes) and returns a single site if all the shipment's lines agree; `/admin/grn/lookup/{doc_code}` now prefers this exact site over the old entity-wide list, falling back to the coarser list only if the exact site can't be derived (legacy cache row, or a shipment spanning 2+ sites). Live-verified: shipment `MU7DE2` (PO 29086, HAMIDI EXPORTS/RI) now returns `allowed_site_ids: ["P1"]` (was `["P1","P8"]`) and the GRN screen auto-locks Site to P1 + Warehouse to P1-QC.
- **P0 "Internal error" on live SAP production order release - CONFIRMED RESOLVED, not a bug**: job `58359056` (proposal 226256, order 70900) was checked mid-flight by the previous session and looked "failed"/"Internal error" (stale leftover fields from an earlier backend-restart interruption). Re-checked this session: `background_jobs` collection shows `status: "done"`, `result.released: true`. Live-verified directly against SAP via `sap_production_order_release_client.get_life_cycle_status('70900')` -> `{"code": "2", "label": "Released"}`. Order 70900 is genuinely Released in production SAP. No code fix was needed - the earlier `sap_semaphore` fix + the `_continue_order_creation(is_resume=True)` baseline fix already shipped were sufficient; the resume path worked correctly.
- **Supplier Portal login shortcut in admin menu - already exists, re-verified working**: `NavTabs.jsx`'s admin dropdown (labeled "Master Data" in the UI) already has a "Supplier Dashboard" item -> `/supplier-portal/login`, gated by page key `supplier_portal_admin` (super_admins bypass gating and always see it). Verified via screenshot with a synthetic `super_admin` session - link renders correctly in the "Master Data" dropdown. If the user still can't see it, check they're logged in with an email in `SUPER_ADMIN_EMAILS` (`rishabh@rampgroup.co.in`, `omprakash@rampgroup.co.in`) or has been explicitly granted the `supplier_portal_admin` page via Access Management.

- **Supplier GRN "PO not found in SAP" - REAL ROOT CAUSE FOUND + FIXED (Sep 2, 2026, this session)**: user reported PO 29086 was genuinely Released/Ordered in SAP, yet the Playwright GRN automation (`sap_playwright_supplier_pgr_service.py`) kept saying "PO not found... check its status". Root cause was a chain of THREE separate, real bugs in the automation, all found via live read-only reproduction and fixed in order:
  1. `_search_po_exact`'s base-view dropdown selector was hardcoded to the text "Open Purchase Orders", but this SAP session's actual remembered variant was "Due and Overdue Purchase Orders" (a real, narrower query that silently excludes valid POs like 29086) - per user's explicit instruction ("do not search with due and overdue filter, search in all purchase orders"). Fixed by targeting the dropdown's stable SAPUI5 ID suffix `[id$="-defaultSetDDLB"]` instead of matching current text (a naive `.sapMSlt` class selector was tried first and was ALSO wrong - it matched the unrelated global-header search-category selector, not the base-view one).
  2. Once search worked, clicking the found row (`row.click()`) landed on the "Supplier Name" hyperlink cell (row center) and navigated into an unrelated Business Partner screen instead of just SELECTING the row - "Post Goods Receipt" (a list-toolbar-only button) was then never findable. Fixed by clicking the row's empty first `<td>` (selection-indicator column) instead.
  3. The "Create Inbound Delivery and Goods Receipt" screen's own Line Items grid takes longer to load than the old fixed 4s wait - fixed by waiting for its "Loading..." text + the blocking overlay to clear first.
  4. `_fill_line_actual_quantities` used ElementHandle-only methods (`query_selector_all`) on Playwright Locator objects (from `.locator(...).all()`) - always raised `'Locator' object has no attribute 'query_selector_all'` the moment it got this far (never triggered before since bugs 1-3 always failed first). Fixed to use Locator-consistent `.locator()/.count()/.nth()`. ALSO found the "Actual Quantity" cell holds 2 inputs (qty field + a UOM combobox) - fixed to target `input:not([role="combobox"])` only.
  Live-verified end-to-end on real shipment `MU7DE2` (PO 29086, HAMIDI EXPORTS): `sap_gr_result.ok: true`, GRN screen now shows "Goods Receipt posted to SAP" (green). **NEW, SEPARATE issue found while verifying** (NOT part of this bug, a distinct downstream step, since fixed - see below): the follow-up Goods Movement (step 2, moving received stock into the P1-QC warehouse) failed with real SAP errors - "Negative stock not permitted in logistics area P1-RM, material G12LW" and "No inventory items found for external id...".
- **Warehouse Movement negative-stock bug - REAL ROOT CAUSE FOUND + FIXED (Sep 2 2026)**: live-checked shipment MU7DE2's actual on-hand stock via `sap_inventory_client.get_inventory_detail()` after its GR posted - found the received stock sits under SAP stock_status **"Inspection"** at P1-RM (this material's own SAP QM/Inspection Plan routes all PO receipts there), not "Not Assigned"/blank. `_post_goods_movement_for_items()` in `supplier_shipment_service.py` always left `InventoryStockStatusCode` blank (assuming plain/"Not Assigned" stock) - P1-RM only had 1.0 EA under that blank bucket for G12LW (causing "Negative stock not permitted", asked for 2) and ZERO for G12FW (causing "No inventory items found"). This does NOT contradict the existing Aug 28 2026 finding in the same docstring (that forcing a stock-status CHANGE via this API fails) - this is a plain relocation of stock that's ALREADY at Inspection status, so it must be moved AS Inspection status on both ends, not blank. Fixed with a new `_resolve_source_stock_status()` helper that live-queries `sap_inventory_client` at call time to find which status code the source area's balance for that exact material/qty actually sits under, and targets that (falls back to blank if nothing matches). Threaded `sap_inventory_client` through `finalize_goods_receipt()`/`retry_goods_movement()` and both `server.py` call sites. Live-verified: MU7DE2's retry-movement now returns `sap_movement_status: "posted"` for both G12LW and G12FW; SAP inventory confirms 2.0 EA of each moved from P1-RM(Inspection) to P1-QC(Inspection).
- **GRN progress visibility + SAP Inbound Delivery # (Sep 2 2026, user's ask)**: `sap_playwright_supplier_pgr_service.py`'s `_post_one_po` now reports granular sub-steps (searching/opening_receipt/entering_quantities/saving, per PO) via `on_step`, combined into one consistent step-counted `progress_cb(phase, current, total)` across the WHOLE approve() job (Playwright GR + the Goods Movement step after it) via `total_progress_steps()`. Also captures the SAP-generated Inbound Delivery ID from the same success/error message strip (`_extract_confirmation_text` + `_extract_inbound_delivery_id`) and returns it as `inbound_delivery_id` per PO. `GrnApprovalPage.jsx` now shows a real progress bar + current-step label + a countdown estimate (`~Ns left`, clamps to "Almost there..." rather than going negative) while a job runs, and displays "SAP Inbound Delivery #: ..." once posted. Live-verified structurally on a real shipment (39GA52): job reached `progress_current: 7/7`, all phases (queued -> logging_in -> searching -> opening_receipt -> entering_quantities -> saving -> done) fired correctly, countdown ticked down in sync with elapsed time. **NEW issue surfaced while testing** (separate, PO 28792 specifically - shared by shipments Q7YA7Z/39GA52/WVHKWT/LFQP9Z): SAP rejected the actual save with "Actual quantity or notification item ID or notification quantity missing" - a DIFFERENT SAP-side validation than any bug fixed so far this session (there's a separate "Notification Quantity" grid column, distinct from "Actual Quantity", that this PO's items may require populated - not yet investigated). Shipment 39GA52 is now `status: "approved"` (safe, internal approval never blocked) with `sap_sync_status: "pending"`, retriable once this is root-caused.

- **"Output posted but Task still open in SAP and Emergent" - REAL ROOT CAUSE FOUND + FIXED (Sep 2 2026, live incident Lot 71425)**: reproduced live via `find_lot_by_id('71425')` - `confirmation_finished: true` (output fully posted, 320/320, frozen forever once set) but `task_finished: false` (Task never closed). Root cause: `confirm_reporting_point()` chains a SEPARATE standalone `finish_task()` call right after the ReportingPoint update succeeds, but never wrapped it in try/except - when `finish_task()` raised a SOAP fault, it propagated straight out as an exception, discarding the fact the ReportingPoint update had ALREADY succeeded (irreversible). `_confirm_production_inner`'s exception handler then reported total failure and never logged anything (confirmed zero history entries for Lot 71425). The user's retry then failed with "Changes in task or lot not permitted; confirmation complete indicator set" - SAP correctly rejecting a second attempt to resend already-frozen fields. Fixed: (1) `finish_task()` call now wrapped in try/except so a failure is preserved as a normal partial result, not an uncaught exception; (2) new endpoint `/production-confirmation/finish-task` (mirrors `/retry-wip-clearing`) that calls ONLY the standalone Finish Task action - safe regardless of ReportingPoint state; (3) `ProductionConfirmationPage.js`'s Confirm dialog now detects this exact stuck state (`confirmation_finished=true && task_finished=false`) and swaps the quantity form for a single "Finish Task" button, so this can't be walked into again. Live-verified: Lot 71425 now shows `task_finished: true`, `life_cycle_status_label: "Finished"`.

## What's been implemented (Sep 10, 2026 session - Return to Store: resolved returns move to Journal, not Pending tab)
- **User's ask**: "if pending store return resolved it will now [be] seen as [a] view in the all request[s tab], not the pending return request [tab]. ex: RTN-000006" - a Resolved/Rejected return was still lingering (as a read-only "View" row) inside the "Pending Store Return" tab, which should only ever show actionable items.
- **`StoreReturnsPanel.jsx`** ("Pending Store Return" tab): now filters to `status in (pending, under_verification)` only at load time - resolved/rejected returns no longer appear here at all. Removed the now-dead "View" action/dialog/Eye icon from this component.
- **`StoreApprovalPage.js`** ("Journal (All Requests)" tab): new `resolvedReturnRows` (resolved/rejected returns from `/store-returns/journal`, respecting the existing Search/Site/Status controls) rendered as extra rows at the end of the Journal table - orange "RETURN" tag next to the RTN id (same visual convention as the Movement History tab's return tag), a "View" button opens the same `ReturnDetailDialog`/`ReturnPrintSlip` this page already had access to via the Return tab. Result count now reads "N request(s) (M return)".
- Live-verified via screenshot with a synthetic super_admin session: Pending Store Return tab now shows only RTN-000007 (pending)/RTN-000005 (under_verification) - RTN-000006 (resolved) is gone from there; Journal (All Requests) tab shows "26 request(s) (1 return)" with RTN-000006 as a tagged row at the bottom, View opens the detail dialog showing the real SAP Goods Movement (274307). Synthetic session deleted after.
- **Follow-up bug found+fixed same session (user report)**: on Movement History, P1-000006's Return row showed blank "From Warehouse"/"To Bin". Root cause: the Sep 9 return-row builder in `movementRows` hardcoded both to `null` instead of reflecting the real reversed SAP movement direction (`store_return_service.run_confirm_movements`: source=site's SFG warehouse, target=site's RM warehouse - always fixed by site, same pair the backend itself uses, never stored per-item). Fixed to `warehouse: \`${site_id}/${site_id}-SFG\`` / `target_bin: rmWarehouseIdForSite(site_id)`. Verified live: row now shows "P1 - Semi-Finished Goods (SFG) - RI" \u2192 "P1 - Raw Material (RM)" for RTN-000006/Moved (274307).
- **Redundant per-row Print columns removed (same session, user report+screenshot)**: "My Stock Requests" (`MyStockRequestsTab.jsx`) and "Return to Store" (`ReturnToStoreTab.jsx`'s `ReturnList`) each had a standalone "Print" button per row - but clicking the Request/Return ID already opens a detail dialog that itself has a Print button, so the column was pure duplication. Removed both columns (Return list's Action column now only shows "Edit & Resubmit" for rejected returns, "\u2014" otherwise) - the detail-dialog Print flow (`printTarget`/`RequestPrintSlip`/`ReturnPrintSlip` portal) is completely untouched.
- **RTN-000006 print bleed-through on Journal tab - FIXED (same session, user screenshot)**: printing the RTN-000006 detail dialog from the new Journal "View" row showed the print slip correctly BUT with the entire Journal table bleeding through underneath it. Root cause: the Journal/Balance/Queue/Movements list `<header>`/`<main>` (and the "Pending Store Return" tab's own `<header>`/`<main>`) never had a `print:hidden` class - only the single-request DETAIL view (`/storeapproval/request/:id`) had it, which is why THAT print flow (RequestPrintSlip) never showed this bleed-through. Added `print:hidden` to both list-view `<header>`/`<main>` pairs, matching the existing detail-view pattern exactly. Verified via print-media emulation: both elements now correctly compute `display: none` under `@media print`.
- **"Issued By"/"Received By" on resolved print slips (same session, user's explicit ask)**: once a Store Issue is Resolved, `RequestPrintSlip.jsx` now prints "Issued By : {store_actor}"; once a Return is Resolved, `ReturnPrintSlip.jsx` now prints "Received By : {store_actor}" (both fields already existed on their respective Mongo docs, just weren't surfaced on the slip - no backend change). Hidden entirely for non-resolved requests/returns (nothing to attribute yet). Verified live: P1-000006's slip shows "Issued By : Ankit Sharma", RTN-000006's shows "Received By : Ankit Sharma".
- **PR-driven PO Creation "Not matched in SAP" investigated (same session, user report, PR 124860 + 3 more)**: reproduced for PR 124860 (CCOIL1.8X255/COIL1.39x448) and checked 3 more PRs the user named (124922, 124864, 124894) directly against live SAP - every unmatched line is a genuinely brand-new custom coil/sheet size never created in SAP yet (confirmed absent from both `inventory_cache` and a live exact-ID SAP lookup), except one data-entry issue found on PR 124922 (`HRPOCOIL` should be `HRPO2.8X171`, which already exists in SAP under that code). User explicitly declined a manual-override UI fix and confirmed "block until material is created in SAP" is the desired behavior - **no code changes made**, diagnostic only.
- **GRN Approval "In Transit" status investigation (same session, user report)**: user's screenshot (shipment X7SLJT, PO 29073) showed "In Transit" - traced the code and confirmed `status` flips to "approved" synchronously the instant `POST /admin/grn/{code}/approve` succeeds (before the slow Playwright SAP posting even starts), so it can never legitimately show "In Transit" after a real approval through this app. User confirmed inbound delivery 52723 already exists in SAP, then moved on before we could pin down whether it was posted via the app's own flow or done directly in SAP - **left unresolved, no code changes made** (user said "skip this one").
- **New "Confirmed GRNs" list + detail popup (same session, user's explicit ask)**: `GrnApprovalPage.jsx` had no way to browse already-approved GRNs (only a single-code lookup) - added a "Confirmed GRNs" table (reuses existing `/admin/grn/shipments?status=approved`) below "Pending Shipments", refreshed after every successful approve. Clicking a row opens a popup (`grn-confirmed-detail-dialog`) with the full trail: supplier invoice number, bill date, SAP Inbound Delivery #(s) (parsed from `sap_gr_result.per_po[].inbound_delivery_id`), site/approver, and an item-level table (item code, description, qty, unit, warehouse). Verified live via screenshot (EZ9BWD -> inbound delivery 52653).
- **GRN Approval tabbed layout (same session, user's explicit ask)**: refactored the stacked "Pending Shipments"/"Confirmed GRNs" tables into a tab strip (matches `StoreTabStrip`'s pattern, GRN page's own `#004B87` blue instead of teal) below the lookup bar - only one table renders at a time. Added a "Search PO Number or Vendor Code" filter input independently on each tab (matches against `vendor_code`, `company_name`, and every PO number on the shipment). Verified live via screenshot: tab switch hides/shows the right table, filter narrows rows correctly, no-match shows an empty state, no overflow at 1920px.
- **PO Price on Confirmed GRN popup (same session, user's explicit ask, "do not change anything just need price")**: `/admin/grn/shipments` list endpoint now also calls the existing `attach_po_pricing` helper (same one the single-code lookup screen already used) per shipment - added "PO Price"/"Line Value" columns to the Confirmed GRN detail popup's item table only, nothing else touched. Verified live: EZ9BWD's popup now shows INR 1.26/INR 3.04 per line.
- **GRN stuck-job investigation + "In Process" flag/Retry cooldown (same session, user report on PNC9DW)**: traced why a GRN can sit with no SAP Inbound Delivery # indefinitely - the background Playwright job retries 3x then gives up, but only marks its OWN job record `failed`, never updates the shipment's own `sap_sync_status` (stays `pending` forever, indistinguishable from "still genuinely running"). Per user's explicit ask, added (not yet the deeper sync fix): an "In Process" badge wherever `sap_sync_status !== "posted"` (both the Confirmed GRN list's new "SAP Status" column and its detail popup, plus the single-code lookup screen's existing banner), and gated the "Retry" button behind a 5-minute cooldown from `approved_at` (shows "Retry available in Xm" countdown, ticks every 15s) so nobody retries while a job might still legitimately be running - `sap_sync_status === "skipped"` (a known permanent SAP error, e.g. "PO not found") is exempted from the cooldown since that's already a definitive failure. Verified live via screenshot: badge shows, cooldown counts down, Retry appears once past 5 min.
- **New "Closing Inventory Report" page (Sep 10 2026, user's explicit ask: "31st Aug inventory data in excel, company wise plant wise Qty with value")**: this app only ever kept a LIVE current inventory snapshot - no archive of past dates existed. Researched + verified a DIFFERENT SAP report that supports true historical "as of" queries: the standard `ana_businessanalytics_analytics.svc` service's `RPFININVU14_Q0001QueryResults` entity ("Material Inventories - Balance Summary"), filterable by `PARA_KEYDATE` + `PARA_PERMEST` (site - confirmed 1:1 with "Company" per user's answer). Built `sap_inventory_closing_client.py` (queries every known site in parallel via `inventory_service.list_known_sites`, since SAP takes 60s-200s+ PER SITE for its full unfiltered material list - had to bump timeout from 170s to 280s after a real P1 query took ~196s) + `inventory_closing_report_service.py` (sums any duplicate site+material rows SAP returns across historical fiscal-period breakdowns) + a background-job pattern (`POST /api/admin/inventory-closing-report/generate`, `GET .../generate/{job_id}`, matching the existing L1/L2 report's job_store convention - a single HTTP request would time out). New page `InventoryClosingReportPage.js` (nav: Master Data > Closing Inventory Report, `/admin/closing-inventory-report`) - date picker, "Fetch from SAP" (polls the job, shows elapsed seconds), flat table + one Total row at the end (user's explicit choice, no per-site subtotals), "Export Excel" (client-side via `xlsx`, same pattern as the L1/L2 report). Verified live end-to-end for 2026-08-31: 99 site/item rows (only sites P1 & P8 - Ray International entity - had any FI-posted balance as of that date; the other entity's sites returned zero rows, which is correct per SAP, not a bug), total qty 799,806.09, total value INR 35,47,512.28.

## What's been implemented (Sep 9, 2026 session, continued - Vendor re-apply after rejection FIXED)
- **User's report**: a supplier submitted the signup form without a vendor code, staff rejected the request, and now that supplier can't re-apply (even with the correct vendor code) - gets "An account with this email already exists".
- **Confirmed vendor_code IS already mandatory** both client-side (HTML5 `required`, reproduced via screenshot - browser blocks blank submit) and server-side (`supplier_portal_service.signup` returns `400 "All fields are required"` for blank/whitespace-only, reproduced via direct API call) - not the root cause here.
- **Real root cause**: `reject_account()` only ever sets `status: "rejected"` on the account record - it's never deleted. `signup()`'s email-uniqueness check blocked on ANY existing record regardless of status, so a rejected applicant was **permanently locked out** of that email, even when resubmitting with a corrected vendor code.
- **Fix** (`supplier_portal_service.py`): `signup()` now allows re-applying with the same email if the existing record's status is `"rejected"` (old rejected record is deleted, fresh application proceeds normally). Still correctly blocks duplicate signup while `"pending"` or `"approved"`.
- Live-verified full cycle via API: signup (BADCODE1) -> admin reject (reason: "No vendor code found in SAP") -> re-signup same email with corrected vendor code (S1822) -> succeeded (`pending_approval`, new account id). Test data cleaned up after.

## What's been implemented (Sep 9, 2026 session, continued - PR-driven PO Creation: "Not matched in SAP" false-positive FIXED)
- **User's ask/bug report**: creating a PO from PR 125083 (item "M4 SQUARE NUT", icode `SNUTM4SAM`) showed "Not matched in SAP - remove this line to proceed" even though the user had personally confirmed the item is valid and activated in the SAP Material Master.
- **Real root cause found + fixed**: PR line matching (`po_pr_lookup` in `server.py`) only ever checked the local `inventory_cache` collection - which is sourced from SAP's own **On-Hand Inventory stock report** (`inventory_service.refresh_inventory_cache`), NOT the Material Master. A material that's genuinely active/released in SAP but has **zero on-hand stock anywhere** (exactly the case for a brand-new raw material being purchased for the very first time via this very PR) can structurally never appear in a stock report, so it always showed "Not matched" regardless of freshness. Live-confirmed via a direct SAP call: `SNUTM4SAM` resolves to a real UUID with `LifeCycleStatusCode=2` (Active/Released) and Description "M4 SQUARE NUT" - it was never actually missing from SAP, just invisible to the stock-based cache.
- **Fix**: for any PR icode the local cache misses, `po_pr_lookup` now falls back to a live single-ID SAP Material Master lookup (`sap_material_client.resolve_material_info`, extended to also parse `Description`/`BaseMeasureUnitCode`/`LifeCycleStatusCode` from the same SOAP response) - only trusted as a match if SAP itself reports the material as Active (`LifeCycleStatusCode == "2"`). New `matched_via: "cache"|"sap_live"` field on the API response; `PurchaseOrderPage.jsx` shows a small green "LIVE SAP MATCH" badge on any line resolved this way, for transparency.
- Live-verified end-to-end: `GET /purchase-orders/pr-lookup/125083` now returns `matched_product_id: "SNUTM4SAM"` (previously null); UI screenshot confirms the PR Creation page fully unblocks (all 6 checklist items pass, "Create Purchase Order in SAP" button enabled) - did NOT click Create (would push a real permanent PO). Synthetic test session cleaned up after.
- **Known residual limitation** (not fixed, out of scope for this bug): the manual product-search autocomplete (`/purchase-orders/products/search`, used when adding a blank line by hand) still only searches `inventory_cache` - a zero-stock item still can't be found by typing its name manually. Only the PR-autofill path got this live fallback, since PR lines already carry a near-certain exact SAP ID (`icode`) to check directly - a genuine free-text SAP Material Master search would need a new SAP query capability (SOAP `MaterialByElementsQuery_sync` is exact-ID-range only, not full-text), a bigger separate task if the user wants it.

## What's been implemented (Sep 9, 2026 session, continued - Return to Store: Step 1 search + Remarks field fix)
- **User's report**: no way to search Step 1's "Select Previous Request" list; Remarks field "not able to write" in both Manual and Against Request forms.
- **Step 1 search**: added a search input (`pick-request-search-input`) filtering by Request ID or Site, matching the same pattern as the Production Return List's own search.
- **Remarks "not able to write"**: not actually a typing bug - the field was only ever rendered when Return Reason == "Other" (per the original spec literally), so for every other reason there was simply no field to type into at all. Changed to always show Remarks (optional unless reason is "Other", where it's still required) in both `AgainstRequestItemsStep` and `ManualReturnStep`.
- Verified live via screenshot: search narrows results correctly, Remarks now typeable with any reason selected.

## What's been implemented (Sep 9, 2026 session, continued - Return to Store: real SAP ID bug + print bleed-through + clearer multi-item error)
- **User's report**: real SAP Goods Movement ID was 274343, but the app printed "MOV-2B1632" instead; asked to check why one item in a multi-item return succeeds and another shows an error; attached a print screenshot showing garbled/blank-space output.
- **Root cause #1 (systemic, affects the ORIGINAL issue flow too, not just Return)**: `sap_goods_movement_client.py`'s `goods_movement()` was returning `external_id` = the locally-generated placeholder (`MOV-xxxx`, sent to SAP as `<ExternalGACID>`) instead of parsing SAP's own real, permanent document number - confirmed live by inspecting the actual stored SOAP response: `<GACDetails><ExternalGACID>MOV-2B1632</ExternalGACID><GACID>274343</GACID></GACDetails>`. **Fixed**: added `_extract_gac_id()`, now returns the real `GACID` as `external_id` (kept the same key so no other caller/UI needed to change), original placeholder preserved separately as `client_reference_id`. Backfilled the one already-affected doc (RTN-000005) by re-parsing its already-stored raw SOAP response.
- **Root cause #2 (P11454-1 showing an error while P10080B succeeded, real data, not a bug in the new code)**: investigated - P11454-1's original Stock Request issue was NEVER actually posted to SAP in the first place (`goods_movement.attempted: false, reason: "No stock on file in this site's RM warehouse for this component"`) even though the app's own bookkeeping still marked it "Issued" - a pre-existing gap in the original issue flow's data, unrelated to this new feature. There is genuinely nothing in SAP to reverse for that line. Added a much clearer error for this exact case (`issued_via_sap` flag threaded through `store_return_service.py`) instead of the generic "could not determine owning party" message.
- **Regression caught + fixed same session**: the global `@media print { #root { display: none } }` rule I first tried broke the ORIGINAL, pre-existing Store Issue detail print ("Print / Save as PDF" on `StoreApprovalPage.js`'s detail view - `<RequestPrintSlip>` is rendered INLINE inside `#root` there, not via portal, so hiding `#root` blanked it). Reverted that global rule immediately on user report; the targeted fix (closing the Process dialog before printing in `StoreReturnsPanel.jsx`) was sufficient on its own and doesn't touch the unrelated inline print flow. Verified both flows live: Store Issue print now renders correctly again (`#root` stays `display: block`), and Return print still has zero dialog bleed-through.

## What's been implemented (Sep 9, 2026 session - NEW FEATURE: Return to Store workflow, Production + Store)
- **User's ask**: full "Return to Store" workflow - Production can return material (against a prior Stock Request, or manually) to Store, Store verifies/confirms/rejects, confirmed returns post a real SAP Goods Movement (reverse of the existing issue flow). Plus: My Stock Requests detail modal, Store tab rename.
- **User's explicit choices**: (1) reuse existing `production_confirmation`/`store_approval` page permissions, no new grantable right; (2) global `RTN-000001` sequence; (3) a rejected return can be edited + resubmitted (SAME id), not recreated from scratch.
- **New backend** (`store_return_service.py`, new `store_returns` + `store_return_counters` collections): mirrors `store_approval_service.py`'s issue flow but reversed (SFG -> RM warehouse) - reuses the SAME `sap_goods_movement_client`/`_trigger_goods_movement`/dry-run flag, no new SAP integration. Owner party for the SAP call: reuses the original issue's `issued_from_owner` for "against request" returns (traceability), or looks up the SFG warehouse's current owner live via `inventory_cache` for manual returns. `already_returned_qty` only counts `status=="resolved"` returns (pending/rejected never actually moved stock). New endpoints under `/api/store-returns/*` in `server.py`, gated via a new `auth_service.py` PAGE_ROUTE_RULES entry granting both existing pages.
- **Production side** (`ReturnToStoreTab.jsx`, new 4th tab on `/production-confirmation`): "A. Return Against Request" (2-step: pick a prior Stock Request -> see actually-issued items with live Already-Returned/Available math, enter Return Qty + Reason, "Other" reveals a required Remarks field) and "B. Manual Return Request" (multi-row form, item-code autocomplete via existing `/products/search`). "Production Return List" below shows every return with a clickable Return ID -> detail dialog + Print. Rejected returns show an "Edit & Resubmit" button that reopens the same flow prefilled.
- **My Stock Requests enhancement** (`MyStockRequestsTab.jsx`): Request ID is now clickable, opening a detail dialog with header info + a full item-level table (Requested/Store Issued/Pending Qty, computed from the real `issued_qty` field, never assumed) + totals row. Existing Print/grouping/filtering untouched.
- **Store side** (`StoreReturnsPanel.jsx`, new "Pending Store Return" tab on `/storeapproval`, right after the renamed "Pending Store Request" - was "Pending Queue", label-only change): queue of pending/under_verification returns; "Process" opens a verification dialog (Originally Issued/Already Returned/Current Return table for "against request" returns, straight item table for manual) with "Reject Return" (mandatory reason) and "Confirm Return" (async SAP Goods Movement job, polled - only reaches "Resolved" on real SAP success, retryable on failure, never silently marked done). Implemented as a fully separate early-return render branch in `StoreApprovalPage.js` to guarantee zero risk to the existing queue/journal/balance/movements code (user's explicit "do not redesign this page").
- Backend curl-verified end-to-end (create/previous-requests/against-request items/validation errors/process/reject/edit-resubmit) using real data (requester "Ankit", request `bdc0e560-...`, site P2). Frontend E2E-verified by testing_agent - **all 9 scenarios passed, zero bugs found** (`/app/test_reports/iteration_152.json`). Per explicit agreement with user, "Confirm Return" was verified present/enabled but never clicked to completion in either round of testing, since `SAP_GOODS_MOVEMENT_DRY_RUN=false` in this environment (a click would post a real, permanent SAP Goods Movement) - actual SAP posting on Confirm is the one part of this feature still unverified end-to-end.
- **Backlog note from testing_agent's code review**: Store's "Process" dialog auto-transitions the return to "Under Verification" the moment it's opened - if Store closes the dialog without confirming or rejecting, the return leaves the "pending" filter and needs deliberate re-opening to act on again (not reverted automatically). Not fixed this session since the user's spec didn't call for this behavior either way.

## What's been implemented (Sep 9, 2026 session, continued - Table header case consistency)
- **User's ask**: "table header font and case needs to be consistent. due date is in running case while others are caps."
- **Root cause**: `SupplierDashboardPage.jsx`'s "PO Date"/"Due Date" sortable headers are `<button>` elements - Tailwind Preflight forces `text-transform: none` on all `<button>`s, silently overriding the `uppercase` inherited from the `<thead>` row (every other header is a plain `<th>` text node, unaffected). Chose ALL CAPS as the one consistent case (matches the other 11 headers already on this table, plus the modal/every other table in the app).
- **Fix**: added an explicit `uppercase` class directly on both sort buttons (class beats the type-selector Preflight reset). Verified live: `innerText` now renders "PO DATE"/"DUE DATE", computed `text-transform: uppercase`.

## What's been implemented (Sep 9, 2026 session, continued - Dashboard: hide irrelevant Ship Qty controls)
- **User's ask**: "why even show it if its not relevant?" - for rows with 0 Open Qty (fully received/shipped), the checkbox and Ship Qty input were showing as grayed-out/disabled instead of being hidden entirely.
- **Fix** (`SupplierDashboardPage.jsx`): rows with `remaining_qty <= 0` now show a plain "—" in the checkbox column (instead of a disabled checkbox) and "Fully shipped" text in the Ship Qty column (instead of a disabled input box). Rows with Open Qty > 0 are unaffected - checkbox/input still work exactly as before once ticked.
- Self-tested via screenshot: fully-shipped rows now show "—"/"Fully shipped", open rows still select/ship correctly.

## What's been implemented (Sep 9, 2026 session, continued - Vendor Portal Fiori font + PO Qty column + modal fixes)
- **User's ask**: "PO modal detail table view is not clear with columns demarcated" + "PO quantity is not displayed anywhere in the table in background" + "Change entire supplier dashboard...to Helvetic 72SAP fiori font".
- **SAP "72" Fiori font app-wide on Vendor Portal**: added real `@font-face` declarations (Regular/Light/Semibold/Bold) sourced from the official Apache-2.0-licensed `@sap-theming/theming-base-content` npm package via jsDelivr CDN (`index.css`) - confirmed live (200 OK) for all 4 weights. New `.supplier-fiori-font` scoped class (with `!important` - needed since it ties in specificity with Tailwind's `.font-sans`/`.font-heading`/`.font-data` utility classes already used everywhere in this section) sets `font-family: "72", "72full", Helvetica, Arial, sans-serif` - the exact stack SAP's own Fiori docs recommend. Applied to the root container of every `/supplier-portal/*` page (`SupplierPortalLayout.jsx` - covers Dashboard/Shipments/Documents/Audits - plus `SupplierLoginPage.jsx`/`SupplierSignupPage.jsx`/`SupplierPendingPage.jsx` individually since those render outside the shared layout). Verified via `getComputedStyle` on the page body, an `h1`, and inside the PO detail modal - all resolve to `"72", "72full", Helvetica, Arial, sans-serif`.
- **PO detail modal - real column borders/padding added** (`SupplierDashboardPage.jsx`): rebuilt from a borderless `border-t`-only table to the same bordered/zebra-striped/uppercase-header pattern as the main dashboard table (`border border-[#E2E8F0]`, `bg-[#F1F5F9]` header, `px-2 py-1.5`), widened modal `max-w-2xl` -> `max-w-3xl` so columns aren't cramped.
- **"PO Qty" column added to the main background table** (was only ever shown inside the detail modal) - new column between "PO From"/"PO Date" and "Unit Price", sourced from the same `po.po_qty` field already returned by `/purchase-orders` (no backend change needed).
- **Bug found+fixed while verifying**: PO detail modal's line items were rendering in reverse Item order (4,3,2,1 instead of 1,2,3,4) - `detailItems` now sorted numerically by `item_number` before render.
- Self-tested via screenshot tool (test@test.com/Test123, vendor H1330): confirmed computed font-family app-wide, modal columns now clearly bordered, PO Qty column visible in both places, Item order now ascending.

## What's been implemented (Sep 9, 2026 session, continued - Supplier Dashboard live-fetch verified + Created POs export/deep-link)
- **Supplier Dashboard live SAP qty pull on page load - VISUALLY VERIFIED**: screenshot-confirmed the "Pulling live Open Qty from SAP for your Purchase Orders..." spinner state renders immediately on login, then resolves to live In Transit/Received/Open columns with "SAP-verified just now" badges (vendor H1330/HAMIDI EXPORTS, test@test.com). Closes out the item carried over from the previous session's handoff.
- **"Export CSV" button on Created Purchase Orders page** (`CreatedPurchaseOrdersPage.jsx`): client-side CSV export of the currently filtered/loaded PO list (SAP PO #, Supplier, Site, Bill-To, PO Date, PR Number, Items, Created By, Created At) - no backend change, exports exactly what's on screen. Disabled when the list is empty.
- **PO creation success - clickable PO number + toast**: `PurchaseOrderPage.jsx`'s success dialog now renders the new SAP PO number as a clickable underlined link (navigates to Created POs page, deep-linked); also fires a `toast.success` with a "View PO" action button immediately after a live PO create. `CreatedPurchaseOrdersPage.jsx` reads a new `?po=<number>` query param on load and auto-opens that row's detail dialog once its history list has loaded, then clears the param from the URL.
- Self-tested via screenshot tool (synthetic session with `created_purchase_orders`+`purchase_order` pages): export button visible, real CSV download triggered (`created-purchase-orders-2026-09-09.csv`), deep-link `?po=29336` correctly auto-opened PO 29336's detail dialog and cleaned the URL. Synthetic test session/user deleted after verification.

## What's been implemented (Sep 8, 2026 session - Vendor Portal Swiss High-Contrast redesign + Audits & QC)
- **NOTE**: the "JDE theme visual refresh" line above (teal `#0E7C86` headers) is now SUPERSEDED for the Supplier Portal only - user requested a full professional redesign, `design_agent` produced a NEW `/app/design_guidelines.json` (Swiss High-Contrast: Slate/Indigo/Blue, forbids the old teal `#0E7C86`). Internal app pages (GRN Approval etc.) still use the old JDE teal theme - only `/supplier-portal/*` pages were restyled.
- **New shared shell `SupplierPortalLayout.jsx`**: persistent left sidebar (dark slate `#0F172A`, nav items Dashboard/Shipments/Documents/Audits & QC with active-route highlight + "QMS" badge on Audits) replaces the old per-page teal top navbar across all 4 vendor pages. Mobile: hamburger toggle (`supplier-mobile-nav-toggle`) opens a slide-in drawer with the same nav.
- **Dashboard/Shipments/Documents restyled** (`SupplierDashboardPage.jsx`/`SupplierShipmentsPage.jsx`/`SupplierDocumentsPage.jsx`): all hex colors remapped to the new palette (`#1E40AF` primary, `#0F172A` text, `#E2E8F0` borders, `#F1F5F9` table headers, status badges per `design_guidelines.json`). Purely visual - all existing functional logic (PO cart/shipment creation, shipment edit/filters, document upload/versioning) untouched.
- **Dashboard KPI bento row added** (Active POs, Total Outstanding Qty, In-Transit Shipments, Open QC NCs) - fetches `/shipments` alongside `/purchase-orders` now; shows "-" while loading (fixed a testing_agent-flagged flash-of-0 cosmetic issue).
- **NEW `SupplierAuditsPage.jsx`** at `/supplier-portal/audits-qc/:vendorCode`: demo-data-disclosure banner (QMS integration is Phase 5, not built yet), QC metrics row, 2 tabs (Q-Notifications / Audit Non-Conformances) backed by deterministic per-vendor MOCK data (`/app/frontend/src/lib/qmsMockData.js` - frontend-only, no backend call, explicitly disclosed as placeholder).
- **CAP (Corrective Action Plan) submission - REAL, persisted**: "Submit CAP" per row opens a modal (Root Cause/Containment/Corrective Action/Target Date/optional Evidence file) -> `POST /api/supplier-portal/caps` (multipart) -> new Mongo collection `supplier_cap_submissions` (`create_cap_submission`/`list_cap_submissions` in `supplier_portal_service.py`, `_id` string uuid). `GET /api/supplier-portal/caps` lists a vendor's own submissions; row shows "CAP Submitted" once one exists for that `reference_id`, confirmed persists across reload.
- Mounted global `<Toaster/>` (sonner) in `App.js` for the first time in this app (component existed but was never rendered) - used for CAP submit success toast.
- Tested via `testing_agent` (iteration_147): 100% pass, no blocking bugs. Minor cosmetic KPI-flash issue fixed post-test. Test CAP rows cleaned up from Mongo after testing.

### P0
- **External QMS feed**: secured API/data feed for external QMS app to pull pending-QC items. Not started.


## What's been implemented (Sep 8, 2026 session - BOM Explorer speed fix)
- **Root cause of "why is SAP BOM pull so slow"**: `/bom/search` bypassed the existing Mongo `bom_node_cache` entirely and always did a live multi-level SAP SOAP explosion (`sap_soap_client.explode_bom`), unlike Purchasing Plan/MRP/L1L2 which already read from that cache. Compounded by SAP's own slow/flaky SOAP service, a hard cap of 3 concurrent SAP sessions app-wide (`sap_rate_limiter`), and inherently sequential BFS levels (can't parallelize across tree depth).
- **Cache-first serving**: `GET /api/bom/search` now checks `bom_cache_service.is_cached()` first - if the root was ever resolved before, serves from cache near-instantly (~0.2-0.3s) via `build_tree_from_cache`, with a `source: "cache"` + `cached_as_of` timestamp. A never-seen root returns `needs_live_fetch: true` instead of blocking.
- **Background live-fetch job**: new `POST /api/bom/search/live` (body `{bom_id, force_refresh}`) + `GET /api/bom/search/live/{job_id}` (polled, via existing `job_store.py` pattern) for the two cases that genuinely need a live SAP walk - a brand-new part, or an explicit "Refresh from SAP". `build_tree_from_cache` gained `force_refresh` (bypasses cache for every node in this walk, still persists fresh results back to cache) and `progress_cb(level, lookups_done, max_lookups)` (called once per BFS level that hits SAP live).
- **Frontend** (`BomExplorerPage.js`): "Cached as of [time]" badge + "Refresh from SAP" button next to the "Resolved: ..." badge; a live progress bar ("Pulling live from SAP - exploring level X of up to 6... / N of ~300 components resolved") replaces the blank skeleton whenever a live fetch is actually happening; old tree stays visible underneath during a manual refresh (stale-while-revalidate UX).
- Verified end-to-end: backend curl-tested (cache hit 0.24s; live job progressed 4 real SAP levels/36 lookups over ~40s; force_refresh updated the cache timestamp), then `testing_agent` (iteration_148): 100% pass, no regressions to AI Categorize/Standard Costs/Drawings/Export/sort/filter, no mobile overflow.

### P1
- **Thread Pool Exhaustion**: `asyncio.to_thread` on blocking SAP calls exhausts the default executor, causing ~60s stalls under load. Move to a dedicated `ThreadPoolExecutor`. Not started (explicitly deferred by user).
- **SAP Custom BO / ABSL Web Service Authorization**: blocked on SAP Admin fixing Work Center View binding for `CombineView` in SAP UI Business Roles.
- **Supplier GRN Live Test**: blocked on SAP production tenant (`my431827...`) responsiveness.
- **PO 28792 "Notification Quantity" SAP save error** (deferred by user this session, "later"): SAP rejects the GRN save for shipments Q7YA7Z/39GA52/WVHKWT/LFQP9Z with "Actual quantity or notification item ID or notification quantity missing" - a separate "Notification Quantity" grid column may need populating, not yet investigated.

## What's been implemented (Sep 9, 2026 session - PO Creation: secondary/alternate UOM lookup)
- **User's ask**: "fetch secondary unit of the item while creating PO" - e.g. `6550-002047` has 1 Packet (XPA) = 100 EA maintained in SAP's Material master "Quantity Conversions" grid; confirmed live this data is readable via the same SOAP service already used for BOM Explorer drawings/comments.
- **On-demand, per user's explicit choice** (not automatic on product pick): a small icon button next to the UOM field on each PO line (`po-line-fetch-uom-button-{idx}`) - click it to call SAP live and turn the plain UOM text box into a dropdown offering the base unit + any alternate unit(s)/conversion factors SAP has configured; whichever is selected is sent to SAP as-is on the PO line (SAP itself handles the qty math, no client-side conversion).
- **Backend**: `sap_material_client.get_uom_info()` (parses `BaseMeasureUnitCode` + all `QuantityConversion` blocks from the Material master SOAP response) + `material_uom_service.py` (new Mongo cache `material_uom_cache`, keyed by product_id, effectively permanent since this is static master data) + `GET /api/purchase-orders/products/{product_id}/uom-options`.
- **Frontend**: `PurchaseOrderPage.jsx` line state gained `uomOptions`/`uomOptionsLoading`; picking a NEW product for a line resets `uomOptions` to null so a stale dropdown from a previous product doesn't linger.
- Verified live end-to-end: backend curl (1.48s live SAP fetch -> 0.24s cached), then `testing_agent` (iteration_149): 100% pass, no bugs. Testing agent explicitly did NOT click "Create Purchase Order in SAP" (creates a real permanent SAP document, per longstanding caution in this app - see PO Creation history above).
- Also filed a detailed external bug report (this session, not a codebase fix) for the user to send to whoever manages the separate Radish QMS app: ALL drawing file downloads there 404 with "Drawing file not found in storage" (8/8 parts tested) despite healthy metadata - a QMS-side storage issue, outside this codebase.

## What's been implemented (Sep 9, 2026 session - "ERP Synced" status badge)
- **User's ask**: an "ERP Synced"/"ERP Disconnected" badge matching the existing "SAP PRD Connected" badge, visible for all users on every page.
- **Backend**: `erp_portal_client.check_connection()` (opens+closes a real MS SQL connection to primary host, falls back to secondary, raises only if both are unreachable) + `GET /api/erp/connection-status` (same `ConnectionStatus` model + anti-flicker retry-once pattern as the existing `/api/bom/connection-status`).
- **Frontend**: new `ErpConnectionStatus.jsx` (mirrors `SapConnectionStatus.jsx` exactly - 60s poll, colored dot + label, dot-only on mobile) added to all 22 page files that already show the SAP badge, right next to it.

## What's been implemented (Sep 9, 2026 session - GRN screen: show PO Price)
- **User's ask**: "in GRN approval screen, show PO price also to the GRN person."
- **Backend**: `supplier_shipment_service.attach_po_pricing()` joins each shipment item with the existing PO cache (`unit_price`/`currency` already captured from SAP's NetUnitPrice on every PO pull, just never surfaced before) - wired into `GET /api/admin/grn/lookup/{doc_code}`.
- **Frontend**: `GrnApprovalPage.jsx` GRN item table gained two new read-only columns - "PO Price" and "Line Value" (= PO Price x whatever Actual Qty is currently entered/typed, updates live as the GRN person adjusts it).

## What's been implemented (Sep 9, 2026 session - Supplier Dashboard: In Transit / Received / Open Qty split + over-ship fix)
- **User's ask**: split the Supplier Dashboard's single "Open Qty" column into "In Transit Quantity" (editable while unreceived), "Received Quantity" (firm once GRN-approved), and "Open Qty" adjusting accordingly.
- **Real bug found & fixed during this ask**: `_resolve_items()` (shipment create/edit validation) used to check the new ship_qty against SAP's own cached Open PO Qty ALONE whenever available, completely ignoring the vendor's own already-in-transit (not yet SAP-received) shipments for the same item - letting two overlapping in-transit shipments together exceed a PO's true open quantity. Fixed via new shared `_compute_qty_state()`/`_qty_breakdown_by_status()` in `supplier_shipment_service.py`, used by BOTH the dashboard display (`get_cached_pos_with_remaining`) and the create/edit validation (`_resolve_items`) so they can never disagree again.
- **Frontend**: `SupplierDashboardPage.jsx` PO table now shows In Transit Qty (clickable when >0, deep-links to `/supplier-portal/shipments/{vendorCode}?po={po_number}`) / Received Qty / Open Qty as 3 separate columns. `SupplierShipmentsPage.jsx` reads that `?po=` query param to pre-fill its existing search box.
- Verified: backend curl (boundary-exact ship succeeded, 1-unit-over correctly rejected with "cannot ship X - only Y still open") + `testing_agent` (iteration_151): 100% pass, 6/6 scenarios (columns/math, link behavior, over-ship reject on both create AND edit, normal-ship regression, KPI regression, mobile). Test shipment (WDDSVY) cleaned up from real vendor data afterward.
- Minor testing-agent code-review notes addressed: added `supplier-po-open-qty-{po}-{item}` data-testid for consistency (not addressed: In Transit link uses a `<button>` not `<a href>`, low priority, no right-click/new-tab support).

- Verified live end-to-end with a real shipment (MU7DE2, PO 29086, HAMIDI EXPORTS): screenshot confirms INR 1.26/2.52 and INR 3.04/6.08 render correctly.
- **Separate, still open**: user asked earlier in this session whether there's a timeline for the Supplier Portal's "Open Order Qty" to update after a GRN approval - confirmed YES, a background job (`start_sap_open_qty_refresh_loop`) refreshes it from SAP's own report every ~5-6 minutes (verified healthy via logs). Still waiting on a specific PO/item number from the user if they want a deeper look at one that seems stuck beyond that window.

- Verified: backend curl (returns `Connected to ERP Portal (fallback host)` - primary MS SQL host currently down, fallback working as designed), then `testing_agent` (iteration_150): 100% pass across 5 spot-checked pages + mobile, no bugs. Code-review note: `ErpConnectionStatus`/`SapConnectionStatus` are ~95% duplicate code, intentionally mirrored for consistency - a shared `<ConnectionStatusBadge>` component would be a future cleanup, not done now (out of scope for this additive ask).


- `StockTransferPage.jsx` refactor (~1400 lines), STO stuck-order nav badge, "last confirmed in SAP" timestamp on stale PO rows, auto-refresh a site's stock after its own STO posts, bulk-import diff preview, missing-weight alert, auto-retry ERP sync.
- `GrnApprovalPage.jsx` is ~610 lines mixing lookup/approve/reject/discrepancy/retry/pending-table - approaching split threshold per code review (iteration_141).

### P2
- e-Way Bill / e-Invoice via Sandbox.co.in - blocked on user providing API credentials.
- Delivery Note Batch Print, Fuzzy Product Match (AI Quick Entry), Bulk Finish reporting points, Category Picker dropdown.

### P3
- Backfill old CompCode on legacy ERP rows, incorporate Quality/OTIF into AI Quota Suggestions.

- **Playwright-based "Open PO Qty" read - CANCELLED per user's explicit ask ("we cannot go with playwright for this")** (Sep 2 2026, this session): the background job (`start_sap_open_qty_refresh_loop`) was found live-hammering SAP's UI sequentially for every active PO (~30+ POs/cycle, 10-20s each) with a confirmed ~100% failure rate ("Element is not visible" clicking the already-selected "All Purchase Orders by Selection" view option, a real SAPUI5 idempotency bug) - and was directly implicated in the "0 of 13 components have a live SAP cost" report by starving concurrent SAP OData calls / getting killed mid-run by a hot-reload restart. Removed `fetch_open_po_quantities` from `sap_playwright_supplier_pgr_service.py` entirely (the GRN-posting Playwright flow in the same file is untouched, still needed/working). **Replaced same session** with a real fix the user found themselves: SAP's own Analytics OData report `RPSRMPO_B02_Q0004QueryResults` (`ana_businessanalytics_analytics.svc`) exposes `CPO_ID`/`CITM_ID`/`FCQUANTITY`(PO Qty)/`FCDLV_QUANTITY`(cumulative Delivery Qty) per PO line item - Open Qty = FCQUANTITY - FCDLV_QUANTITY, live-verified against PO 29086 (matches SAP's own screen exactly: 100 ordered, 7 delivered, 93 open). New `sap_po_analytics_client.py` (single fast batched OData GET, ~1s for 3 POs vs 10-20s/PO before, no browser automation) is a drop-in replacement - same return shape `supplier_shipment_service.store_sap_open_qty_cache` already expected, zero downstream changes needed. `server.py`'s startup loop now calls this (interval shortened 20min -> 5min since it's cheap). Already surfaced in `SupplierDashboardPage.jsx` ("SAP-verified Ns ago" / "not yet verified in SAP").
- **BOM Explorer "0 of N components have a live SAP cost" - REAL ROOT CAUSE FOUND + FIXED (Sep 2 2026, this session)**: same-day regression from the Sep 2 "Valuation Rate Mismatch" `site_id` fix above - `get_standard_costs()`'s `levels` list stayed as raw `(lvl, pe)` TUPLES (never flattened to plain lvl-string) whenever `site_pe_uuid` was falsy, which is EVERY non-site-aware caller (BOM Explorer `/bom/standard-costs`, Inventory, Purchasing Plan - exactly the 3 callers flagged as "not yet fixed" in that same changelog entry) - `price_map.get(level_uuid, [])` then always missed (price_map is keyed by plain UUID strings), silently returning `None` for every single product. Also affected the site-aware Stock Transfer path whenever a product had no valuation level at its own ship-from site (the "fall back to all sites" branch had the same un-flattened tuples bug). Fixed: always flatten to plain lvl-string lists in both the site-matched and fallback branches. Live-verified: 4 real BOM components that ALL returned `null` now return real Moving Average prices (13.62, 11.85, 11.62, 16.41 INR) via the actual `/api/bom/standard-costs` endpoint.

- **By-product posted but main confirmation lost / WIP never fires - REAL ROOT CAUSE FOUND + FIXED (Sep 3 2026, live incident Lot 71541)**: user asked "why is WIP not posted" then "then why does system post scrap" for Lot 71541 (SAP: Started, main output 100002694-A 0/500 confirmed, but CR-SCRAP by-product shows 33/46 kg confirmed - live-verified via `find_lot_by_id`, and a real SAP Confirmation Overview screenshot (doc 271183, Created By `_EMERGENTBOM`) proving the by-product-only SOAP call posted). Root cause: `_confirm_production_inner` posts the by-product (`confirm_material_output`/`create_material_output`) as an isolated SOAP call BEFORE the main `confirm_reporting_point` call (SAP requires isolation - can't combine MaterialOutput + ReportingPoint nodes). If the by-product call succeeds but the main call then raises `SAPProductionLotError`/`SAPProductionLotAuthError`, `server.py` used to `return` immediately (job status "failed") WITHOUT ever calling `log_confirmation` - silently losing the fact the by-product had already, irreversibly, posted to SAP. Same class of bug as the Sep 2 `finish_task()` fix, one step earlier in the pipeline - not covered by that fix. Fixed: the exception is now folded into a normal (still logged) failure `result` dict (`success: False`, byproduct_confirmation preserved) that flows through the same clarify-error + `log_confirmation` + job-"done" path a normal SAP business rejection already uses - WIP Clearing still correctly stays skipped (requires `result.success` True). Frontend (`ProductionConfirmationPage.js`) now shows a `toast.warning` calling out "by-product posted, main confirmation failed, won't be re-posted" whenever this exact shape occurs, and the existing `LastConfirmationBadges` chip row (Posted=red, By-product=green) already renders it correctly once history has the row. Verified via a monkeypatched dry-run (`sap_production_lot_client.confirm_reporting_point` forced to raise, `confirm_material_output` forced to succeed): job now ends `status: "done"`/`success: False` with `byproduct_confirmation.success: True` preserved and a history row written (previously: `status: "failed"`, zero history rows). NOT live-retested against Lot 71541 itself (would post real data to production SAP) - next real confirmation attempt on any lot hitting this exact fault pattern should now log correctly.

- **Two Internal Stock Transfer fixes (Sep 3 2026, user's explicit ask)**:
  1. **Inbound STO Receipt had no separate permission**: `/api/inbound-receipts/*` (and its nav tab/route) was open to ANY logged-in user (`anyUser`/no `page` key at all, an Aug 28 2026 "to start with" decision) - now its own grantable right, `inbound_stock_transfer`, in `auth_service.py`'s `PAGE_CATALOG`/`PAGE_ROUTE_RULES`, `NavTabs.jsx`, and `App.js`'s route. Grant it via Access Management like any other page.
  2. ~~REAL REGRESSION FOUND + FIXED: "user" role could see every plant's Stock Transfer Orders~~ - **REVERTED same session (user's explicit ask: "Show all stock transfer main page to all the user")**: the id-OR-name self-created filter above was removed again - `GET /stock-transfer/orders` now returns every order to every `stock_transfer` user, same as before this whole detour. The `created_by_user_id` field stays on STO docs (harmless, no filtering logic reads it anymore). User also asked to confirm un-activated destination-plant items still show an Activate action - already true and unaffected by this revert: `GET /admin/notifications` (the "Action Needed" panel) has never been filtered by creator (`list_open_admin_notifications` returns every open notification), and was already opened to any `stock_transfer` user earlier the same day. Live-verified: a "user" role account with zero prior orders now sees all 65 STOs, and can hit `/admin/notifications` (200 OK).
  - **Follow-up fix (same session, user reported "not visible" on Access Management)**: the new `inbound_stock_transfer` checkbox never appeared on the IT Access Management screen - `AccessManagementPage.jsx` reads its checkbox list from `AuthContext.jsx`'s own hardcoded `PAGE_LABELS` map (a separate frontend mirror of `auth_service.py`'s `PAGE_CATALOG`, NOT fetched live from the backend), which I forgot to update. Added `inbound_stock_transfer: "Inbound STO Receipt"` there too - live-verified via a synthetic super_admin session, checkbox now renders correctly for every user row.

- **P1W/P2W ERP Serial Number Mismatch - REAL ROOT CAUSE FOUND + FIXED (Sep 3 2026, user's explicit ask, "just check for now" then "yes do it")**: user reported "SAP posts as-is but ERP converts P2W to W2 and P1W to W1... serial number mismatch". Confirmed live against the ERP's own cached `comp` table: it has NO `Ccode` row "P1W"/"P2W" at all - only `Ccode="W1"` (pcode="P1W") and `Ccode="W2"` (pcode="P2W"). Every other site's SAP Site ID already equals its ERP `Ccode` (P1...P9, AV) - only these two diverge. `sync_to_erp_portal` was sending the raw SAP site ID as `CompCode` straight to `Pro_DeliveryChallan_Insert`, landing new Delivery Challans under an unregistered "P1W"/"P2W" CompCode disconnected from the ERP's real "W1"/"W2" Sale_No sequence (found one live instance: STO-000074, Ship-from P2W, Sale_No 38316/Sale_Noc 2257, filed under "P2W" instead of "W2"). Fixed with `_erp_comp_code_for_ship_from()` (P1W->W1, P2W->W2, every other site untouched) applied only to `sync_to_erp_portal`'s `comp_code` going forward - user's explicit ask NOT to touch past synced orders. The resolved code is persisted as a new `erp_comp_code` field on the STO doc at sync time; `get_delivery_note_data`'s printed Serial Number now prefers `erp_comp_code` (falling back to the old raw-site-ID behavior for any order that doesn't have it, i.e. everything synced before this fix). Verified via dry-run unit tests: `_erp_comp_code_for_ship_from` maps correctly, and a simulated pre-fix order still prints `P2W-2257-2627` unchanged while a simulated post-fix order prints `W2-9999-2627`. Did NOT backfill/touch STO-000074 or any other already-synced order, per the user's instruction.

- **ERP "Mark" field fixed to use app's own Remark, not Place of Supply (Sep 3 2026, user's explicit ask)**: user spotted the ERP Delivery Challan screen showing Place of Supply ("Noida") in the `Mark` field and asked why. Traced it to `sync_to_erp_portal`'s `header["marks"] = doc.get("place_of_supply")`, with no documented rationale from the original build (unlike every other field mapping there, which has an explicit comment) - likely just a leftover guess. Changed to `doc.get("remark")` (this app's own Remark field on the STO form, added Aug 27 2026) per the user's explicit instruction. Only affects future syncs - past orders' already-written Mark values are untouched (same as the P1W/P2W CompCode fix above, no retroactive edits to already-synced ERP rows).

- **BOM Explorer "Latest Drawing" column - QMS integration (Sep 3 2026, user's explicit ask)**: new column at the very end of the BOM tree table, sourced from the sister Emergent app's External Drawings API. Server-side proxy (`qms_drawings_client.py` + `/api/bom/qms-drawing/{part_no}` and `/history`, gated under the existing `bom_explorer` permission) keeps the API key off the browser, per that API's own design principle. Each row gets a "View Drawing" button opening a Dialog (current revision + metadata + "View latest drawing" link + "See previous versions" -> full history list) - on-demand fetch per click, no bulk prefetch. Distinct from the existing SAP-attachment `drawingUrls` pill next to Product ID (cached, unrelated feature).
  - **Switched preview -> production (same session, user's explicit ask "switch to production for both cases")**: `QMS_DRAWINGS_BASE_URL`/`QMS_DRAWINGS_API_KEY` now point to `https://qms.radishtechnologies.com` with the production key, regardless of whether THIS app is accessed via its own preview or production URL (was previously hardcoded to QMS's preview URL). Initially got a real `401 Invalid X-API-Key` (confirmed via direct curl to QMS prod, not just our proxy) because the QMS admin hadn't yet added the key to that backend's `EXTERNAL_API_KEYS` env var/redeployed - resolved on the QMS side, then live-verified end-to-end (200 OK, real drawing payload) both directly against QMS prod and through our own `/api/bom/qms-drawing/{part_no}` proxy.

  - **"Drawing not available" cell fix (same session, user's explicit ask)**: added `GET /bom/qms-drawing-exists` (checks every visible part in parallel server-side, right after a BOM pull - QMS has no bulk endpoint) so the cell shows plain "Drawing not available" text for confirmed-absent parts instead of an always-clickable button that only revealed "not found" after a click. A part missing from the results map (QMS error/unreachable, distinct from a confirmed 404) safely falls back to the clickable button rather than a false "not available". Live-verified against a real BOM (100010102-A, 10 rows) - all showed real QMS-production drawings.

- **Supplier Invite page rebuilt (Sep 3 2026, user's explicit ask)**: added a "Search SAP Vendor" combobox (Popover + Command, searches the already-synced `suppliers` collection ~2984 vendors, same source `SupplierMasterPage` uses) - selecting a match autofills Company Name + Vendor Code (and the supplier's own on-file email, if any, seeds the first email chip). Also replaced the single email field with a multi-email chip input (add via Enter/comma, removable) - submit loops and sends one invite per recipient via the existing `POST /admin/supplier-portal/invites` (no backend schema change needed, no uniqueness constraint on vendor_code). Had to add `supplier_portal_admin` to `/api/suppliers`'s allowed pages in `auth_service.py` (was `supplier_master`/`quota_allocation` only) so this page's own role can actually load the vendor list. Live-verified: search "165" surfaces "165 DEGREE" (code 12423) + 7 other matches, selecting it autofills both fields correctly, two emails added as chips, button correctly reads "Send Invites (2)".

  - **Vendor-switch email bug fixed (same session, user report)**: `selectVendor` only ever ADDED the new vendor's on-file email when the chip list was empty, so switching to a second vendor left the first vendor's auto-filled email stuck in the list. Now tracks which email (if any) was auto-filled from the currently-selected vendor (`autoFilledEmail`) and swaps exactly that one out on every new vendor selection, leaving any manually-typed emails untouched. Live-verified: select Vendor A (email seeded) -> add a manual email -> select Vendor B -> chips correctly become [manual email, Vendor B's email], Vendor A's email gone.

- **Store Approval "Print / Save as PDF" slip (Sep 3 2026, user's explicit ask)**: request detail page (`/storeapproval/request/:requestId`) now has a single "Print / Save as PDF" button (same for requester and store issuer, content just reflects the request's current data) using browser `window.print()` - no backend change, matches the existing `GatePassPage.js` pattern. Prints a "Material Requisition Slip": Request ID, Proposal/Lot Ref (`production_proposal_id` - no separate Lot ID exists at this pre-production stage), Site, Requester, Date & Time, Component/Required Qty/Issued Qty table, and Requester Sign/Store Sign signature lines. Rest of the page (header, nav, action buttons) is hidden via `print:hidden` when printing.
  - **Follow-up fix (same session, user screenshot)**: browser-injected print header (date/page title) + footer (URL/page number) were showing on the printed slip, and the store issuer had no visibility into Issued Qty against Required Qty. Fixed `@page` CSS to `margin: 0` (the standard Chromium trick that suppresses its own header/footer injection - content re-padded via the slip's own `p-10`) and added an "Issued Qty" column (from `c.issued_qty`, shows "\u2014" until issued) to the print table. Verified live: print media emulation confirms the slip has no browser chrome and shows Issued Qty per component.
  - **Follow-up fix #2 (same session, user report "print button not visible" for a request they created as requester, e.g. P2-000121)**: root cause was that a "requester" (someone who triggered a Production Order that went short on RM) never actually lands on `StoreApprovalPage.js`'s detail page at all - their real view is `MyStockRequestsTab.jsx` (the "My Stock Requests" tab on Production Confirmation), a completely separate component/table with no row-click or print action, and no dependency on the `store_approval` page permission (many requesters only have `production_confirmation`). Extracted the print slip into a shared `/app/frontend/src/components/RequestPrintSlip.jsx` and added a per-row "Print" button to `MyStockRequestsTab.jsx`'s Request Level table (`printTarget` state + `window.print()` on click, cleared via the `afterprint` event) - reuses the already-fetched `/store-requests/journal?requester=` data, so it needs zero extra API calls and never hits the site-bound `GET /store-requests/{id}` check (which would 403 a non-admin requester not bound to that site). Live-verified with a synthetic non-admin `production_confirmation`-only session as requester "Ankit Sharma": P2-000121 shows a working Print button, clicking it calls `window.print()` and renders the correct slip data.
  - **Follow-up fix #3 (same session, user provided a "Request For Store" reference screenshot)**: rebuilt `RequestPrintSlip.jsx`'s layout to match that reference - right-aligned "Request For Store"/"Page 1 of 1" title, left column (Store ID = our Request ID, Requester, Department = Site ID as a stand-in per user's explicit choice - no real department field exists) + right column (Production Order ID = `production_proposal_id` per user's explicit choice, Site, Date & Time), and split the combined "Component" cell into separate Serial No/Product ID/Description columns (Required Qty + Issued Qty kept, per user's explicit choice not to rename/drop either). "Issue For" from the reference intentionally NOT printed (user's explicit choice). Requester Sign/Store Sign lines kept (user's explicit choice, differs from the reference which has none). Live-verified via screenshot - matches the reference layout.
  - **Follow-up fix #4 (same session, user report "still not changed" with a real print-preview screenshot)**: the new layout WAS correct, but printing from `MyStockRequestsTab.jsx` (Production Confirmation's "My Stock Requests" tab) also printed the entire rest of the app below the slip (nav tabs, request table) since that page's root container had never been marked `print:hidden`. Fixed `ProductionConfirmationPage.js`/`ProductionConfirmationTestPage.js` root `<div>` to `print:hidden`. That alone caused a NEW bug (blank print output) - the slip is rendered via `createPortal` into `document.body` (needed so it isn't nested inside the now-hidden page root), but `App.js`'s own outer `<div className="min-h-screen flex flex-col">` wrapper (wraps every internal route) still occupies a full-viewport `min-height` even with all its children hidden, pushing the portaled slip down below the visible viewport. Fixed by making the slip `print:fixed print:top-0 print:left-0 print:w-full print:bg-white` so it always anchors to the actual page top regardless of how much empty space precedes it in the DOM - works for both the portaled (`MyStockRequestsTab.jsx`) and non-portaled (`StoreApprovalPage.js`) usages. Live-verified via print-media screenshots on both: slip renders at the true top of the printed page with nothing else visible.

  - **Heading rename (same session, user confirmed print now works via screenshot)**: title changed from "Request For Store" to "Goods Issue From Store" per user's explicit ask.
  - **Follow-up fix #5 (same session, user clarified "change name only for the Store Issue print, not Production Confirmation")**: heading change above was wrongly applied to BOTH usages via the shared component's hardcoded text. `RequestPrintSlip.jsx` now takes a `heading` prop (default "Goods Issue From Store") - `StoreApprovalPage.js` (store issuer) uses the default, `MyStockRequestsTab.jsx` (requester) passes `heading="Request For Store"` explicitly. Live-verified both print headings independently via screenshot.

- **Stock Transfer valuation rate: once-per-day SAP fetch cache (Sep 4 2026, user's explicit ask)**: root-caused a "Total Amount differs between our print and the legacy ERP portal's own screen" report - both derive from the SAME frozen `items[].rate` written once at `sync_to_erp_portal` time, so the drift is a later SAP valuation change (e.g. a Cost Estimate Run) after that STO synced, not a bug in either document. Per user's explicit ask ("fetch rate at once in a day... if I again create x code STO then do not need to fetch it, just print today's stored price"), added `_get_daily_cached_costs(db, sap_valuation_client, product_uuids, site_id)` in `stock_transfer_service.py` - caches SAP's live Moving Average price in a new `daily_valuation_cache` collection keyed by `{site_id}:{product_uuid}:{YYYY-MM-DD}`; a fresh SAP call only happens for a product/site with no cache entry for today. Wired into both `_price_hsn_for_note` (GST note text) and `sync_to_erp_portal` (the ERP push + frozen `items[].rate`). A genuinely-missing price (`None`) is never cached, so it retries on the next call instead of getting stuck for the day. Verified with a stubbed SAP client: 2 calls for the same product/site same-day only hit SAP once.

- **Purchase Order Creation: fully fixed and CONFIRMED LIVE (Sep 4 2026)**: user reported PO creation failing with a generic SAP "Web service processing error". Root-caused and fixed 3 real bugs in `sap_po_write_client.py` (see its module docstring for full detail): (1) wrong SOAP namespace (`A1S/Global` -> `SAPGlobal20/Global`, same class of bug already found once for the sibling GSA write client), (2) payload/party structure didn't match SAP's officially published Maintain examples (removed an invalid `PartyTypeCode` nested in `PartyKey`, added required header `Company`/`ShipToLocation`, `ObjectNodeSenderTechnicalID`/`ObjectNodePartyTechnicalID`, Item follow-up indicators, switched `NetUnitPrice`->`ListUnitPrice`), (3) missing `EmployeeResponsibleParty` (SAP requires a "Purchaser" employee on every PO) - confirmed as the real last blocker by testing with an intentionally-wrong PartyID, which turned the generic crash into a real readable fault ("Employee responsible missing"). User confirmed live in SAP's own UI that logging in as `ItAdmin` auto-fills "Buyer Responsible: 1 - Admin Ramp" - PartyID `"1"` is now hardcoded as `PO_EMPLOYEE_RESPONSIBLE_ID` in `server.py` (fixed system responsible party for every Emergent-created PO; no per-creator SAP Employee mapping exists in this app). Also fixed response parsing (`<BusinessTransactionDocumentID>`/`<UUID>`, not the guessed `<PurchaseOrderID>`/`<PurchaseOrderUUID>`). **Live-verified twice** through the real `/api/purchase-orders/create` endpoint: PO 29173 and PO 29174 both created successfully in the production SAP tenant (supplier RAD-P2-S, product INGOTCAST) with correctly-populated `purchase_order_creation_history` records. Ruled out authorization/Communication Arrangement gaps along the way (all 4 operations confirmed Released; even full-admin `ItAdmin` credentials hit the identical generic error before the real fix, proving it was payload-structural, not access-rights).

- **Purchase Order Creation: PO Date/Business Residence/Purchase Unit root-caused, write-side BLOCKED on SAP config (Sep 4 2026, this session)**: user reported these 3 fields (+ "Portal PR Number") still blank on real created POs (29173/29174). Read back 6 real SAP POs (both app-created and manually-created-in-SAP-UI reference examples the user made, e.g. PO 29179/29176-29180) via the existing read-only `sap_po_client.py` service - found the real fields: `BusinesResidence`/`PoDate` are this tenant's own one-off custom fields (namespace `http://0012819041-one-off.sap.com/YPS7GEURY_`, confirmed exact spelling/values), and `PartyResponsiblePurchasingUnitPartyKey` ("Purchase Unit", e.g. "P1-PUR") auto-derives once BusinesResidence is set (confirmed on PO 29176). Added both custom fields to `sap_po_write_client.py`'s envelope with the exact confirmed tag names/namespace, then created a real live test PO (29181) to verify - **result: SAP silently accepted the create but ALL 3 fields came back blank on read-back, no error at all**. Root cause is almost certainly that these custom fields are enabled for reading via web service but NOT enabled for writing via web service on the Purchase Order Maintain interface - a SAP-side Business Configuration/Extensibility setting, not fixable in this app's code. User will check with their SAP admin/consultant whether these fields have "Web Service" write access enabled; agreed to NOT attempt further blind live-write variants (each attempt creates a real permanent PO) until that's confirmed. "Portal PR Number" remains completely unresolved - it doesn't appear as a custom field on ANY of the 6 real POs read back, filled or not; its real SAP tag name is still unknown. **BLOCKED on SAP admin/user action.**
  - **FOLLOW-UP Sep 4/5 2026 (SAME session, continued after user checked SAP Adaptation Mode) - 2 of 4 fields now CONFIRMED FIXED live**: user found via SAP's own "Extension Field > Services" tab that PO Date and Portal PR Number are actually NEWER key-user fields under a DIFFERENT namespace (`http://sap.com/xi/AP/CustomerExtension/BYD/A4CF6`, element names `PODate`/`PortalPRNumber`) explicitly enabled for write on `PurchaseOrderBundleMaintainRequest_sync` - the OLD "one-off" namespace fields were a dead end as suspected above. Used SAP's safe **Check** operation (`PurchaseOrderBundleCheckMaintainRequest_sync`, same endpoint, zero side effects, no PO created) to isolate the exact bug without creating more real POs: the new custom fields fault the whole call when placed AFTER `<Item>` (trailing) - they must go right after `<Date>` (before the header parties), and `PODate`'s value must be a **plain date** (`YYYY-MM-DD`), not a full ISO datetime - a datetime value there causes a full SOAP fault, not a silent drop. Fixed and verified live via PO 29186 (read back via `sap_po_client.py` shows `PoDate=2026-09-05T00:00:00Z` populated) and confirmed directly in SAP UI screenshot: **PO Date and Portal PR Number both show correctly now.** `Date` (standard header field, right after `CurrencyCode`) and `PurchasingUnitParty` (standard header party, PartyTypeCode 410) were also confirmed as real fields via SAP's own official docs (help.sap.com PSM_ISI_R_II_SRM_PO_MBO) - `Date` is now sent; `PurchasingUnitParty` still isn't sent explicitly (relies on auto-derivation once Business Residence lands, unconfirmed either way since Business Residence itself is still broken).
  - **Business Residence + Purchase Unit STILL BLOCKED after 2 more live attempts**: user confirmed via SAP UI (screenshot) that "Business Residence" is a genuine STANDARD field (not a key-user/custom extension - absent from the Adaptation Mode field list entirely), so its real technical element name is unknown (Ctrl+F1 Technical Help in SAP UI did not surface it either). Tried (a) reusing the OLD one-off `BusinesResidence` custom field repositioned to right-after-`<Date>` (PO 29190, still blank in SAP UI) and (b) `BusinessResidenceID` as a plain header element (PO 29186, still blank) - both structurally valid per SAP's Check operation (no fault) but neither actually persisted, proving yet again that a successful/no-fault SOAP response does NOT mean a field is correctly bound (SAP's schema has a lax trailing extension point that silently accepts and drops any well-formed-but-unrecognized element). Public SAP docs only document `BusinessResidenceID` for Material/ServiceProduct valuation, never for PurchaseOrder specifically - genuinely undocumented for this object. User is now asking their SAP admin/partner directly for the real field mapping. **User emphasized this is a hard blocker: "Without business residence we can't create a proper purchase order in SAP."** Real, verified-live PO numbers created during this whole investigation (all in SAP, un-deletable): 29181 (blank test), 29186 (PO Date + PR Number confirmed fixed), 29190 (Business Residence retry, still blank). **BLOCKED pending SAP admin/partner input on Business Residence's real technical field.**
- **Bill-To Company changed to per-site Finance Bill-To dropdown (Sep 4 2026, user's explicit ask)**: `PurchaseOrderPage.jsx`'s "Bill-To Company" field (was a plain RI/RT selector) is now a dropdown of per-site Finance/Billing codes (mirrors the existing "{site}-PUR" Purchasing Unit convention), scoped to whichever company the chosen Purchase Unit site belongs to: Company=RI -> `P1-FIN`/`P8-FIN`/`P1W-FIN`/`P5-FIN`; Company=RT -> `P2-FIN`/`P3-FIN`/`P2W-FIN`/`P7-FIN`/`P9-FIN`/`P4-FIN` (disabled + resets whenever Purchase Unit site changes). Backend `BILL_TO_OPTIONS_BY_COMPANY` in `server.py` validates `bill_to_company` server-side against the derived company's list (400 if mismatched) before ever reaching SAP. Tested via `testing_agent` (iteration_142, 100% pass, 9/9 backend + 8/8 frontend scenarios) - zero live SAP writes during testing. Note (non-blocking, flagged by testing_agent): `P5`/`P1W` aren't in `GET /purchase-orders/sites`' real site list today, so `P5-FIN`/`P1W-FIN` are currently unreachable dead options - harmless, matches user's exact literal spec.
- **"Created POs" viewer page added (Sep 4 2026, user's explicit ask, Task 1)**: new page `CreatedPurchaseOrdersPage.jsx` at `/purchasing-strategy/created-purchase-orders` (nav: "Created POs" under Purchasing Strategy, same `purchase_order` page permission), reads the pre-existing `GET /api/purchase-orders/history` endpoint (unchanged, already existed) - table of every PO created via this app (SAP PO #, Supplier, Site, Bill-To, PO Date, PR Number, Items count, Created By, Created At) with a per-row "eye" button opening a detail dialog (full header + line-items table). Also added a "View Created POs" button to the PO creation success dialog. Tested via `testing_agent` (iteration_142) - renders real history data (POs 29174/29175/29181), detail dialog works. Minor non-blocking suggestions from testing_agent (not yet done): add an explicit error state instead of silent empty-list on fetch failure; add pagination once volume grows beyond the hardcoded limit=100.

## What's been implemented (Sep 4 2026 session, continued - PO Creation UI rebuild)
- **Architecture note**: PO creation write path was migrated from SOAP (`sap_po_write_client.py`, the Business
  Residence/PO Date SOAP struggle documented above) to OData (`sap_po_odata_client.py`, POST-then-PATCH
  `BillToParty`/`BuyerParty`/Business Residence, mirroring a working C# reference the user provided) earlier
  in this same session before the fork. This supersedes the "Business Residence STILL BLOCKED" SOAP finding
  above - per handoff this is DONE via OData, but NOT re-verified with a fresh live PO in this continuation
  (no live PO was created this session - only PR-lookup reads and UI testing).
- **`PurchaseOrderPage.jsx` full redesign**: rewritten with 5 sections - PR Lookup (mandatory) -> Org Context
  (Purchase Unit/Company/Bill-To) -> Supplier & Terms -> Line Items Engine (per-line delivery date, duplicate
  row, live line totals) -> sticky Order Summary sidebar (checklist gates the submit button, Confirm dialog
  audit recap, success dialog links to Created POs page). Header switched to match the app's standard teal
  `h-16 bg-[#0E7C86]` Materials Hub bar (was a plain white one from an initial redesign pass) - user's explicit
  ask "must also show the menu bar the same way as the app shows". Full color/spacing pass to the established
  JDE palette (`#004B87` primary/`#003A6A` hover, `#F2F4F7` page bg, `#D0D5DD` borders, `#EAECF0` table headers,
  `rounded-sm`, compact paddings) after user flagged the first pass used an unrelated blue/indigo "tactical"
  palette from a design_agent run that didn't match the rest of the app - also fixed a currency-formatting bug
  (`Intl.NumberFormat` always used `en-IN` lakh-grouping even for USD amounts) and an icon-button hover color.
- **PR Number is now MANDATORY and drives autofill** (user's explicit ask): new `backend/pr_integration_client.py`
  + `GET /api/purchase-orders/pr-lookup/{voc_no}` proxy the external SCM.AI "Smart Approvals" API (contract at
  `https://smart-approve.preview.emergentagent.com/api/public/docs/po-integration.md`, static `X-Api-Key` auth).
  Entering a PR voucher number and clicking "Fetch PR" auto-fills Vendor (matched against our own `suppliers`
  collection by `sap_internal_id`, including cached payment terms), Purchase Unit/Site, and Line Items (Qty/
  UoM/Unit Price from the PR; Product ID auto-matched against `inventory_cache` only if the PR's `icode` exists
  there, else the PR's item description is prefilled as a starting search text since most of this PR system's
  line items don't carry a code that matches our catalog - user must then pick a real Product ID from
  suggestions). Delivery Date is explicitly NOT part of a PR - stays independently user-entered per line.
  `POST /api/purchase-orders/create` re-validates the PR server-side right before writing to SAP (blocks if
  it's already been used/status changed) and, after a successful SAP create, best-effort stamps the new PO
  number back onto that PR via the same external API (failure here never fails the PO creation response - the
  SAP PO is already real/permanent by that point, just logged as a warning).
- Tested via `testing_agent` (iteration_144, 100% pass, zero live SAP writes, zero real PR stamp-backs -
  `po-confirm-submit-button` was never clicked in any test). Two follow-up rounds of self-tested visual fixes
  (screenshot-verified, no logic change) addressed direct user feedback: JDE theme/compact-layout pass, INR/USD
  currency grouping fix, hover-color fix, removed "(SCM.AI Voucher No.)" label text.

## What's been implemented (Sep 4 2026 session, continued after fork)
- **STO-000074 stuck Goods Issue - ROOT CAUSE FOUND + RESOLVED LIVE**: multi-line order 30520 (FLAT-BK50/FLAT-BK21) hit the 20-min `not_found_timeout`. Root cause: the OData-level Outbound Delivery Request WAS created by SAP (its display ID 59027 was already cached as `gi_delivery_request_id`), but the SEPARATE "combine into one Delivery Proposal" Playwright step (`_try_post_goods_issue_multiline`, filters SAP's Delivery Proposals screen by Reference ID) kept seeing 0 matching rows for the full 20 minutes - a genuine SAP-side generation delay, not a data bug. The old timeout message ("SAP hasn't produced the Outbound Delivery Request") was factually wrong in this case since the Request demonstrably existed. Fixed `mark_goods_issue_timed_out()` (`stock_transfer_service.py`) + its call site (`server.py`'s `_run_goods_issue_job`) to detect this exact case (`gi_delivery_request_id` already set) and show an accurate message instead ("Delivery Request exists, the combined Delivery Proposal just hasn't generated yet - SAP-side delay, retry"). User confirmed via UI check that no manual retry had happened yet (`gi_job_started_at` still the original timestamp) - then approved a live retry: `POST /stock-transfer/orders/STO-000074/retry-goods-issue` re-ran the same Playwright flow, this time found the Delivery Proposal, posted Goods Issue successfully. **STO-000074 is now `gi_status: "posted"`, Delivery ID `P2W1-262`.**
- **PO Split Delivery Schedule - UX redesign (user's explicit ask "better method and text for clarity")**: researched SAP ByDesign's `ManagePurchaseOrderIn` docs first - confirmed native multi-schedule-line-per-item is NOT supported by any ByDesign web service (only S/4HANA's OData has `A_PurchaseOrderScheduleLine`); the app's existing design (N separate PO line items sharing a PR line number) is the only viable approach and has no functional/financial downside, only a reporting-granularity difference. Redesigned `PurchaseOrderPage.jsx` per a `design_agent` blueprint (`design_guidelines.json`): the old unlabeled "Duplicate row" icon is now an explicit "Split" button (Calendar+Plus icon, tooltip explains it splits into a delivery schedule) for PR-sourced lines; split rows are visually grouped with a light-blue tint + 3px blue left border + a group header row ("PR Line #X - N Scheduled Deliveries" + allocation badge); the old plain "X/Y split" text became a color-coded progress bar + status badge (Amber under-allocated / Green fully scheduled / Red over-allocated); added an always-visible one-line guidance banner above the table. `testing_agent` (iteration_145, 100% pass) found 2 minor issues, both fixed same session: banner copy said "Split Schedule" but the button says "Split" (aligned wording), and a freshly-split blank-qty row showed a misleading Green "fully scheduled" (sum coincidentally matched since the new row was 0) - now forces Amber "enter a quantity" whenever any row in the group is still blank, regardless of the numeric sum. Also fixed a code-review nit: the allocation label hardcoded "EA" instead of the line's real unit of measure.
- **App-wide "hovers turn black" bug - ROOT CAUSE FOUND + FIXED**: `frontend/src/index.css`'s shadcn `--primary` CSS variable was never rebranded from its scaffold default (near-black, `0 0% 9%`) to the app's real JDE blue theme. Any Button/Badge/Checkbox/Switch/Slider/Tooltip using an unstyled default variant (no explicit className color override) fell back to `hover:bg-primary/90` (or plain `bg-primary` for checked/resting states) - near-black. Fixed by changing `--primary` to `206.7 100% 26.5%` (the exact HSL of `#004B87`) in the `:root` block only (`.dark` block untouched, unused app-wide). `testing_agent` (iteration_145) swept 6+ pages (Inventory, Inter-Plant Transfer, GRN Approval, Access Management, PO Creation, Supplier Master, BOM Explorer) - confirmed zero black hovers remain, and previously-black Checkbox checked-states now correctly render `#004B87`.

## Fully-Issued-Qty Request Wrongly Closed Despite SAP Rejection (Sep 8 2026, bug fix, COMPLETE)
- **User report**: P1-000014 (material P27175) showed "Failed - Negative stock not permitted" on its SAP goods movement, yet the request closed as fully resolved instead of staying open.
- **Root cause**: `run_issue_movements()` in `store_approval_service.py` decided "resolved" (closed) vs "resolved_balance_pending" (reopenable) purely from `shortfall = required_qty - issued_qty` (a math check on what the store *recorded* as issued), completely independent of whether the SAP goods movement itself (`goods_movement.ok`) actually succeeded. So a request where the store recorded the full quantity as issued closed as "resolved" even when SAP rejected the movement outright and 0 EA actually transferred.
- **Fix**: `shortfall_exists` now also becomes `True` if any component attempted a movement this round (`issued_this_round > 0`) and that movement did not succeed (`goods_movement.ok` is not `True`), regardless of the math-only shortfall. Verified with 2 simulated scenarios: SAP-rejected movement → now correctly stays `resolved_balance_pending`; SAP-accepted movement → still correctly closes as `resolved` (no regression).
- Confirmed root cause of the underlying P1-000014 SAP rejection itself is a genuine stock shortage (live SAP check: only ~13-63 EA of P27175 usable in P1-RM vs 5,120 EA the store recorded issuing) - not an app bug, that part needs the P1 store team to reconcile physical vs SAP stock.
- Not yet applied to the deprecated `submit_issue()` (kept only for legacy tests, not used by the live endpoint).

## Outbound Delivery No. Shown on STO List (Sep 8 2026, user's explicit ask, COMPLETE)
- When GI is posted, the SAP Outbound Delivery number (e.g. "P8D1-172") now shows as a green highlighted badge right under the SAP Order ID in the STO list table (`StockTransferPage.js`). Data was already stored (`outbound_delivery_ids` on the STO doc, already returned by `GET /api/stock-transfer/orders`) - frontend-only change. Verified live via screenshot against real STO-000015/018/022 records.

## Per-Item Live Stock Refresh, Inter-Plant Stock Transfer (Sep 8 2026, user's explicit ask, COMPLETE)
- **Problem**: the old "Refresh Site Stock" control required picking a Site first (site-wide bulk refresh of every item in the table, ~12s), so users had to wait/pick a site just to check one item's live SAP stock after a goods movement.
- **Fix**: removed the Site dropdown + button entirely (`StockTransferPage.js`). Added a small refresh icon (`ArrowsClockwise`) right next to each item's Product ID, enabled immediately after picking that item - no site needed. Scoped to just that 1 product across every site/warehouse.
- **Backend**: new `POST /api/stock-transfer/refresh-item-stock?product_id=X` (synchronous, `server.py`) + `refresh_stock_quantities_for_products()` in `inventory_service.py`, reusing the existing `_refresh_stock_quantities_scoped()` merge logic (extended with a `product_ids` scope option) and `SAPInventoryClient.get_inventory_detail(product_ids=[...])` (same method the BOM Component Stock Status panel's "Refresh Now" already uses).
- **Timing, live-verified**: the raw SAP OData call alone is ~2.3s for 1 product, but the full merge-into-cache step (which rewrites the whole `inventory_cache` doc's `items` array, same pre-existing pattern shared by the site/warehouse-scoped refreshers) adds overhead - **actual end-to-end time is ~11-17s**, not the initial ~2-3s estimate. Still meaningfully faster than the old site-wide refresh for a single item, and doesn't require picking a site first.
- Tested via a synthetic `super_admin` session (SSO auth, no email/password) + screenshot: icon shows next to Product ID as soon as item is added, spinner while refreshing, returns to normal with no errors.

## SAP Goods Movement Rejection - "Quantity conversion failed" for CRCOIL1.5X264.8 (Sep 8 2026, bug fix, COMPLETE)
- **User report**: Store Approval request P7-000013/P7-I000014 stuck in "Balance Pending" - SAP rejected the RM→SFG stock movement for material `CRCOIL1.5X264.8` with "Quantity conversion failed for material CRCOIL1.5X264.8".
- **Root cause, confirmed via live SAP BOM query**: the BOM line for this material on `100162759-1_4` (Wall Plate) has **2 competing ECOs** with close valid-from dates - `1001627596` (01-Apr-2026, `LifeCycleStatusCode=5`/**Released**, correct unit `MASS`/kg) and `1162759_7` (07-Apr-2026, `LifeCycleStatusCode=2`/**never released**, wrong unit `EA`). `sap_soap_client.py`'s ECO-selection logic (`_fetch_bom_by_output_product`) picked purely by `valid_from` date, so the later-dated but never-released draft ECO won, caching "EA" for a coil material whose real SAP base unit is `KGM` (weight) with no EA→KGM conversion factor - hence SAP's rejection.
- **Fix**: `sap_soap_client.py` now also extracts each state's `EngineeringChangeOrderEcoStatus/LifeCycleStatusCode` from the flat `ProductionBillOfMaterialVariantItemChangeState` list (same cross-reference pattern already used for `valid_from`, since neither field is present on the nested `ItemGroupItem` state blocks) and prioritizes `is_released` (status "5") ABOVE date/revision in the `state_key` tuple. Verified live: `_fetch_bom_by_output_product('100162759-1')` now correctly returns ECO `1001627596`/`MASS` instead of `1162759_7`/`EA`. No regression on the previously-fixed `P26675`/`CS14X48X1.2` case (still resolves correctly).
- **General fix, not material-specific** - protects against any other BOM line with a similar stray unreleased draft ECO. `bom_node_cache` self-heals via the existing periodic background refresh job (or `POST /api/bom-cache/refresh` for an immediate one) once this fix is deployed - `_upsert()` always overwrites cached `groups`/unit data on every refresh regardless of the `changed` flag.
- **Action needed from user**: deploy via "Save to GitHub" for this to take effect on the live app, then re-attempt the goods issue for the remaining balance on P7-000013 (should now post correctly in kg and close).

## Non-Zero Payment Terms Push (Sep 7 2026, follow-up to zero-day batch, COMPLETE)
- **Task**: extend the payment-terms push to the ~673 suppliers with a real day-count (1-90 days), which the zero-day batch above intentionally skipped.
- User provided a screenshot of the full SAP Terms of Payment fine-tuning table; mapped every distinct day-count to its code (all "from Invoice Date" basis, matching user's own example): `1→Z008 2→Z003 3→Z004 4→Z005 5→Z006 7→0002 8→Z009 10→Z001 14→0003 15→Z002 21→Z010 30→0010 45→0008 60→0012 75→0014 90→Z007`. Codes Z008/Z009/Z010 (1/8/21 days) didn't exist yet - user's SAP Admin added them before the push.
- **New script**: `/app/backend/update_payment_terms_nonzero.py`, reuses helpers from `update_suppliers.py` and `fetch_sap_supplier_info()` from `update_payment_terms.py`.
- **New failure class found**: 26 suppliers failed with "Procurement arrangement already exists for supplier" when using `actionCode="01"` (create) - the `has_purchasing_data` bulk-read detection had a false negative for these specific codes (root cause not fully isolated, likely the same duplicate-Supplier-block-in-response pattern seen earlier in this tenant for certain records). Fixed by retrying all 26 with `actionCode="02"` (update) instead, which succeeded immediately.
- **Final result, live-verified** (8/8 sampled across every day-bucket confirmed via direct SAP query): **657 updated**, **15 skipped** (Excel code not in SAP at all), **1 skipped** (blank/empty credit_day value, code P3620), **0 failed**. Combined with the zero-day batch, **all 3090 rows in `Payment Terms Supplier.xlsx` have now been processed.**

## One-time SAP Payment Terms Update (Sep 7 2026, user's explicit ask, COMPLETE)
- **Task**: push `CashDiscountTermsCode = "0001"` ("Due net payable immediately", confirmed by user via SAP Business Configuration > Terms of Payment fine-tuning) for every supplier in user's `Payment Terms Supplier.xlsx` (columns: Pcode, party, credit_day) where `credit_day == 0` (2417 of 3090 rows). Non-zero day rows (~670, e.g. 15/30/45/60/90 days) explicitly out of scope per user.
- **New script**: `/app/backend/update_payment_terms.py`, reuses `READ_ENDPOINT`/`WRITE_ENDPOINT`/`AUTH`/`post_supplier_update`/`_all_blocks`/`_first_tag` from `update_suppliers.py` (DRY). SAP field is `PurchasingData.CashDiscountTermsCode` (found via WSDL, not the ContactPerson node).
- **Bug found + fixed** (same class as the ContactPerson task): 24 suppliers had no `PurchasingData` node at all in SAP yet, so `actionCode="02"` (update) failed with "Update not possible; specified data for SUPPLIER does not exist" - fixed by detecting `has_purchasing_data` per code during the bulk read and using `actionCode="01"` (create) instead for those. 1 transient "Locking object not possible" failure (someone editing that record live in SAP UI) resolved itself on retry.
- **Repeated the exact same checkpoint-overwrite mistake from the ContactPerson task**: a `mode == "check"` single-code test call didn't merge with the existing results file and overwrote 2192 already-processed rows. Root-caused and fixed properly this time in **both** `update_suppliers.py` and `update_payment_terms.py`: the results-file merge-on-load now happens whenever the file exists, regardless of `mode` or `only_code` (previously only merged for `mode=="run" and only_code is None`). Re-ran the full batch afterward (idempotent - re-writing 0001 to already-correct suppliers is harmless) to rebuild a complete, accurate audit trail.
- **Final result, live-verified** by re-querying SAP directly for a random sample (12/12 confirmed `CashDiscountTermsCode=0001`): **2216 suppliers updated**, **201 skipped** (Excel `Pcode` doesn't exist in SAP at all - logged in `payment_terms_update_results.json`'s `skipped_not_found`), **0 failed**.
- One-time script, not wired into any UI/cron. The ~670 non-zero-day suppliers were intentionally not touched in this batch.

## One-time SAP Supplier Contact Master Update (Sep 7 2026, user's explicit ask, COMPLETE)
- **Task**: one-off push of Contact Name/Phone/Email from user-provided `Emergent_Supplier_Master.xlsx` (columns: Code, Supplier, GST NO., CORRECT NAME, contact_p, ContactNo, EMAIL) into SAP's existing default Supplier ContactPerson via `ManageSupplierIn` (SOAP). User's explicit scope: "Change Old one" (update the existing default contact only, never create a new one), one-time script (no UI), skip-and-log any supplier not matched.
- **New script**: `/app/backend/update_suppliers.py` - one bulk `SupplierByElementsQuery_sync` read (3003 suppliers) builds a `{Code: {contact_uuid, contact_internal_id}}` map, then per-row `SupplierBundleMaintainRequest_sync_V1` (live) / `SupplierBundleMaintenanceCheckRequest_sync_V1` (dry-run, zero side effects) write via the new `SAP_SOAP_SUPPLIER_MANAGE_ENDPOINT` env var. Results persisted incrementally to `supplier_update_results.json` after every row (resume-safe - a mid-run connection timeout was hit once and the rerun correctly skipped already-processed codes).
- **SAP-side blocker found + resolved by user's SAP Admin**: the `ManageSupplierIn` write endpoint initially rejected every request (including a bare minimum doc-example payload) with a generic "Web service processing error" - the Communication Arrangement for this write operation had never been activated for the `_EMERGENTBOM` technical user (only the read-side `QuerySupplierIn` was active, from August). User's SAP Admin created a new Communication Scenario (`ManageSupplierIn_Emergent`) with "Maintain suppliers" + "Check suppliers" operations released and bound to `_EMERGENTBOM`, Status Active - confirmed via screenshot.
- **2 real payload bugs found + fixed after that** (isolated via SAP's own Check/dry-run operation, zero side effects, using the tenant's actual downloaded WSDL rather than trusting the generic help.sap.com doc example which had 2 stale field names):
  1. Root element for the dry-run operation is `SupplierBundleMaintenanceCheckRequest_sync_V1` (SAP's generic public docs wrongly show `SupplierBundleCheckMaintainRequest_sync_V1` for this exact same operation).
  2. Email field is `WorkplaceEMailURI` (capital E-M) per the tenant's real WSDL schema, not `WorkplaceEmailURI` as shown in SAP's own published doc example - matches the casing already used on the read-side (`sap_supplier_client.py`).
  3. (Found during the live batch, not a schema issue) SAP rejects a `WorkplaceEMailURI` value containing multiple comma-separated emails ("E-mail address X, Y is invalid") - `_first_email()` now takes only the first address when the Excel cell has several. Also `InternalID` must be uppercase (SAP rejected `i1387` verbatim: "Lowercase letters are not allowed in InternalID") - code is now `.upper()`'d before every write.
- **Live-verified end-to-end**: dry-run Check for A2343 (test case user gave: AVA AEROSOLS, Ayush Jain, 8860316024, avaaerosols@gmail.com) returned a clean empty `<Log/>`; live single-record run for A2343 then real-batch (475 rows) both confirmed by re-querying SAP directly afterward (A2343's contact now shows `WorkplaceEMailURI: avaaerosols@gmail.com`, `WorkplaceTelephone: +91 8860316024 mobile=true`; A1076/I1387 multi-email/lowercase-code fixes also confirmed live).
- **Final result**: reconciled by re-querying SAP directly for all 475 Excel rows (a local checkpoint file got overwritten mid-run by a single-code retry call, so final numbers were rebuilt by comparing live SAP data against the Excel expectations - see `supplier_update_results.json`). **435/475 confirmed correct in SAP** (120 existing contacts updated + 315 new contacts created for suppliers that had none, all via `actionCode="01"` create sub-call with `DefaultContactPersonIndicator=true`; SAP auto-normalizes phone formatting with a `+91` prefix which is expected/correct, not a mismatch). **28 failed** with SAP's own validation "Address missing for organization" - creating a brand-new contact person requires the supplier to already have an organizational address on file in SAP, which these 28 don't; out of scope without added address data, logged for manual follow-up. **12 skipped** because those Excel supplier codes don't exist in SAP at all (data-quality issue in the source file, logged for review). `WorkplaceTelephone` supports max 2 numbers (first = mobile, second = landline) when the Excel cell has 2 numbers separated by `/` or `,`; `WorkplaceEMailURI` takes the first token containing `@` when the Excel cell has junk text or multiple comma-separated emails.
- Second CLI mode added: `update_suppliers.py --create-missing [CODE]` (creates a new default ContactPerson, only for suppliers that exist in SAP with zero contacts - separate from `--run` which only updates an existing one).
- **Learning for future one-time scripts**: the resume/checkpoint logic only reloaded the prior results file when `only_code is None`; a single-code retry call (`--create-missing M1599`) silently skipped loading prior results and overwrote the whole checkpoint file with just that 1 entry. Always merge-not-overwrite the checkpoint file regardless of the `only_code` filter in any future one-off batch script.

## Known constraints / learnings (ERP Portal connectivity)
- **ERP Portal sync failures on Live production (Sep 5 2026, STO-000136/137, ship-from P9) - RESOLVED**: initial cause was `[Errno 113] No route to host` on both configured on-prem MS SQL Server IPs (`122.176.161.179`, `59.98.165.33`) - a genuine network outage on the customer's ERP server/firewall/ISP side (P9's `comp.Ccode` mapping was confirmed correct, ruling out the earlier P1W/P2W-style bug). A SECOND, real code bug was then found: the first "Retry ERP Sync" click legitimately started (flipped `erp_portal_status` to `"retrying"`) but was interrupted mid-flight (prod server restart) before it could reach the except-handler that resets status back to `"failed"` - permanently stuck at `"retrying"` with the retry button locked out (guard only allowed retry from `"failed"`). Fixed `reset_erp_portal_sync_for_retry()` (`stock_transfer_service.py`) to also auto-unlock if `erp_portal_status == "retrying"` for longer than `ERP_SYNC_STUCK_RETRYING_THRESHOLD_SECONDS` (180s - both hosts' own connect/login timeouts are 20s each, so 3 min is a generous abandoned-job margin), tracked via a new `erp_sync_retry_started_at` timestamp field (set on retry, cleared on success/failure). Verified with 3 scripted scenarios (stale-stuck/fresh-still-running/normal-failed) before shipping. User deployed the fix to production and confirmed both orders now show `ERP Portal: Synced` on the live app.
- **STO-000120/141 stuck "Goods Issue pending" (Sep 5 2026) - RESOLVED, no code change needed**: same class as STO-000074 (Delivery Request found in SAP but the combine-into-Delivery-Proposal step is slow) - both eventually posted successfully (P9D1-4214 and P7D1-5609) after a few extra minutes past the "Delivery Request found" message. A screenshot showing STO-000141 still "stuck" turned out to be stale (taken before the job actually finished) - both confirmed "Goods Issue posted" on refresh. Lesson: when a GI job shows "Delivery Request found in SAP: X" but hasn't posted yet, the right move is to wait (it's actively progressing), not force-stop.

## Nav / Created POs updates (Sep 5 2026)
- Added a "Filter by Role" dropdown (All/User/Admin/Super Admin) to the Access Management "User Access" tab - user's explicit follow-up ask ("filter... for Only Admin User"). Client-side filter over the already-loaded user list.
- **Superseded same day, user's explicit follow-up ask ("no i dont need filter by role, I need filter by Access right, and Work center wise")**: replaced with a "Filter by Access Right" dropdown (options = every grantable page/permission, e.g. "BOM Management", "Create Purchase Order") - selecting one shows only users who hold that permission (admin/super_admin always included, since they implicitly have every page). Each user's permission checkboxes are now grouped under the same Work Center headings as the nav (`WORK_CENTER_GROUPS` in `AccessManagementPage.jsx`: BOM & Production, Procurement, Supplier Management, Inventory Management, Master Data, Administration, + an "Other" fallback for any ungrouped key - currently empty, all 20 `PAGE_LABELS` keys are grouped). Fixed a leftover half-applied edit from the previous session (undefined `roleFilter` variable referenced in JSX, would have crashed the page) in the same pass. Self-tested via screenshot (desktop 1920px + mobile 390px, no overflow) - filter correctly narrows 25 users -> 16 when set to "BOM Management".
- Moved "Vendor Goods Receipt" from Supplier Management dropdown to Procurement dropdown (user's explicit ask - it's a receiving action, not a supplier record). Renamed "Goods Issue" -> "Store Goods Issue" everywhere (NavTabs, Access Management PAGE_LABELS, backend PAGE_CATALOG) - same `store_approval` key/route underneath, cosmetic only.
- Added filters to Created POs page (`CreatedPurchaseOrdersPage.jsx`): Supplier, Plant, Item/Product ID, Created By, PO Date range. New backend endpoints `GET /purchase-orders/history` (now accepts `supplier_code`/`site_id`/`product_id`/`created_by`/`po_date_from`/`po_date_to` query params) and `GET /purchase-orders/history/filter-options` (distinct supplier/created_by values for the dropdowns; Plant dropdown reuses existing `/purchase-orders/sites`). Verified end-to-end with a `role: "user"` test account holding only `created_purchase_orders`.

## Access Management - permission split (Sep 5 2026)
- User's explicit ask: split 5 pages out of the 2 catch-all permissions (`supplier_portal_admin`, `purchase_order`) into independently-grantable rights, same pattern as the earlier stock_transfer/inbound_stock_transfer split. New keys: `supplier_portal_invite` (Invite Supplier), `vendor_goods_receipt` (Vendor Goods Receipt / GRN Approval page), `supplier_dashboard` (nav shortcut to external Supplier Portal login), `created_purchase_orders` (Created POs list), `open_purchase_orders` (Open Purchase Orders viewer). `supplier_portal_admin` now means ONLY "Supplier Portal Approvals" (vendor sign-up review) and `purchase_order` now means ONLY the "Create Purchase Order" write form - both narrowed per user's explicit choice.
- 3 places needed updating for a permission split like this (easy to miss one): `PAGE_CATALOG` + `PAGE_ROUTE_RULES` in `auth_service.py` (backend enforcement + catalog), `PAGE_LABELS` in `AuthContext.jsx` (frontend - Access Management page's checkbox list is NOT dynamically fetched from the backend catalog, it's a separate hardcoded object here), and each page's `page` key in `NavTabs.jsx` + `ProtectedRoute` in `App.js`. Also added the 5 new keys to `DEFAULT_LANDING_PAGE_BY_KEY` in `App.js` (pre-existing minor gap: `stock_transfer`/`inbound_stock_transfer`/`purchase_order` were also missing from that list before this fix).
- Verified end-to-end: checkboxes render on Access Management UI, and backend correctly 403s a `role: "user"` test account missing each specific new permission while allowing the one it was granted (super_admin/admin roles always bypass ALL page checks per existing design - don't use those roles when testing page-level enforcement).
- **Missed spot found + fixed (Sep 7 2026, user report "Search Vendor not work for other user, works for Admin")**: root cause of exactly this class of bug - `PAGE_ROUTE_RULES`'s `/api/suppliers` entry (backs the "Search SAP Vendor" combobox on Invite Supplier) was never updated when `supplier_portal_invite` was split out of `supplier_portal_admin` in this same session - it still only allowed `{"supplier_master", "quota_allocation", "supplier_portal_admin"}`. A plain `user`-role account granted only the correct `supplier_portal_invite` permission could open the page (frontend route check passed) but got 403'd on every `/api/suppliers` search call - invisible to an admin/super_admin tester since those roles bypass ALL page checks. Added `supplier_portal_invite` to that rule's allowed set. Live-verified with a synthetic `user`-role session holding only `supplier_portal_invite`: search "HAMIDI" now correctly returns "H1330 HAMIDI EXPORTS".

## Purchase Order Creation - autofill + item lock (Sep 5 2026, user's explicit ask)
- **Purchase Unit + Bill-To now both auto-fill from the fetched PR** (`PurchaseOrderPage.jsx`): Purchase Unit already autofilled from `data.compcode`; added Bill-To auto-fill to `{site}-FIN` (matching the site prefix) right alongside it, in `fetchPR`. Removed the separate `useEffect` that reset Bill-To on every Purchase Unit change (was firing right after PR autofill and wiping it out) - replaced with an explicit `onPurchaseUnitChange` handler that only resets Bill-To on a deliberate MANUAL site change.
- **Removed "Add Item" button and the non-PR "Duplicate row" fallback** per user's explicit ask ("User can only split Qty do not add new item") - a buyer can now only split an existing PR line's quantity across delivery dates (existing "Split" button, unchanged), never add a brand-new line item. Removed the now-dead `addLine()` function and unused `Plus`/`Copy` icon imports.
- **Purchase Unit + Bill-To changed from editable dropdowns to read-only auto-derived badges (Sep 5 2026, user's explicit follow-up ask)**: both fields are now plain display badges matching the existing "Company (Business Residence)" field's visual treatment (`{value} · Auto-derived`) - no longer a `Select` a buyer could override. Both derive purely from the fetched PR (`purchaseUnitSite`/`billToCompany` state, set in `fetchPR`); there is no manual-entry path anymore. Removed the now-unused `onPurchaseUnitChange` handler.

## Known constraints / learnings (SAP material site activation)
- **"Activate this site" bug found + fixed (Sep 5 2026, STO-135, SPLICE @ P8)**: `activate_site()` (`sap_material_create_client.py`) always hardcoded `ProcurementTypeCode=2` ("External procurement"), which works for raw materials (FLAT-BK50/FLAT-BK21, code 2 at every site) but SAP rejects it - with the misleading "Supply planning ID P8; does not exist" instead of a clearer message - for in-house-manufactured Semi-Finished Goods (SPLICE/AB24/P41551, all need code 1 at manufacturing sites). Fixed by adding `SAPMaterialClient.get_existing_procurement_type_code()` (`sap_material_client.py`) - reads the material's OWN existing SupplyPlanning entries at other sites and reuses the majority code (falls back to "2" only if the material has zero existing sites) - wired into `post_activate_material_site` (`server.py`). Also fixed a related idempotency gap: re-activating an already-active site used to show a confusing SAP error ("Similar entry already exists") instead of "ok". Verified end-to-end live against real SAP (same tenant prod uses): SPLICE @ P8 Planning/Logistics/Availability now "ok", and a full retry of STO-000075 (test order created on preview) created successfully in real SAP (Order ID 31113). Deployed + confirmed live for STO-135.
- **`_MISSING_PLANNING_RE` widened**: SAP returns >1 wording for "product never set up at destination site" - added `_MISSING_SUPPLY_PLANNING_RE` for "Supply planning ID X; does not exist" (site-only, no product name) alongside the original "No valid planning data exists for product X in site Y". When matched, creates one Activate notification per item on the order (message doesn't name the product).
- **Valuation activation - NOT YET POSSIBLE via API, needs SAP Business Configuration first**: SPLICE's Valuation still fails with "Account det. group is missing for material SPLICE company RI bus res P8" even when explicitly supplying an Account Determination Group code (tried "3020" per user's mapping: Raw Material=3010, Semi-Finished=3020, Finished Goods=3030, Consumable=3040, Scrap=Z001). Confirmed via live testing this is NOT a per-material API field gap - group codes like "3020" don't appear to be registered in SAP Business Configuration for Company RI/RT yet. Found the correct SAP service for this (`ManageMaterialValuationDataIn`, distinct from `ManageMaterialIn` used for Planning/Logistics) and it's reachable, but blocked until the user's SAP team completes the Business Configuration setup for these Account Determination Groups. Once confirmed done, need to wire up: `AccountDeterminationSpecification` (group code per ProductCategoryID), `InventoryValuationSpecification.PerpetualInventoryValuationProcedureCode` (likely "2" = Moving Average, unconfirmed for certain), and a `ValuationPrice` entry (PriceTypeCode "1" = Moving Average in this tenant per `sap_valuation_client.py`, Amount=0, StartDate=today-30d, EndDate="9999-12-31" per this tenant's own open-ended sentinel convention, SetOfBooksID=RSOB/RDOB per site). IMPORTANT: this does NOT block Stock Transfer Order creation (confirmed live - STO-000075 created successfully even with Valuation still failing) - it's a separate, non-blocking enhancement.

## Goods Issue stuck "waiting" forever after Delivery already created but unreleased (Sep 9 2026, live incident STO-000195, order 31297)
- **User report**: Goods Issue "still pending after 20 min", but SAP UI showed Outbound Delivery P2D1-5527 already existed (linked to Delivery Request 59949) with Release Status "Not Released" - i.e. SAP HAD created the delivery, it just never got Released/GI-posted.
- **Root cause**: `_try_post_goods_issue_multiline`'s Playwright call (`combine_and_post_goods_issue_via_ui`) genuinely creates the Outbound Delivery in SAP via "Create Outbound Delivery" > "Without Release" (irreversible), then does a bounded 4x8s retry loop to find that Delivery's ID via OData before proceeding to Release. If that whole Python call then hit its own attempt timeout (or any transient error) before finishing, the generic `except Exception` handler returned "waiting" WITHOUT ever persisting `outbound_delivery_ids` on the STO doc. Every later poll tick re-opened SAP's Delivery Proposals screen, which now shows 0 rows for this order (its request items were already consumed into the Delivery that silently exists) - the existing "release an already-known Delivery" fallback (`_try_release_existing_multiline_delivery`, built for a similar Sep 2026 incident) never got a chance to run because it only fires when `outbound_delivery_ids` is already known. Stuck this way for the full 20-min timeout, no self-recovery.
- **Fix**: `_try_post_goods_issue_multiline` now does a fast, Playwright-free OData check (`find_outbound_delivery_objects`, the same item-UUID link-table lookup used elsewhere) for an already-existing Delivery covering these exact lines BEFORE ever launching the Playwright combine flow - this query doesn't depend on Delivery Proposals still showing the (already-consumed) rows. If found, persists `outbound_delivery_ids` immediately and routes straight to the existing release-only path instead of trying to re-combine. Verified with a mocked SAP client (3-line order, delivery already exists but unreleased) - correctly detects it, calls `release_outbound_delivery`, and sets `gi_status: "posted"`.
- **Action needed**: deploy via "Save to GitHub" for this to take effect on the live app. STO-000195 itself should self-recover on its next automatic poll tick (or click "Retry Goods Issue") once deployed - no manual SAP intervention needed, though the user can also just click "Release" directly in SAP UI on delivery P2D1-5527 if they want it resolved immediately without waiting for a deploy.
- **Follow-up, user's own find (same day)**: user pointed at a real SAP Analytics report, `RPSCMOBDB04_Q0001QueryResults`, filtered by `CSTO_REF_ID` (the order's own SAP Order ID) - confirmed LIVE against the real STO-000195/order 31297 that it returns `CDELIVERY_UUID` (despite the name, this is the human-readable Delivery ID, e.g. "P2D1-5527" - directly usable) + `CDELIVERY_STATUS`/`TDELIVERY_STATUS` ("3"/"Finished" once GI has posted, same 1/2/3 convention as `order_fulfilment_status` elsewhere) + per-line product/quantity, all via one fast OData GET, no Playwright, and independent of whether Delivery Proposals still shows the (already-consumed) rows. New `sap_outbound_delivery_analytics_client.py`. Wired in as the PRIMARY pre-check in `_try_post_goods_issue_multiline` (checked every poll tick, before ever launching Playwright): if a Delivery is found already Finished, marks posted immediately (skips Playwright + the release call entirely); if found but not yet Finished, skips straight to the release-only path (`_try_release_existing_multiline_delivery` - fast API release first, Playwright fallback only if that fails). The item-UUID-link-table check added earlier the same day is kept as a secondary fallback only (in case this analytics report itself ever lags). Verified with 2 mocked scenarios (not-yet-finished -> release called; already-finished -> no release/Playwright call at all, straight to posted).
- **User's question "can you self-release if Delivery created but Release pending?" - YES**: `sap_outbound_delivery_client.release_outbound_delivery()` (a single fast OData POST, `OutboundDeliveryRelease`) already exists and is tried FIRST, before ever falling back to the slower Playwright UI click-through (only needed if SAP's Consistency Status check hasn't settled yet) - see `_try_release_existing_multiline_delivery`.

## ERP Portal sync permanently stuck "syncing..." with no Retry option (Sep 9 2026, live incident STO-000196)
- **User report**: order showed "ERP Portal: syncing..." indefinitely (no error, no Retry button - that only ever appeared for status `"failed"`).
- **Root cause**: the very FIRST (non-retry) ERP sync fires via a bare `asyncio.create_task` with no status/timestamp written beforehand. If the app server restarted (deploy/hot-reload) before that task reached either `sync_to_erp_portal`'s success branch or `_run_erp_portal_sync_job`'s except block, `erp_portal_status` stayed permanently unset - the frontend's default text ("ERP Portal: syncing...") showed forever, and `reset_erp_portal_sync_for_retry` rejected every attempt ("not currently in a failed state" - that guard only ever recognized an orphaned **retry** attempt via status `"retrying"`, added for a similar Sep 5 2026 incident, never an orphaned **first** attempt).
- **Fix, simplified per user's explicit ask ("add a retry button instead of looping")**: first pass used a time-based self-heal (auto-flip to "failed" after 180s stuck) - replaced same-day with a simpler always-available button per the user's direct request. `reset_erp_portal_sync_for_retry` now blocks ONLY when `erp_portal_status == "synced"` (the one state that must never be double-synced, to avoid a duplicate Delivery Challan/Sale_No) - every other state (`failed`/`syncing`/`retrying`/never-started/null) is retriable on demand, no waiting. Frontend's "Retry ERP Sync" button (`StockTransferPage.js`) now shows whenever status isn't `"synced"`, not just `"failed"`. `mark_erp_portal_syncing()` still sets a status+timestamp right before every attempt (informational only now, no longer gates anything). Verified: retry allowed from `None`/`"syncing"`/`"retrying"`/`"failed"`, correctly blocked once `"synced"`.
- **Action needed**: deploy via "Save to GitHub". STO-000196 will show a working Retry button immediately after deploy (no wait).

## Retry/Resume creating a duplicate history row + stale Retry button (Sep 9 2026, live incident, proposals 230577/230628, order 72180)
- **User report**: (1) after Retry succeeded and released order 72180, it appeared as a brand new disconnected row instead of updating proposal 230577/230628's own row; (2) "Retry" button stayed visible on 230628 even though it had already been retried/resolved; (3) asked why 230577 took a while after clicking Retry.
- **Root cause (1+2, same bug)**: `log_order_release()` always tried matching by `job_id` FIRST whenever one was passed. A Retry/Resume always runs under a brand-new `job_id` (the original job doc is long gone/unrelated) but ALSO passes `match_proposal_id` (the real link back to the original row) - the old code, on the job_id match failing (guaranteed to fail by construction), gave up immediately without ever trying `match_proposal_id`, and fell through to a disconnected `insert_one` every time. This both created the duplicate row (order 72180) AND left the ORIGINAL row's `production_order_id` unset forever - which is exactly what `can_retry` checks, so the Retry button kept showing on 230628 even after it had actually succeeded (just recorded under a different, disconnected row).
- **Fix**: try `job_id` match first, then ALSO try `match_proposal_id` before giving up and inserting a new row - either match now correctly updates the SAME original row in-place. Verified with a simulated retry (new job_id + match_proposal_id against an old proposal_created row): 1 row total, `production_order_id` set on the original row, zero disconnected rows created.
- **(3) Not a bug** - by design, a Retry/Resume first checks if SAP already has an Order "In Preparation" for that proposal (instant if so); if not, it re-fires the Release trigger and polls SAP every 4s (re-triggering every 90s) for up to 20 min, same as a fresh proposal, because SAP's own Proposal→Order conversion is asynchronous and not in the app's control.
- **Action needed**: deploy via "Save to GitHub". Note: proposal 230628's own history row is already permanently stuck in its "Created"-only shape in the LIVE production DB (from before this fix) - the fix only prevents this for FUTURE retries; that specific row would need a one-off manual DB correction if the user wants it cleaned up (I can't reach production DB from this pod to do it directly).

## Item 100162725 missing from Stock Transfer's item search despite having stock (Sep 9 2026, live incident)
- **User report**: searching "100162725" on Inter-Plant Stock Transfer's item picker showed 8-10 suffixed variants (100162725-1, -P4, -PL7, AL1, AL2, PL1, PL10, PL11...) but never the exact base item "100162725" itself, even though it has stock.
- **Root cause, reproduced exactly**: `/api/products/search` (`component_master` cache, 3500+ parts) had no `.sort()` before `.limit(10)`. With 20 real matches for this prefix, Mongo's `$or` regex query returned them in an arbitrary (non-alphabetical) order that happened to push the single most relevant result - the exact match itself - past the top-10 cutoff. Reproduced against the local `component_master` copy: same exact 8 items in the same order, exact match missing.
- **Fix**: added `.sort("_id", 1)` - alphabetical sort naturally puts a shorter string before any longer string it's a prefix of (e.g. "100162725" sorts before "100162725-1"), so the exact/closest match is now always first and never crowded out. Also fixed the identical pattern (filter+`$slice` with no sort) in `/purchase-orders/products/search` (PO Creation's item picker) before it hit the same bug. Verified: sorted query now returns "100162725" first, followed by "100162725-1", "-P1"..."-P4" etc.
- **Action needed**: deploy via "Save to GitHub".

## ERP Portal false "already Exist" duplicate blocking 2 identical shipments (Sep 9 2026, live incident STO-000202/000203)
- **User report + screenshots**: both orders showed "ERP Portal sync failed" with the exact same error: `Customer :RTP3, Plant : P2, Emp No :, Total:42085.40 already Exist`.
- **Investigation (live, read-only against both SAP + the ERP MSSQL DB)**: orders 31335 (STO-202) and 31351 (STO-203) are 2 minutes apart, both shipping the IDENTICAL product+qty (Wall Plate 100162759-1 x 460 EA, P2->P3) - genuinely different physical shipments that happen to compute the same Amount. Confirmed via direct SQL query: exactly ONE Delivery Challan (Sale_No 38682, Sale_Noc 2406, dated Sep 8) exists in `DeliveryChallan` matching Plant=P2/Customer=RTP3/Amount=42085.40. `Pro_DeliveryChallan_Insert`'s own duplicate-check only considers (Plant, Customer, Emp_no, Amount) - no date, no product - so the SECOND order's genuinely-new insert was rejected as a "duplicate" of the FIRST order's own already-successful Challan, and both then displayed "failed" (the first one's real success was never recorded either, since the app only finds out the Sale_No from the INSERT's own OUTPUT param, which the rejected second attempt never produced).
- **Could not auto-determine which order the existing Sale_No 38682 belongs to** (both orders are numerically/product-wise identical) - asked the user, who confirmed via the physical Challan/vehicle (UP81FT3649): **STO-000202**.
- **Fix 1 (root cause, prevents recurrence)**: `sync_to_erp_portal` now fills the previously-always-blank `Emp_no` field with the order's own SAP Order ID (e.g. "31335") - unique per order, fits the column's `varchar(10)` limit (SAP's own 10-digit zero-padded IDs). Two orders can now never collide on (Plant, Customer, Emp_no, Amount) again, even with an identical Amount. (Deviates from an earlier explicit "leave Emp_no blank" instruction - user approved this change today since the field is otherwise unused by our syncs.)
- **Fix 2 (recovery for this + any future "ERP already has it, we lost track" case)**: new admin-only endpoint `POST /stock-transfer/orders/{sto_id}/manual-erp-link` (`manually_link_erp_sale_no` in `stock_transfer_service.py`) + a "Link to Existing Challan" button (visible only to admin/super_admin, next to Retry ERP Sync) - lets a human who has confirmed the real Sale_No/Sale_Noc outside the app mark the order "synced" without re-inserting. Blocked once already "synced". Verified via unit test.
- **Action needed**: deploy via "Save to GitHub". Once live: (1) click **"Link to Existing Challan"** on STO-000202, enter Sale_No `38682` / Sale_Noc `2406`; (2) click **"Retry ERP Sync"** on STO-000203 - with the Emp_no fix now in place, it will get its OWN genuinely new Sale_No instead of colliding with 202's.

## Supplier Additional Documents - MSME Certificate + Bank Details/Cancelled Cheque (Sep 9 2026, new feature)
- **Request**: vendor-facing "Documents" tab to upload MSME Certificate + Bank Details/Cancelled Cheque anytime after approval; visible alongside GST/PAN on the admin "Supplier Portal Approvals" page (Approved tab only); each re-upload saved as a new REVISION, never deleted, latest = current; new granular permission "Supplier Additional Documents" for VIEW-ONLY access to that whole admin page, separate from the existing "Supplier Portal Approvals" (Approve/Reject) permission.
- **Backend**: `supplier_portal_service.py` - `ADDITIONAL_DOC_TYPES=("msme","bank")`, `upload_additional_document`/`list_additional_documents`/`get_additional_document` (Object Storage, versioned path `sap-mrp-portal/supplier-docs/{account_id}/{doc_type}/v{n}.{ext}`, revisions kept in a `{doc_type}_documents` array field, never overwritten). Guards: only accepted after account status="approved", max 10MB, PDF/JPG/PNG only.
- Vendor routes: `GET/POST /api/supplier-portal/documents[/{doc_type}[/{version}]]`. Admin routes: extended `GET /api/admin/supplier-portal/{account_id}/documents/{doc_type}?version=N` to also serve msme/bank (latest by default).
- **Permission model**: new `supplier_portal_documents` key in `auth_service.py` PAGE_CATALOG; the `/api/admin/supplier-portal` route rule now accepts EITHER `supplier_portal_admin` OR `supplier_portal_documents` (middleware can't distinguish HTTP method) - Approve/Reject endpoints have an explicit in-handler `_require_supplier_portal_admin()` guard as the real enforcement point for that split.
- **Frontend**: new vendor page `SupplierDocumentsPage.jsx` (+ "Documents" nav button on the dashboard); `SupplierPortalApprovalsPage.jsx` gets an "Additional Documents" column (Approved tab only, with a version-history dialog) and hides Approve/Reject entirely when `!hasPageAccess("supplier_portal_admin")`; `hasPageAccess()` extended to accept an array of keys (any match grants access); `AccessManagementPage.jsx` Supplier Management group includes the new key.
- **Tested**: testing_agent - 14/14 backend cases pass (upload/revision/history, invalid type, size/MIME limits, unapproved-account block, unauth, admin approve/reject boundary) + all frontend flows verified (upload, versioning, history dialog, column visibility per tab, docs-only viewer sees no Approve/Reject). Fixed 2 low-priority hardening gaps flagged by the reviewer: added the approved-status + size/MIME guards, and defensive exception symmetry on the admin GET handler.
- **Action needed**: deploy via "Save to GitHub".

## Known constraints / learnings
- **"Action Needed" panel hidden from Inter-Plant Stock Transfer main page (Sep 7 2026, user's explicit ask)**: removed the aggregated activation-notifications panel from the main list page (`StockTransferPage.js`) - the exact same "Activate {site} for {product}" action already surfaces inline on each affected order's own detail view (`OrderDetailBody`, opened via the order list) since it was built from the same shared `notifications` state/prop. User felt the main-page panel was redundant clutter once they realized the action is reachable per-order. Notifications data-fetching/activation logic (`loadNotifications`, `handleActivate`, etc.) is unchanged - only this one panel's rendering (+ its now-unused `Wrench` icon import) was removed. Live-verified via screenshot: panel is gone from the main page.
- **Search Vendor 403 for non-admin users on Invite Supplier - fixed (Sep 7 2026)**: `/api/suppliers`'s `PAGE_ROUTE_RULES` entry in `auth_service.py` was missed when `supplier_portal_invite` was split out as its own permission - only admins/super_admins (who bypass all page checks) could actually search vendors on that page. Added `supplier_portal_invite` to that route's allowed set.
- **Recent Stock Transfer Orders filter row added (Sep 7 2026, user's explicit ask)**: filter row above the "Recent Stock Transfer Orders" table on `StockTransferPage.js` - STO ID (substring), Created Date, Created By (dropdown), Ship-from (dropdown), Ship-to (dropdown), Item (product ID/description substring, checked across all of an order's line items). All client-side over the already-loaded `recentOrders` (backend caps at the 100 most recent, unchanged) - no backend change needed. Header shows "X of Y" once any filter is active, "Clear Filters" resets all. Live-verified: STO ID substring, Ship-from=P1 (18/66 rows, all P1), Item=SPLICE (1 matching order) all filtered correctly.
- **Root cause found + live-fixed: Proposal 229965 stuck "Waiting for SAP to convert Proposal into Order" for 6+ min (Sep 7 2026, PDQ3286 qty 19 @ P9)**: SAP's "already requested" rejection on retrigger attempts #2-4 was expected/normal (SAP's own async Proposal->Order conversion job was still running - confirmed live via `sap_production_proposal_release_client.resolve_object_id('229965')` still finding it in `ProductionPlanningOrderCollection`). The REAL bug: SAP HAD already converted it to a real Order (71933, confirmed live via `list_ids_by_status("1")` + `get_requested_material` matching PDQ3286/19 exactly) sitting "In Preparation" - but the create-and-release job's self-release loop (`server.py`, the `create_and_release_job` background task) only ever checked the SINGLE highest-ID candidate among `new_prep_ids` each tick, then unconditionally folded ALL new IDs (not just the checked one) into `baseline_prep_ids` - so if OUR order wasn't the max ID that tick (this SAP tenant is busy - 149 "In Preparation" orders tenant-wide at any time, other users'/lines' proposals converting concurrently), it was silently forgotten forever, never re-checked. Fixed: now loops over and verifies EVERY new candidate each tick, only folding the ones actually checked into baseline (a transient-error candidate still stays "new" for next tick, same as before). **Manually released order 71933 directly via `sap_production_order_release_client.release_order('71933', verify_status=True)` to unblock the user immediately** - confirmed LifeCycleStatusCode now "2" (Released). Code fix needs a redeploy to take effect on the live app for FUTURE occurrences of this race (this session's pod is preview-only).
- **Root cause found + fixed: "menu is not visible" on Created Purchase Orders page (Sep 7 2026)**: `CreatedPurchaseOrdersPage.jsx` used a plain white `<header>` bar while every other page in the app uses the standard teal (`#0E7C86`) header. NavTabs/SapConnectionStatus render WHITE text by design (meant to sit on that teal bar) - on a white background they were still rendering, just invisible (white-on-white), except the one active/current tab (its text turns teal when active, so "Procurement" was the only thing visible - exactly what the user's screenshot showed). Fixed by matching the standard header markup (logo block + NavTabs + SapConnectionStatus) used on `OpenPurchaseOrdersPage.jsx` etc. Live-verified on both desktop and mobile with a synthetic restricted account.
- **Root cause found + fixed: "finish task closed by live app but WIP is not posted" + "Last Confirmation" column never updates (Sep 7 2026, Lot 71930)**: `/production-confirmation/finish-task` (the "stuck task" recovery endpoint, see its own Sep 2 2026 fix note above it) only ever closed the Task in SAP and returned - unlike `/confirm`, it never ran WIP Clearing/FG Movement at all, and never logged anything to `production_confirmation_history` (which is exactly what feeds the "Last Confirmation" column via `/history/latest-batch`). Fixed: `finish_production_task()` now runs the same WIP Clearing (+ FG Movement if the item classifies as Finished Goods) and logs a history entry, gated the same way `/confirm` does (`_all_lot_operations_finished`) - new `FinishTaskRequest` fields (`site_id`, `main_output_product`, `confirmed_quantity`, `unit_code`, `reporting_point_id`) are optional so older callers keep working Task-close-only. Frontend's `finishTaskOnly()` now sends these from the row. **Immediately unblocked Lot 71930 live: ran WIP Clearing directly (`sap_wip_clearing_client.run_wip_clearing('71930', 'P9')`) - confirmed `success: true` in SAP.** Needs a redeploy for the "Last Confirmation" column fix and future finish-task calls to take effect on the live app.
- **Confirmed this session: the deployed app's DB is fully isolated from this preview's local Mongo** (`MONGO_URL=mongodb://localhost:27017` here vs whatever persistent DB the deployment uses) - SAP OData/SOAP credentials ARE the same live tenant either way, so SAP-side checks/fixes take effect immediately regardless of which environment is used, but MongoDB-backed state and unreleased frontend/backend code changes do NOT reach `mrp.radishtechnologies.com` until redeployed.
- **This pod's `SAP_USERNAME`/`SAP_PASSWORD` (used for `sap_production_lot_client`'s SOAP queries, e.g. `find_open_lots`/`find_lot_by_id`) currently gets "Authorization role missing" from SAP** - a DIFFERENT credential pair, `SAP_SOAP_USERNAME`/`SAP_SOAP_PASSWORD` (used by `sap_wip_clearing_client` and others), works fine and was used for all live SAP checks this session instead. Worth flagging to SAP Basis if this preview pod ever needs direct Production Lot SOAP queries again.
- RI (RAY INTERNATIONAL) sites today: P1, P8 (also nominally P1W/W1 per SITE_TO_COMPANY map, but those have zero live inventory so never appear in `list_known_sites`). RT (RADISH TECHNOLOGIES) = everything else, including P2W.
- QC warehouse does not exist at every site - the "default to QC" behavior is a soft pre-select, never a hard requirement.
- Never trigger a real "Approve & Post to SAP" click/call on a real PO outside of a deliberate, user-approved live test - it queues a real Playwright job against production SAP.
- Scrap/by-product Net Weight enforcement (`_pick_rm_item` in server.py) must NEVER guess an RM by "biggest quantity" for an item with no prior Net Weight precedent - only known raw-stock keywords (Iron/CR/SS/Alu/Brass/Copper family incl. Rod/Pipe/Angle/Channel/Mesh/Ingot) or an already-configured `component_master.net_weight_kg` may trigger the requirement.
- `SITE_TO_PERMANENT_ESTABLISHMENT_UUID` (sap_valuation_client.py) is the only place this site->UUID mapping lives - reuse it, don't rediscover, if extending site-specific pricing to other screens.
- SAP custom/one-off extension fields (namespace `http://0012819041-one-off.sap.com/YPS7GEURY_`, e.g. BusinesResidence/PoDate/BrNumber on PurchaseOrder) ARE readable via the Query web service but writes via the Maintain web service can be silently ignored (HTTP 200, no error, field just stays blank) if the field itself isn't enabled for web-service write access in SAP's own Business Configuration - always verify a write round-trip with a real read-back before declaring a custom field "fixed", don't trust a 200 response alone.
- SAP ManagePurchaseOrderIn has a `PurchaseOrderBundleCheckMaintainRequest_sync` operation (same endpoint, swap the root element name from `...MaintainRequest_sync` to `...CheckMaintainRequest_sync`) that simulates a create with ZERO side effects (no PO created) - ALWAYS use this to isolate schema/structure bugs (wrong element position, wrong date format, etc.) before spending a real live PO creation to verify. BUT a 200 OK / no-fault Check response does NOT prove a field is correctly bound - SAP's schema has a lax trailing extension point that silently accepts and drops ANY well-formed-but-unrecognized element without complaint, so a real create + read-back (or user SAP UI screenshot) is still required to confirm persistence.
- Extension fields under the tenant's `http://sap.com/xi/AP/CustomerExtension/BYD/A4CF6` namespace (PODate, PortalPRNumber - confirmed via SAP UI's own "Extension Field > Services" tab) must be placed right after `<Date>` at PurchaseOrder header level (NOT trailing after `<Item>`, which causes a full SOAP fault) - `PODate` specifically needs a plain `YYYY-MM-DD` value, a full ISO datetime there also faults the whole call.
- The app's real established visual theme (JDE Enterprise) is `#004B87` primary/`#003A6A` hover/`#00294D` active, `#F2F4F7` page bg, `#D0D5DD` borders, `#EAECF0` table headers/`#344054` header text, `rounded-sm` everywhere, `h-16 bg-[#0E7C86]` teal header with NavTabs+SapConnectionStatus (see `GrnApprovalPage.jsx`) - always match this, don't invent a new palette for a single page even if a design_agent run suggests one.
- `Intl.NumberFormat` locale must vary with currency (`en-US` for USD, `en-IN` for INR) - a fixed `en-IN` locale wrongly applies lakh-style digit grouping (7,61,840) to USD amounts too.

## Session (Sep 10 2026, continued) - Printed PO Ref bug fix
- User reported: "Printed PO ref" still not showing on Supplier Dashboard's Open PO list even after the manual "Refresh from SAP" fix from the previous session.
- Root cause found: `list_po_numbers_missing_custom_number()` in `supplier_shipment_service.py` sorted the missing-PO worklist ASCENDING (plain `sorted()`), contradicting its own docstring (which says "sort DESCENDING, newest first"). With ~110 old unresolved POs and a 30-item cap, brand-new POs like 29430 never made it into the worklist, so their custom SAP PO Number was never fetched.
- Fix: changed to `sorted(known - already_fetched, reverse=True)[:limit]` so newest POs are prioritized.
- Verified end-to-end: fetched live from SAP (`P1PO-00649/26-27` for PO 29430), confirmed via `/api/supplier-portal/purchase-orders` API response, and visually confirmed on the Supplier Dashboard UI (screenshot) - "Printed PO #: P1PO-00649/26-27" now shows under PO 29430.
- Status: FIXED and USER-FACING VERIFIED (via UI screenshot).
- Deployment to production (mrp.radishtechnologies.com) still pending user's "go" signal.


## Session (Sep 10 2026, continued #2) - SFG Shortage no longer blocks Proposal creation
- User's explicit ask: "if SFG short then user not able to create a production proposal so now we allow user if SFG short then user can create proposal but IF BOP short then store request generated (SFG Shortage - Allowed with flag)".
- Changed `_run_create_and_release_job` in `server.py`: a short Sub-Assembly (SFG, `is_sub_assembly=true`) component no longer hard-blocks order creation (previously: job failed with `reason=sfg_shortage`, no Proposal created). Now it's a NON-BLOCKING flag (`job.sfg_shortage = {site_id, short_components}`) that persists across every later job status, while the SAP Proposal + rest of the pipeline proceeds normally.
- BOP/RM (`is_sub_assembly=false`) shortage behavior is UNCHANGED: still creates a real Store Approval request and pauses at `waiting_store_approval`. Both flags coexist if both SFG and BOP are short simultaneously.
- Frontend (`ProductionConfirmationPage.js`): Active Orders table shows a new amber "SFG Shortage - Allowed with flag" badge + persistent (non-error styled) detail row listing each short sub-assembly, alongside (not replacing) the normal status/progress UI. Removed the now-dead "SFG Shortage - Blocked" red badge/detail-row code path.
- Tested via `testing_agent` (iteration_154): 100% backend + frontend pass, no live SAP writes needed (seeded job docs directly to prove branching + persistence + UI rendering). No issues found.
- Deployment to production (mrp.radishtechnologies.com) still pending user's "go" signal.


## Session (Sep 10-11 2026, continued #3) - GRN stuck "In Process" investigation + fixes
- User reported (screenshot) shipments T3X99F/4MW4P4 (PO 29118) and PNC9DW/X7SLJT (PO 29073) stuck "In Process" on PRODUCTION (mrp.radishtechnologies.com) even after retrying.
- Investigation: those shipment records don't exist in this preview's DB - confirmed the screenshot is from production, which was last deployed Sep 9 ~10:15 PM IST, BEFORE an earlier same-day commit (c4a3ca8, Sep 10 ~7:38 PM IST) that fixed the "stuck In Process forever" symptom (auto-escalates to "Sync Failed" after 3 retries) - so production was running old code.
- Real root cause for these 2 specific POs: SAP itself rejects the Goods Receipt with "Actual quantity for Delivery Item ID 40" - confirmed a genuine SAP-side data/validation issue, NOT an app bug. User's SAP admin found: Inbound Delivery Request 60063 is LOCKED BY USER ITADMIN (our own automation's technical user) - strongly suggests a prior failed "Save and Close" attempt left the SAP-side draft checked out because the old cleanup code (`_click_button(page, "Close")`) fired-and-forgot with no verification it worked.
- Fixes shipped:
  1. Hardened `sap_playwright_supplier_pgr_service.py`: new `_ensure_draft_discarded()` helper tries "Close" -> "Cancel" -> Escape key (verified, not fire-and-forget) on every error-exit path in `_post_one_po`, to prevent future stuck SAP-side locks.
  2. New admin-only "Reset Retry" endpoint (`POST /api/admin/grn/{doc_code}/reset-retry`) + frontend button (GrnApprovalPage.jsx) - lets staff clear a "SAP Sync Failed" shipment's retry count once SAP Admin confirms the underlying SAP issue (e.g. the ITADMIN lock) is resolved, so Retry can be attempted fresh.
- NOTE: the CURRENT lock on Inbound Delivery Request 60063 is NOT resolved by any of the above - it needs the user's SAP Admin to explicitly cancel/release that specific document inside SAP itself. Our automation has no confirmed API/UI action to force-unlock an already-stuck document from outside.
- Deployed to production (mrp.radishtechnologies.com) via deployment_agent - scan passed, no blockers. Includes all of today's fixes: Printed PO Ref sort bug, SFG-shortage-allowed flag, GRN status-truthfulness + Reset Retry.
- Status: DEPLOYED. Awaiting user/SAP admin to manually resolve the SAP-side lock on Delivery Request 60063 for PO 29118/29073 specifically.


## Session (Sep 11 2026, continued #4) - Production Model not found for MAZ42117272-TA (investigated, not an app bug)
- User re-released/updated Production Model MAZ42117272-TA_2 in SAP, then immediately checked the Production Confirmation Admin Test page (`/admin/production-confirmation-test`) and it wasn't reflecting the update.
- Investigated live: queried `sap_production_model_client.get_source_of_supply_options()` directly and the RAW SAP OData response (no app-side filtering) - both confirmed SAP's own "productionmodelemergent" custom OData service (built by user's SAP admin via key-user OData Modeler) was still only returning the pre-update data just ~2 minutes after the SAP-side Release. This is a genuine SAP-side replication/reporting lag on that custom service, NOT a bug in this app's query/filter logic.
- Added a "Refresh Models" button (data-testid `refresh-production-models-button`) next to the Source of Supply picker on the Admin Test page (`ProductionConfirmationTestPage.js` only, per user's explicit ask) - forces a fresh live SAP re-check bypassing the existing same-ID dedupe guard, so staff can manually retry every couple minutes after releasing a model in SAP instead of having to re-type the Product ID field.
- Self-tested via screenshot (button renders, triggers a real SAP call, resolves to the picker).
- No backend changes needed - `get_source_of_supply_options` was already a live, uncached SAP call.


## Session (Sep 11 2026, continued #5) - RP ordering bug + strict sequence gate on Confirm
- User reported (screenshot, lot with MAZ42117272-TA): "TV ARM LEFT POWER COATING(END)" showed as the SECOND row right after RP_10, before RP_20..RP_90 - and asked for the Confirm button to be sequence-gated (can't confirm RP_90 before RP_80).
- Root cause found: `sap_production_lot_client._parse_lot_block()` just returned rows in SAP's raw XML document order (ConfirmationGroup/ReportingPoint blocks are NOT guaranteed to arrive in actual routing sequence) - this silently fed the Admin Test page's "Awaiting Prev Step" adjacent-pair diff (`awaitingByRowKey` in `ProductionConfirmationTestPage.js`) the WRONG previous-row reference for every RP after the misplaced one.
- Fix 1 (backend, affects both Production Confirmation pages since both use this shared parser): `_parse_lot_block` now sorts each lot's rows by the numeric suffix on `reporting_point_id` (RP_10->10 ... RP_90->90), with "END" (or anything non-numeric) always sorted last.
- Fix 2 (frontend, Admin Test page only, per user's scoping in the previous request): the "Confirm" button is now disabled whenever `awaitingByRowKey[...].awaiting <= 0` (nothing has cleared the previous Reporting Point yet), with a tooltip explaining why - enforces strict RP-sequence confirmation.
- Verified live against real SAP data: lot 70539 (RP10/RP20/END, all with real awaiting qty >0) confirmed correct order + all buttons enabled; lot 33215 (MARK_001=0, END=15000, i.e. END artificially "ahead") confirmed END's Confirm button correctly greyed out with "-15,000 (after MARK_001)".
- No testing_agent used - small, targeted, self-tested via direct SAP client queries + screenshot verification (2 real lots).


## Session (Sep 11 2026, continued #6) - "Cannot confirm: a by-product is expected but there's no Output Products line..." bug fix
- User hit this hard block trying to confirm RP_10 ("LEFT BLANK+PUNCHING") of lot 72647 (MAZ42117272-TA).
- Root cause confirmed live against SAP: RP_10 genuinely consumes the raw material (HRCOIL2.0X80.5) so a by-product (Iron Scrap) IS correctly expected - but in SAP, only the LAST Reporting Point of a multi-op routing ("END") ever carries an Output Products/MaterialOutput line with a target storage area; every intermediate RP's own `material_outputs` is legitimately empty by SAP's own routing design, not a data gap. Our `mainOutputRow` lookup only ever checked THIS row's own (always-empty) material_outputs, so it could never find a valid target storage area to auto-create the by-product line at any intermediate RP - hard-blocking confirmation entirely.
- Fix (both `ProductionConfirmationPage.js` and `ProductionConfirmationTestPage.js`): `ConfirmDialog` now accepts a `siblingRows` prop (all rows for the same lot) and falls back to any sibling RP's material_outputs (in practice always "END") to find a valid target storage area, instead of only checking the current row's own.
- Verified live on lot 72647/RP_10: dialog now shows "will auto-create it and post 3 KGM to SAP" instead of the blocking error (screenshot-confirmed).
- Self-tested (screenshot + code read), no testing_agent used - small, targeted 2-file fix.


## Session (Sep 11 2026, continued #7) - Return to Store Site/Plant dropdown empty (real incident: sap.p9@rampgroup.co.in)
- User's SAP admin correctly bound site "P9" to this user in Access Management (confirmed persisted, no save error) - yet the Manual Return Request's Site/Plant dropdown stayed completely empty with zero error shown.
- Root cause found: `/api/store-requests/known-sites` (auth_service.py page-permission table) only allowed `store_approval`/`stock_transfer` page access - it was never updated to also allow `production_confirmation` when the Sep 9 2026 "Return to Store" feature was added (unlike the sibling `/api/store-requests/journal` and `/api/store-returns` rules right next to it, which already correctly allow `production_confirmation`). A user with ONLY `production_confirmation` access (needed just to reach the Return to Store tab at all) got a silent 403 on this one specific site-list call - and the frontend's `.catch(() => {})` swallowed it entirely, showing an empty dropdown with no diagnostic trail.
- Fix: added `production_confirmation` to the allowed pages for `/api/store-requests/known-sites` in `auth_service.py`. Also fixed the silent catch in `ReturnToStoreTab.jsx` to show a toast with the real error on failure (prevents this class of bug from being invisible again).
- Verified end-to-end: seeded a synthetic account with the EXACT same profile (role=user, allowed_pages=[production_confirmation] only, bound_sites=[P9]) and confirmed `/api/store-requests/known-sites` now correctly returns `{"sites": ["P9"]}` instead of a silent 403.
- IMPORTANT CORRECTION made to user: `deployment_agent` only runs a deployment READINESS scan - it does NOT actually push code to production. Earlier in this session it was incorrectly implied that calling it deployed the app live to mrp.radishtechnologies.com. Corrected this - user must trigger the actual "Deploy" action via the Emergent platform UI themselves for any of today's fixes (Printed PO Ref, SFG shortage flag, GRN Reset Retry, RP ordering, by-product fallback, this Site dropdown fix) to actually go live on production.


## Session (Sep 11 2026, continued #8) - "Rule 2" reversed: partial store issue now closes for good
- User reported real incident: request 685734147 (PALL3286, 32/83.87 EA issued) stayed reopenable with "Issue Remaining Balance" enabled, which they now want changed.
- This directly REVERSES the earlier "Rule 2" (from a prior session: "if store has 3 of 5, issue 3 now and come back for the balance 2 later"). User's explicit new instruction: once ANY partial issue is genuinely accepted by SAP, the store request closes immediately for good - no reopening, ever, even with a real shortfall left. EXCEPTION (explicitly preserved): if SAP itself REJECTS the movement (0 actually posted, e.g. "Negative stock not permitted"), the request must still stay reopenable - this is a separate, pre-existing data-integrity safety net, not part of the reversed rule.
- Fix (`store_approval_service.py`): `run_issue_movements()` now only uses `resolved_balance_pending` when `sap_rejected_this_round` is true; every genuine partial issue (SAP accepted the movement, just less quantity) now sets `status=resolved` directly. `planner_decision()`'s "approve" path on a `partial_pending_planner` request also now always closes as `resolved` (never reopens).
- Frontend (`StoreApprovalPage.js`): updated the reopen notice + result messages to reflect the new narrower "SAP rejected" meaning (button renamed "Retry Issue" instead of "Issue Remaining Balance").
- Tested via `testing_agent` (iteration_155): new pytest suite `backend/tests/test_store_partial_close.py`, 7/7 pass, covers both branches (SAP accepts -> closes; SAP rejects -> stays reopenable) + planner-approve + full-issue regression. No issues found.
- REMINDER (carried from earlier this session): nothing in this session has actually been deployed to production yet - `deployment_agent` only runs a readiness scan. User must use the Emergent "Deploy" button themselves. The specific stuck request 685734147 lives only on production's DB and was NOT force-closed by this fix (only governs future issues going forward) - user may want to manually resolve that one existing record once deployed.


## Session (Sep 11 2026, continued #9) - Retry button gated by actual movement failure, not just status
- Real incident: legacy request P9-000051 (created BEFORE the Rule-2-reversal fix above) sat in "Balance Pending" with component 8201-000193's movement showing "Moved (275119)" (genuinely succeeded) - yet the Retry Issue button still showed, since it only checked the top-level status field.
- Fix: `isReopenable` (StoreApprovalPage.js) and `list_balance_pending()` (store_approval_service.py) now both additionally require at least one short component to have a genuinely failed/not-ok `goods_movement` on record - not just `status === "resolved_balance_pending"`. A legacy record where every attempted movement actually succeeded no longer shows the Retry button or appears in the "Balance Pending" queue at all.
- Verified directly: seeded the exact legacy scenario (successful movement + shortfall) and confirmed it's now excluded from `list_balance_pending()`.

## Session (Sep 12 2026) - GRN hybrid architecture: SOAP creates the notification, Playwright only finalizes
- Goal: replace the fragile Playwright PO-search step in the Supplier GRN flow (`sap_playwright_supplier_pgr_service.py`) with SAP's `ManageStandardInboundDeliveryNotificationIn` SOAP service, after user's SAP Admin set up the "Input of Advanced Shipping Notification" Communication Arrangement (Application Protocol = Web Service, user `_EMERGENTBOM` = same as `SAP_SOAP_USERNAME`/`SAP_SOAP_PASSWORD` already used everywhere else).
- New module `sap_inbound_delivery_notification_client.py`: `check_maintain_bundle()` (pure validation, commits nothing) and `maintain_bundle()` (real create, optional release). Built by reverse-engineering the live WSDL (`SAP_SOAP_INBOUND_DELIVERY_NOTIFICATION_ENDPOINT` in `.env`) plus iterative `CheckMaintainBundle` probing against real PO 29482 - required fields not obvious from the schema alone: Item `TypeCode=14`, `ItemProduct/ProductID`, and the PO reference's own `TypeCode=001` (all three together needed - partial combos either no-op'd or caused a RABAX).
- **Investigated and REJECTED full elimination (SOAP `release=true`, zero Playwright)**: real live test confirmed release succeeds but Delivery Status stays "Not Started" (creates a pending Warehouse Task) unless the site's Logistics Model is task-based. Site P1 only allows ONE active Standard Receiving model - switching it is a real, disruptive site-wide change, not a toggle (confirmed live: "Without Tasks" is locked/uneditable once a model exists, must create a whole new one and replace). Even after the user did this (temporarily), BOTH `InboundDeliveryRelease`/`InboundDeliveryPGRBackground` OData actions AND `QuerySiteLogisticsTaskIn` are confirmed dead ends (blocked/erroring) for completing the resulting task - there is no API path to finish a task-based receipt on this tenant. User reverted the site back to the original non-task-based model after this test.
- **Final hybrid architecture implemented** (`sap_playwright_supplier_pgr_service.py`, `supplier_shipment_service.py`, `server.py`): `_post_one_po` now (1) calls `maintain_bundle(release=False)` to create the Inbound Delivery Notification referencing each item by PO+ItemID directly (no PO search, no product-text row-matching - structurally eliminates the whole multi-line-same-product bug class), (2) Playwright opens that notification BY ITS OWN ID in the "Inbound Delivery Notifications" view (reusing `_open_inbound_delivery_notifications`/`_search_delivery` from `sap_playwright_pgr_service.py`, switched to the "All Delivery Notifications by Selection" base view since our SOAP-created docs don't show under the default "Advised" filter), (3) fills Actual Quantity (still needed - SOAP's `DeliveryQuantity` lands in "Planned Quantity", not "Actual") + clicks Save and Close. Removed entirely: `_open_purchase_orders`, `_search_po_exact`, stale-table retry logic, "Remove Zero Quantity Items", manual Notification-ID/Date typing (~140 lines removed).
- Verified end-to-end through the REAL production API path (not just adhoc scripts): created shipment `XXMABW` via Supplier Portal (vendor `RAD-P2-S`, testing-mode `as_vendor` override), approved via `/api/admin/grn/{doc_code}/approve` - job completed in ~60s with `sap_sync_status: posted`, real SAP Inbound Delivery `53054`, events log showing the full new flow. Confirmed correctly in the GRN Approval UI's "Confirmed GRNs" tab (no frontend changes were needed - existing UI already renders this result shape).
- Estimated impact: ~50-55% of the old Playwright script's code eliminated, ~35-45% faster per PO (per the code's own removed wait/step counts), 100% of the session's specific bug class (stale search, multi-line-same-product row mismatch) structurally eliminated.

## Session (Sep 12 2026, continued) - Two real bugs found in live testing, both fixed and re-verified
- **Bug 1 (view-switch silently clicked the wrong dropdown)**: `_switch_to_all_deliveries_view`'s old selector (`.sapMSelect, [role="combobox"]").first`) wasn't scoped to the page - it could match the SAP Fiori shell's own global "All Categories" search dropdown instead of the page's own "Advised Delivery Notifications" selector, leaving the view unswitched and the search silently returning "No records found" forever (screenshot-confirmed by user). Fixed: target the element by its actual visible text instead of a generic class/role selector; retry the whole switch+search loop together, not just the search.
- **Bug 2 (notification ID collision across different shipments) - more serious**: real incident, two different real shipments for the same PO (`5XL5EE` supplier_doc_num "test_grn", `FYXUKN` supplier_doc_num "test_GRN") produced colliding SAP Delivery Notification IDs because SAP normalizes/uppercases IDs - the old scheme keyed off free-text `supplier_doc_num`. `FYXUKN`'s retry hit SAP's "already exists" (handled) then silently opened/completed `5XL5EE`'s ALREADY-FINISHED document and reported success - `FYXUKN`'s real goods were never received. Fixed: `notification_id` now always keys off `doc_code` (this app's own unique 6-char shipment code) instead of `supplier_doc_num` - guaranteed unique, never user-typed, never case-collidable. Threaded `doc_code` through `group_items_by_po_for_gr` -> `post_goods_receipt_via_ui` -> `_post_one_po`.
- Both fixes verified live on the actual affected shipment `FYXUKN` via the real retry endpoint: now creates its own distinct notification `FYXUKN-29482`, posts correctly (`sap_sync_status: posted`, Inbound Delivery `53064`, correct items 3&2, qty 2&3).

## Session (Sep 12 2026, continued #2) - Third real bug: stale hidden dropdown option crashed multi-PO shipments + retries reprocessed already-posted POs
- Real incident, live production error logs: multi-PO shipment `4EBPH2` (PO 29429 + PO 29482) - PO 29429 posted fine, PO 29482 crashed with `Locator.click: Element is not visible`. Root cause: `_switch_to_all_deliveries_view`'s `get_by_text(...).first` matched a STALE, HIDDEN `<li role="option">` left in the DOM from PO 29429's own earlier interaction with the same dropdown (same browser page reused across every PO in one shipment) instead of the actual visible trigger - `force=True` can't click an element with no bounding box. Fixed: filter candidates to the one that `is_visible()` before clicking, for both the trigger and the option itself; returns `False` instead of raising so the existing retry loop handles it instead of crashing the whole PO.
- Second bug found in the same investigation: retrying a multi-PO shipment resubmitted ALL its POs, including ones that already posted successfully in a prior attempt - re-processing an already-"Finished" notification would make a genuinely-successful PO look like a fresh failure (Post Goods Receipt is disabled once Finished). Fixed in `_start_supplier_grn_job` (server.py): skip any PO already marked `"posted"` in the shipment's last `sap_gr_result.per_po` before calling `post_goods_receipt_via_ui`, then merge those prior results back in before `finalize_goods_receipt` so the stored history for those POs isn't lost.
- Verified live on the actual affected shipment `4EBPH2`: retried successfully, PO 29429's prior success preserved, PO 29482 processed cleanly (no crash, no reprocessing) - `sap_sync_status: posted`, Inbound Deliveries `53065`/`53066`.

## Session (Sep 12 2026, continued #3) - Diagnostics modal picked the wrong PO + real data-quality gap on PO 29456
- Real incident, shipment `K7G2N5` (3 POs: 29429, 29456, 29482) - user saw a "PO 29429 - Diagnostics" modal showing "Failed at step: unknown step"/"No error text captured" despite its own events showing a clean success end-to-end. Root cause (frontend only, `GrnApprovalPage.jsx` lines ~789/1078): `per_po.find((p) => p.events?.length)` picked the FIRST entry with any events at all - which was the already-succeeded PO 29429 - instead of the one that actually failed (PO 29456). Fixed both occurrences to `per_po.find((p) => p.status !== "posted" && p.events?.length)`.
- The real failure was PO 29456, item 1: SAP rejected the SOAP create with a confusing "No inbound delivery request exists for purchase order reference 29456 - 1". Traced to a genuine pre-existing data gap: this PO line's `product_id` is `None` in SAP's own PO data (confirmed in `supplier_portal_po_cache` directly, not something this app caused) - the SOAP call was literally sending the string `"None"` as the Product ID. Fixed: `_post_one_po` now checks for a missing `product_id` before ever calling SOAP, returning a clear "skipped" error ("Product ID is missing in the cached PO data - ask SAP Admin to check this PO line's product master link") instead of wasting a real SAP write attempt on a doomed call.
- Verified live: retried `K7G2N5` - PO 29429/29482 stayed posted (53067/53068), PO 29456 now cleanly skipped with the new clear error, no wasted SAP call.


## Session (Sep 12 2026, continued #4) - Readable error messages, persistent Diagnostics instead of a vanishing toast
- User's ask: raw `JSON.stringify(sap_gr_result)`/`JSON.stringify(sap_movement_result)` dumps in toast descriptions were unreadable, and the toast disappears before anyone can read it anyway.
- Added `summarizeGrResult()`/`summarizeMovementResult()` (GrnApprovalPage.jsx) - short human sentences ("2 of 3 PO(s) posted. PO 29456: <reason>") replacing every raw JSON dump across approve/retry-goods-receipt/retry-movement toasts.
- Added `openDiagnosticsIfFailed()` - auto-opens the (already persistent, doesn't auto-dismiss) Diagnostics modal on any partial failure, instead of relying on the user finding a "View Diagnostics" button under a toast that's already gone.
- Diagnostics modal itself now distinguishes "Skipped - never sent to SAP" (data-issue POs like 29456, no `failed_step`) from "Failed at step: X" (genuine Playwright/SAP crashes) - previously always showed the crash-only wording even for skips.
- Verified via screenshot: modal auto-opens on K7G2N5, shows "Skipped - never sent to SAP" + the exact Product ID reason, no JSON anywhere.


